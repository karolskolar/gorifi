# Gorifi — Friends portal + guest flow redesign ("Podpultovka Neobrutal PP") — Backlog

Task definitions live in **`docs/specification/02…06-*.md`** (UC IDs are those files'; the design
bundle is `docs/design/friends-portal-redesign/`, its `theme.css` + 17 screenshots are the fidelity
reference). This file is the dependency-ordered build plan derived from them.

**Legend** — `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked.
One row = one shippable pipeline run (`/next-task`). **Top-to-bottom = dependency order** — never
start a row before the rows above it that it depends on. `⚠` marks a cross-task seam: what an earlier
task deliberately left unfinished and which later row completes it. ` · model=heavy` tags slices worth
a stronger model (money paths, state machines, dense pin surfaces); untagged rows inherit the session model.

> **Stack note:** Vue 3 + Vite + Tailwind 3, plain JS. Frontend-only re-skin — **no backend, schema
> or API change of any kind**; every GSO/auto-save behavior contract in CLAUDE.md still holds.
> The e2e harness already exists (`e2e/`, Playwright, target-agnostic via `BASE_URL`) at a
> **238 passed / 3 skipped** baseline — no harness task needed. Every row must leave the suite green;
> the ONLY sanctioned edits to existing specs are: 2 in `guest-link.spec.js` (RD-KG-2) and 5 listed
> in 06 §UC-GX-011 (spread over RD-GX-2/3/4). Anything beyond that list is a regression.
> Admin invariance is a hard gate on every row: no `pp-*`/`neo/`/theme classes under admin views.

> **Open product decisions** (all have spec-recorded defaults; none block a row): index.html
> title/lang/favicon rebrand (02/03 — default: leave "Gorifi"); positive-balance styling (03 —
> default: green mono); transactions modal restyle (03 — default: keep as-is); appbar subtitle
> "name · code" source (04 — default: name only); locked-cycle Zaplatiť shortcut for unpaid friends
> (04 — default: keep hidden, money touchpoint); share-sheet title "Objednávka Gorifi" → Podpultovka
> (05); `13-shot.png` duplicates the cancelled state — verify the editable status state against the
> live prototype (06).

> **Open SPEC amendment, raised by RD-DS-4 — affects every row that composes a form on `NeoModal`
> (RD-FL-2, RD-FL-6, RD-FO-4, RD-GX-1, RD-GX-3).** A text-selection drag that *starts* inside
> `.m-body` and *releases* over the scrim **closes the modal**. Reproduced: `mousedown` on a
> paragraph, drag out, `mouseup` on the scrim → the DOM `click` target is the nearest common
> ancestor (`.modal-scrim`), so `@click.self` fires. This is **faithful** to UC-DS-010's fixed
> structure block and to `ui.jsx`, so it is not a defect in RD-DS-4 — but the shell carries the
> profile, forced-password-change, pickup, checkout and guest-identity dialogs, and losing a
> half-filled form by dragging to select text is real data loss. Fix needs a UC-DS-010 amendment:
> additionally require the *mousedown* to have originated on the scrim
> (`@mousedown.self="scrimDown = true"` / `@click.self="scrimDown && requestClose()"`, reset on any
> `mousedown`). Decide before the first form-bearing modal lands.

---

## 0. Design system foundation (02) — inert until screens apply `.app`

- [x] RD-DS-1  friends-theme.css scoped port (adaptations A1–A8) + shadcn `--ui-*` token de-collision + Google Fonts + Tailwind `pp` palette/fonts — `02 §UC-DS-001..005,013,014` ⚠ applies `class="app"` to NO view — each screen row below starts by mounting it; index.html title/lang stays untouched (open decision); `.pimg` no-photo fallback OPEN is closed by RD-FO-2. Acceptance: before/after screenshots of `/` and admin pages identical, suite 238/3, `vite build` green.
- [x] RD-DS-2  neo/ primitives: NeoIcon (14 glyphs, verbatim ui.jsx paths) + BrandChrome (appbar/hazard/static ticker ×3) — `02 §UC-DS-006,007` ⚠ per-screen ticker copy + slot content supplied by consuming rows; e2e smoke deliberately deferred to RD-FL-2 (module 03 is the primitives' regression net).
- [x] RD-DS-3  neo/ form primitives: NeoStepper (incl. `decTestid/valTestid/incTestid` + `incDisabled` props) + NeoCheckbox (incl. `disabled`) + snapTab helper + money convention (`fmtEur`, Courier Prime) — `02 §UC-DS-008,009,012` ⚠ stepper has NO max — stock ceilings stay in views; testid pass-throughs exist for RD-FO-2/RD-GX-1's pinned e2e hooks; NeoStepper/snapTab e2e smoke lands with RD-FO-1/2.
- [x] RD-DS-4  neo/ overlay primitives: NeoModal (Teleport shell, scrim/Esc/`closable:false`, scroll-lock, focus return, one-at-a-time) + NeoCopyRow (2 s "Skopírované!" flip, timer restart, `valueTestid`) — `02 §UC-DS-010,011` ⚠ shell only — every concrete modal is composed by later rows; NOT radix (`ui/dialog` stays admin-only); `valueTestid` consumed by RD-KG-2.

- [x] RD-DS-5  **Module-02 amendments required by module 03** (added 2026-08-07, not in the original backlog — both items were surfaced by RD-DS-2/RD-FL-2 review and RD-FL-3 is blocked on them): (a) **UC-DS-006 amendment** — BrandChrome's fixed three slots cannot express the portal appbar, so add an `#after-titles` slot (between `.titles` and `.grow`) plus a titles-click affordance, and amend the UC text; (b) **UC-DS-001 port amendment** — `friends-theme.css` gains `line-height:normal` on `.inp`/`.btn`, because Tailwind preflight's `button,input,…{line-height:inherit}` inflates every field by 4.5 px and every button by 3 px against the canon. ⚠ Both touch **shared design-system files** that every later row consumes — do them together, once, rather than per-screen.
- [x] RD-DS-6  Self-host the three brand webfonts (Figtree, Darker Grotesque, Courier Prime) so they survive the deployed CSP — follow-up to RD-DS-1 · ⚠ **found on staging after the effort merged**: `style-src 'self'` blocks the `fonts.googleapis.com` stylesheet and `font-src 'self' data:` blocks the `fonts.gstatic.com` woff2, so `document.fonts.size` is **0** and the whole redesign renders in the fallback face. RD-DS-1 (`27c7dd9`) introduced the external `<link>`; 24 rows built on it. ⚠ **Every gate in this effort ran against Express on `localhost:3997` with NO nginx and therefore NO CSP header** — 525 green tests were not evidence on this point. ⚠ Fallback is the TIGHT face (RD-FO-1: 320px tab headroom 41.3 → 32.5px, 3.6px with a 1-digit badge, a 3-digit badge OVERFLOWS by 2px). ⚠ **`latin-ext` is a separate subset and is NOT optional** — Slovak č/š/ž/ľ/ť/ď/ň/ĺ/ŕ are U+0100–017F; shipping `latin` only makes them fall back mid-word. Self-host rather than relaxing CSP: tightening CSP is an open follow-up, and `font-src 'self'` then needs no deploy change.

## 1. Friend login + portal (03) — first consumer, primitives' regression net

- [x] RD-FL-1  FriendPortal scaffold: `.app` root + BrandChrome in all 3 states + legacy-login pin (untouched branch) + loading/error restyle — `03 §UC-FL-001,003` ⚠ legacy/transition login + voucher modal deliberately keep transitional look (out of scope); `gorifi_friend_auth` localStorage shape is written directly by 3 e2e suites — byte-identical.
- [x] RD-FL-2  Modern login (Kto klope?, eye-toggle, remember-me, invite explainer) + forced password-change on NeoModal + new modern-mode e2e — `03 §UC-FL-002,012` ⚠ first real e2e smoke for BrandChrome/NeoModal/NeoCheckbox/NeoIcon (02's deferred obligation); modern specs must provision `auth_mode='modern'` and restore legacy — the shared seed stays legacy. ⚠ **BLOCKER from RD-DS-4 — `NeoModal` has NO focus trap, and this row is where that stops being cosmetic.** `FriendPortal.vue:1272` currently renders the forced password-change gate with radix `Dialog`/`DialogContent`, which **does** trap focus and mark outside content inert. `NeoModal` sets `aria-modal="true"` (so AT treats the outside as inert) but **Tab still walks out into the page behind the scrim** — measured order `m-x → inside-btn → copy → foot-btn → BODY → opener → …`. Porting the gate to `NeoModal` as-is would therefore make **Tab a functional bypass of a `closable:false` gate that blocks the portal until resolved** (§UC-FL-012) — a behavioural regression, not a cosmetic gap. Add the trap to `NeoModal.vue` here as an **additive** change (suggested: opt-in `trapFocus` prop defaulting to `closable === false`) and pin it with the modern-mode e2e this row already owns. Everywhere `closable:true` the gap stays cosmetic, since Esc and the scrim remain available.
- [x] RD-FL-3  Authenticated appbar (name/uid/pencil/Pozvať chip/logout) + FriendBalanceCard restyle — `03 §UC-FL-004,005` ⚠ appbar taps are triggers for RD-FL-6/7 (modals stay old-skin until then); `BalanceBadge.vue` NOT modified (shared with admin) — card renders its own 3-state span. ⚠ **BLOCKER surfaced by RD-DS-2 — needs a spec decision before this row starts:** UC-DS-006 declares BrandChrome's structure fixed with exactly three slots (`#leading`, `#titles`, `#trailing`), and none of them can express this appbar. `portal.jsx:97` puts `cursor:pointer` + `onClick` on the `.titles` div **itself** (component-owned, so a consumer cannot attach a handler), and `portal.jsx:101` puts the pencil **between `.titles` and `.grow`** — `#trailing` renders *after* the spacer and would fling it to the right edge, `#leading` is the wrong side, and `.titles` is `flex-direction:column` so putting it in `#titles` stacks it under the name. See `screenshots/02-shot.png`: the pencil sits next to "LEGO", not at the edge. **RESOLVED by RD-DS-5** — `BrandChrome` now has an `#after-titles` slot (between `.titles` and `.grow`) and a `titlesAction` prop + `titles-click` emit, with UC-DS-006 amended to match. ⚠ This row must **discharge two obligations RD-DS-5 deliberately left**: (i) the positive `#after-titles`/keyboard e2e path, since no shipped consumer opted in and providing one would have meant editing a view — the acceptance criteria are already written into UC-DS-006 for you; and (ii) the now-stale hand-off comment at `FriendPortal.vue:783-784` ("BrandChrome's three fixed slots cannot express that position"), which this row outdates and must delete. `titlesAction` is a **label-carrying String, not a Boolean**, because `.titles` holds the friend's name and code — a bare `role="button"` announces "LEGO lego-4821, button", the person's name and never the action's. ⚠ **Two residuals handed over by RD-FL-1** (both minor, both spec-compliant where they stand, neither to be inherited as precedent): (1) the `voucherResolved` banner wrapper still carries `max-w-4xl` while the authenticated column moved to `max-w-[760px]` — measured at 1180 px the banner is 896 px @ x=142 against a 760 px column @ x=210, a **68 px overhang each side**. UC-FL-001 pins the banner "visually untouched" so RD-FL-1 could not move it; move it to `mx-auto w-full max-w-[760px] px-4 sm:px-7` when the voucher surface is next touched, or record it as an accepted residual in 03's spec. (2) **`error` currently surfaces NOWHERE for an authenticated friend** — a pre-existing gap, larger than the dead branch that exposed it: `v-else-if="error && authState === 'loading'"` is unreachable (the outer catch in `loadInitialData` sets `error` and `authState='login'` in the same breath), while the four other writers — `resolveVoucher`, `saveProfile`, `saveSubscriptions`, `openInviteModal` — all run under `authState === 'authenticated'`. So a failed profile save, subscription save, invite-code fetch or voucher resolve is silent today. Keep the restyled branch (the spec asks for it, it costs nothing) and add a `.banner danger` at the top of the authenticated column keyed on `error && authState === 'authenticated'`.
- [x] RD-FL-4  Cycle list: header + gear trigger + cycle cards (badges, plan block, folded "Objednané ·" qty) + archive fold — `03 §UC-FL-006,008` · model=heavy ⚠ **CORRECTED — this row's original text was wrong and unimplementable.** "Ships WITHOUT the share row" cannot hold: `guest-link.spec.js:305-320` clicks the share button *inside* `cardFor()` and asserts `toHaveCount(0)` on a locked card, and that spec may not be edited — so removing the button would leave `main` red for the whole interval between RD-FL-4 and RD-FL-5. What actually shipped: the **unchanged shadcn ghost button** as a transitional control (squarely inside UC-DS-002's guarantee), positioned as the card's **last child directly under the badge row** — exactly where `portal.jsx:73-81` puts the designed row. **RD-FL-5 REPLACES that element in place**, it does not add one. Locked cycles stay clickable (read-only view is 04's).
- [x] RD-FL-5  Share row + colleague-count badge: seq-guarded per-open-cycle `GET /guest-links/cycle/:id` (existing endpoint, frontend-only addition — the module's ONLY script change) + GuestShareDialog entry contract — `03 §UC-FL-007` ⚠ dialog INTERNALS are RD-KG-2's — wire only `:open/:cycle-id/:cycle-name` + `@click.stop`; visible "Zdieľať" + `aria-label="Zdieľať s kolegami"` keeps guest-link.spec.js unmodified. ⚠ **This row REPLACES RD-FL-4's transitional shadcn share button in place** (it is already the card's last child, under the badge row); the target geometry is written out verbatim in a seam comment at that site. `portal-cycles.spec.js`'s adjacency assertion was deliberately written structure-agnostic — it matches the share affordance by accessible name from **either** source (visible text now, `aria-label` later) and searches the last child's subtree — so wrapping the button in UC-FL-007's border-top `div` needs **no** spec edit. ⚠ Two rules apply that this row's original text didn't restate: the GSO-T2 **`loadSeq` guard** on the new per-cycle fetch (UC-FL-007 does say it), and — RD-FL-3's rule, reinforced by RD-FL-4 clearing `showArchive` — **`switchUser()` must clear the new counts map**. Here that is *not* the mild case: a stale count is **another friend's colleague data**.
- [x] RD-FL-6  Profile modal on NeoModal: read-only uid/username row, name/Packeta fields, password-change fold, errors move into modal body — `03 §UC-FL-009` ⚠ `saveProfile` side-effects preserved (appbar name from RD-FL-3 updates immediately).
- [x] RD-FL-7  Subscription modal (NeoCheckbox rows) + invite modal (NeoCopyRow) — `03 §UC-FL-010,011` ⚠ NeoCopyRow ABSORBS the view's `copyInviteLink()`/`inviteCopied` — delete once unused; never hardcode the prototype's demo host. ⚠ **RD-FL-6's rule applies to both modals: an OPENER must clear `error`.** `error` is one page-level ref shared by `saveProfile`, `saveSubscriptions`, `openInviteModal` and `resolveVoucher`, and once a modal renders it as its own `.banner.danger.slim` (while suppressing the page banner), a message left by any *other* writer opens inside it looking like that modal's own failure — with no `Chyba:` prefix and no dismiss ×. `openProfileModal()` now clears it on open; do the same in `openSubscriptionModal()` (`openInviteModal` already clears). Consequence to keep: opening a modal is treated as abandoning the previous attempt, so it dismisses that message for good. ⚠ **Also do the two cheapest items of the anti-leak plan below** (`portal-session-boundary.spec.js`, and the `sessionRef` factory) — this row touches `switchUser()` anyway, and the class has now bitten **six times in six rows**. Worth considering while you are in here: a dedicated `profileError` ref would remove the need for **both** the page-banner suppression **and** the `openProfileModal` clear, retiring the rule this bullet propagates.
- [x] RD-FL-8a **Module 03 debt paydown** (split out of RD-FL-8 by the orchestrator, 2026-08-08). UC-FL-013 defines RD-FL-8 as a **verification** row, but four *code* changes had accumulated on it — including a whole-view refactor — which is not a reviewable diff alongside a fidelity audit, and the line-height fix must land **before** the audit re-baselines fidelity. So: fixes here, audit in RD-FL-8b against the fixed state. The four items are listed on RD-FL-8b below.
- [x] RD-FL-8b Module 03 closeout: pinned-selector audit, 378/1180 px fidelity vs 01/02/17-shot + live prototype modals, 320 px eyeball, admin invariance re-assert — `03 §UC-FL-013` ⚠ **The four items below are RD-FL-8a's, not this row's — this row VERIFIES them.** (1) **The structural end to the `switchUser()` leak class** (item 4 of the plan below): extract the authenticated surface into `FriendPortalSession.vue`, rendered `v-if="authState === 'authenticated'"` with `:key="friendId"`, so logout destroys the instance and every ref inside is re-created — **there is no list to forget**. The view is ~2000 lines with ~40 top-level refs serving two disjoint lifetimes; the parent keeps only the six login-screen refs, small enough to audit by eye. In-repo proof the pattern works: `GuestShareDialog.vue:55-77` holds session data too and has **never** leaked, because it resets on both edges of its `open` watch. This row already scopes an audit of this view, so the `:key` lands naturally. (2) **The `.field-lbl`/`.field-help`/`.sub` line-height residual** — Tailwind preflight's `html{line-height:1.5}` reaches every plain-text theme class, so labels measure 19/20 px against the canon's 15/15. Same defect RD-DS-5's A9 fixed for `.inp`/`.btn` only; **the reviewer's advice was that it land before module 04 starts**, since 04–06 will write pixel-fidelity assertions against whatever ships and re-baselining costs more than the one-line fix. If it slips past module 04's start it MUST be caught here. (3) **RD-FL-5's count storm** — `loadGuestCounts` `Promise.all`s one `GET` per **open** cycle in a single tick, before `getSubscriptions` is issued. The code comment says "typically 1–2"; the e2e DB has **135**, and the portal's own critical requests then starve behind the browser's 6-connection limit (this really happened — it flaked a test until `muteGuestCounts` was added). Cycles are never auto-closed, so it degrades silently with age. Cap concurrency or fetch only for cycles near the viewport, and drop the "typically 1–2" claim. (4) **Converge the THREE-WAY `error` strategy** now coexisting in one view — page banner / suppressed shared ref (`error && !showProfileModal`) / dedicated ref (`inviteError`). The extraction is the right place: give the profile modal a `profileError` and the subscription modal a `subError`, after which `error && !showProfileModal` collapses to `error` and the suppression pattern retires. ⚠ `subError` is not cosmetic: a failed `saveSubscriptions()` writes the shared `error`, the modal **stays open**, and the page banner renders **behind the scrim** with its dismiss unreachable — measured, and not a regression (radix behaved identically), but it is exactly the condition RD-FL-7's `inviteError` rationale argues against, so the user sees a modal that simply "did nothing".

> ### ⚠ Ending the `switchUser()` leak class — a plan, after SIX consecutive rows each found a leak
>
> RD-FL-3 found `error`/`voucherResolved`/`subscriptions`; RD-FL-4 `showArchive`; RD-FL-5 `guestCounts`;
> RD-FL-6 the credential-setup block (**friend B's dialog auto-opened pre-filled with friend A's
> plaintext password**) and then `showLoginPassword`. Reviewing a ref list has demonstrably not worked —
> the sixth miss was found by a reviewer's own sweep, not by the list.
>
> 1. **Done in RD-FL-6.** The one-line `showLoginPassword` clear.
> 2. **RD-FL-7 — the highest-value artifact, and the only one that would have caught all six.**
>    A generic `e2e/tests/portal-session-boundary.spec.js` that enumerates the **surface, not the refs**:
>    log A in (transition mode, one instance, no reload), walk the whole authenticated surface — open
>    every disclosure and every modal reachable from the appbar and the cycle cards, fill every
>    `input`/`textarea` with a unique `SENTINEL-<n>`, click every `[role=checkbox]` and reveal toggle —
>    log out, log B in, re-walk, then assert three **surface** invariants: `page.content()` contains no
>    `SENTINEL-`; every visible password-ish field (`autocomplete` contains `password`, or id matches
>    `/password|heslo/`) has `type === 'password'`; every field value is `''` or re-derived from B's own
>    profile GET. It needs **no knowledge of the ref list**, so it keeps working as later rows add refs.
>    Known limit, and acceptable: it only covers state that *renders* — a leak that never renders is not
>    the hazard.
> 3. ~~**RD-FL-7 — a declaration-site marker**~~ **DROPPED (orchestrator decision, RD-FL-7):** item 4
>    **supersedes** it — the component extraction deletes the need for a factory entirely, so building it
>    now means writing ~40 call sites that RD-FL-8 then removes. Item 2 is the one that pays off
>    immediately *and* is what validates item 4's extraction, so RD-FL-7 does 2 and RD-FL-8 does 4.
>    Kept below for the record in case item 4 is ever deferred: a `sessionRef(makeInitial)` factory
>    that registers its own reset, so `switchUser()` collapses to `resetSessionState()` plus the four
>    steps that genuinely must stay hand-written (`clearTimeout(usernameCheckTimeout)`, `guestCountSeq++`,
>    `clearFriendsPassword()`, `localStorage.removeItem`). ⚠ It must take a **factory, never a literal** —
>    `{}`/`[]` must not be shared across resets. The reviewer's question then moves from "is this ref in a
>    list 400 lines away?" to "`ref` or `sessionRef`, right here?", answered where the answer is obvious.
> 4. **RD-FL-8 — the fix by construction.** See that row.
>
> ⚠ **Do NOT reach for a lint rule.** `frontend/package.json` has no ESLint at all, so it would mean a
> toolchain for one rule — and the rule still cannot tell a session ref from a device preference
> (`rememberMe`, `loginTab`, `authMode` must **not** be cleared), so it would need a hand-maintained
> allowlist: the same list, in a different file.
>
> **Sweep result, so the next reviewer needn't redo it.** Safe **by reachability, not by construction**:
> `pendingVouchers`/`currentVoucher`/`showVoucherModal` (all four call sites await `checkPendingVouchers()`
> *before* `authState='authenticated'`, and a non-empty list opens a full-screen overlay with **no dismiss
> control**, so the logout control cannot exist while any is dirty); `showSubscriptionModal`/`subCoffee`/
> `subBakery`, `showInviteModal`/`inviteCopied`, `shareCycle` (radix/child overlays block the appbar, and
> each is recomputed on open); `forcedPasswordChange` + fields (`closable:false` + trapped — the deliberate
> exception). ⚠ **Each is one product decision away from becoming the seventh leak** — e.g. giving the
> voucher modal a "later" button would immediately expose another friend's euro amount and cycle name.

> ## ✅ MODULES 02 + 03 ARE CLOSED (2026-08-08). What module 04 inherits.
>
> Ten rows: RD-DS-1..5 (design system) and RD-FL-1..8b (friend login + portal). Suite
> **238 → 353 passed / 3 skipped**, **8 spec files added, ZERO pre-existing specs modified**
> across the whole effort (`git diff --diff-filter=M 7c3f85e..HEAD -- e2e/tests/` is empty).
> Admin invariance proven against a build of **`7c3f85e`** — the commit *before* RD-DS-1 —
> across 12 routes and ~20.5 M pixels, which is stronger than any single row's parent-commit
> check.
>
> **Read these five before writing a line of module 04:**
>
> 1. ⚠ **`.app>*{position:relative;z-index:1}` neutralises EVERY Tailwind positioning utility
>    on a direct child** — `fixed`, `absolute`, `sticky`, even `relative z-10`. It fails
>    **silently**: no build error, no failing spec. `FriendOrder.vue`'s `<header … sticky top-0>`
>    is a direct child and un-sticks the moment `.app` lands; its `purpose-tabs` strip's
>    `top-16` was calibrated against that header and will pin 64 px below the viewport once
>    non-sticky `BrandChrome` replaces it. Full section in CLAUDE.md.
> 2. ⚠ **A class list cannot reach plain text.** Any element with an inline `font-size` and no
>    theme class inherits preflight's `line-height:1.5` and renders **4–13 px** taller than the
>    canon. A10 now covers 26 selectors — do **not** grow it to chase call sites; add
>    `line-height:normal` at the call site. A10 was written on a screen sweep twice and
>    **falsified twice**; §UC-DS-001 now carries the reproducible derivation (font-bearing rules
>    minus line-height-bearing rules) so the next author re-derives instead of trusting.
> 3. ⚠ **Your primitives were inflated and are now canon-accurate.** `.cartbar .*`,
>    `.stepper .val`, `.vbox .*`, `.suborder .items` (**+25.5 px**), `.tbl`, `.confirmbox` were
>    all fixed in RD-FL-8b **specifically so 04–06 baseline against the canon, not against
>    drift**. Measure against the current build; anything measured before 2026-08-08 is stale.
> 4. ⚠ **Session-scoped state dies with the session — now by construction.** Six consecutive
>    rows each found a leak (worst: friend B's dialog auto-opening pre-filled with friend A's
>    **plaintext password**). Fixed by extracting the authenticated surface into a `v-if`-gated
>    child keyed on a **handshake sequence, not an identity** — keying on identity leaks the
>    payload, because it flushes at the first `await` while the payload is seeded once at setup.
>    If module 04 extracts `FriendOrder` the same way, key on the handshake. A fragment (multi-root)
>    child silently no-ops any wrapper directive, `v-show` included.
> 5. ⚠ **Playwright matches `getByRole(…, {name})` as a case-insensitive SUBSTRING** unless
>    `exact: true`. `NeoModal`'s × is `aria-label="Zatvoriť dialóg"` — a **synonym**, because
>    "Zavrieť dialóg" still collided with the spec-verbatim "Zavrieť" footer buttons that three
>    **non-editable** guest specs query unscoped. Don't rename it back.
>
> **2026-08-08 · RD-FO-1 · `task/rd-fo-1` · [PR #55](https://github.com/karolskolar/gorifi/pull/55)** — module 04 opened. Suite **353 → 367 passed / 3 skipped**; `FriendOrder.vue` net **−280** lines; **zero pre-existing specs modified**. The predicted `.app>*` hazard was handled and independently swept: `.app` has exactly **4** direct children in all four states (BrandChrome is multi-root, so its three land at root, plus the page column), none carrying a positioning utility; every dialog portals to `body`; the appbar moves 1:1 with the page; `.cat-tabs` is sticky at `top:0`/`z:40`; the cart footer is still `fixed` at **depth 2**. ⚠ **RD-FO-1 found a real bug while merging two divergent product lists** — see the RD-FO-2 row. ⚠ **A "4px of headroom" claim was WRONG and the correction matters**: it compared a tab's `clientWidth` (132) to its own `border-box` (136.2) — **the same box**, the 4.2px being the tab's transparent border. The real model is **`min₁ + min₂ + gap ≤ content`** on a `grid-auto-columns: 1fr` = `minmax(auto, 1fr)` track pair, and the binding factor is the **badge**, not the label: headroom runs **41.3px** (no badge) → **12.3** (1-digit, Figtree) → **3.6** (1-digit, fallback font) → **−1.0** (2-digit, fallback — i.e. 10+ colleagues, entirely realistic) → 3-digit **overflows by 2px**. **No spec exercised the badge at 320px**, so `order-shell.spec.js`'s 320px test now creates a real guest sub-order and gates on the badge being visible before measuring; mutation-checked by widening `.tabgroup .tab` padding, which drives headroom to −11.73 and reddens it. Note the headroom model goes negative **one case before** `scrollWidth − clientWidth` reports anything, which is why the headroom assertion earns its place beside the overflow one — and that the shipped test pins the 12.3px case only while Google Fonts is reachable.
>
> Also settled: the page column is **`mx-auto w-full max-w-[760px] px-4 sm:px-7 py-4 sm:py-7`**
> (16/28 both axes, per the prototype) — four axis utilities, never the `p-4` shorthand, which
> would collide with `guest-link.spec.js`'s `cardFor()` locator.

## 2. Friend order (04) — biggest module; tab shell gates module 05

- [x] RD-FO-1  FriendOrder shell: `.app` chrome + status banners + main-switch `.tabgroup` (pinned testids, v-show panels, badge from `summary` emit) + sticky `.cat-tabs` (`data-testid="purpose-tabs"`, snapTab) — `04 §UC-FO-001..004` · model=heavy ⚠ `#panel-guests` built as v-show SHELL only — RD-KG-1 cannot start before this row; badge computed here and never re-owned by 05; carries the appbar-subtitle OPEN — **DECIDED by the orchestrator 2026-08-08: NAME ONLY**, the spec's own proposed default. Rationale: `GET /orders/cycle/:id/friend/:id` returns only `{id, name, packeta_address}` and no API change is allowed; module 03's portal appbar gets its uid from the *session*, which `guest-host-view.spec.js:641-647` proves is **optional** in the stored shape (it writes no `friendUid`), so sourcing it here would render "name · code" for some friends and "name" for others — worse than a consistent name. Revisit only if a client-side code source becomes guaranteed. snapTab regression spec lands here. ⚠ **Two SILENT regressions this row must handle when it writes `class="app"` — see CLAUDE.md §"`.app > *` neutralises every Tailwind positioning utility".** (1) `FriendOrder.vue`'s `<header … sticky top-0 z-40>` is a **direct child** of the root, so `.app>*{position:relative;z-index:1}` (same `(0,1,0)` specificity, later stylesheet) silently un-sticks it the moment `.app` lands. `BrandChrome` replaces that header in this same row so the fix is free — but landing `.app` before replacing the header is a regression **no build error and no spec will catch**. (2) `TabsList data-testid="purpose-tabs" class="sticky top-16 z-30"` is *nested*, so `sticky` survives — but `top-16` (64 px) was calibrated against the sticky header that non-sticky `BrandChrome` replaces, so it will pin **64 px below the viewport top**. `mobile-no-h-overflow.spec.js` only asserts the strip scrolls within itself and will **not** catch a wrong `top`. The three hand-rolled `fixed bottom-0 z-50` cart bars are nested and survive as-is; don't hoist them to root level while they're still on Tailwind utilities.
- [x] RD-FO-2  Product cards: coffee card + `.vbox` variants + stock bar (kg display, gram math untouched) + bakery card — `04 §UC-FO-005..007` ⚠ **RD-FO-1 left you ONE card, not two** — it merged the radix `TabsContent` copy and the single-purpose fallback into one `activeProducts` list. Those had **silently diverged in four ways**: the fallback had no roast/roastery badges, **no stock-limit bar at all**, **no 500g variant** (its own comment read "150g / 200g / 250g / 1kg"), and no `:disabled` on "+". The two that mattered **hid product from the user**; the `canIncrement` one was cosmetic, since `increment()` already guarded. Verified byte-identical to the richer copy after the merge, so nothing was lost. ⚠ **Two locators in `order-shell.spec.js` break the moment you land `NeoStepper`**: `:337`/`:342` click `getByRole('button', {name: '+' | '-', exact: true})`, but `NeoStepper.vue:62,70` labels its buttons `aria-label="menej"`/`"viac"` and renders **U+2212**, not a hyphen — and aria-label wins over text content, so `'+'` breaks too. That is a legitimate pipeline-authored re-point; cite the mandate.
  · **DONE 2026-08-08 · [PR #56](https://github.com/karolskolar/gorifi/pull/56).** Suite **367 → 381 passed / 3 skipped**; 1 pipeline-authored spec re-pointed (2 lines, case (b) cited), 0 pre-existing. ⚠ **A hit-target defect was caught on review, in a 6-pixel band.** The `1fr 1fr` grid overflows at 320 px, so a single-column fallback was added — but its breakpoint arithmetic **omitted `.card`'s own `border:3px solid` (6 px)**, giving `min-[362px]` where the content box is `viewport − 66` and the true threshold is **368**. Because `grid-cols-2` is `repeat(2, minmax(0,1fr))` whose minimum is **0**, between 362 and 367 the track sits *below* the `.vbox`'s 146 px min-content and the shortfall is absorbed by the stepper buttons shrinking — **36.5–37.75 px wide**, silently losing the 38×38 hit target 02 §UC-DS-008 pins as "from CSS — do not override". Only the *width* shrank (`flex-shrink` on a `width:38px` flex item), which is why nothing noticed. The corroborating clue was already in the diff: the code comment predicted 154 px tracks at 378 while CLAUDE.md recorded 151 — **151 renders, and only reproduces from `−66`**. Fixed to `min-[368px]`, pinned by a 5-width test (320/364/367/368/378) asserting column count **and** `[38,38]`, mutation-verified by reverting to 362. ⚠ **The recorded rationale was also wrong and is corrected**: forcing `grid-cols-2` at 320 px yields **zero** document overflow (tracks collapse to 122 px, buttons to 26 px) — the 15 px overflow appears **only** with the spec's *inline* `1fr 1fr`, since bare `1fr` is `minmax(auto,1fr)` and `auto` floors at min-content. So the **class-vs-inline switch alone** satisfies `mobile-no-h-overflow`; the media query's real job is protecting the `.vbox`. ⚠ **`canIncrement` is NOT e2e-reachable and no test was invented to pretend otherwise**: `NeoStepper.inc()` returns before emitting when `incDisabled` is bound, so a forced click never enters `onQty` → `increment()` → `canIncrement()`; the two assertions jointly pin `incDisabled` + the silent refusal and nothing more. **RD-FO-3 must bind `incDisabled` on the cartbar's steppers and must not read a green suite as proof the ceiling survives.** Also closed: an unbreakable 44-char product name overflowed the document by 263 px at 320 px (a hyphenated name does *not* reproduce it — `-` is a break opportunity, so the fixture is deliberately hyphen-free); `overflow-wrap:anywhere` sits on the **container** per RD-FL-4's `plan_note` precedent so the description lines inherit it. `.pimg` OPEN resolved in 02 §UC-DS-013: no photo ⇒ bare gradient frame, no bag internals, no placeholder icon. ⚠ closes RD-DS-1's `.pimg` OPEN: no-photo = bare gradient frame; product name stays a `<h3>` heading for `getByRole('heading')`; NeoStepper e2e smoke lands here.
- [x] RD-FO-3  Sticky `.cartbar`: deadline/count/`paymentTotal`, actions, inline dirty warnings (dismissable ✕), flat cart lines behind `<details>` — auto-save matrix preserved verbatim — `04 §UC-FO-008,009` · model=heavy ⚠ Zaplatiť opens PaymentModal with pinned props — modal internals are RD-GX-2's, do NOT touch PaymentModal.vue here; page-level warnings move into the cartbar. ⚠ **Three things from RD-FO-1.** (1) `order-shell.spec.js:271` finds the cart footer by `getComputedStyle(d).position === 'fixed'`; UC-FO-009 makes it `.cartbar` = **`sticky`**, so **this row must re-point that assertion** — a legitimate pipeline-authored edit, cite the mandate. (2) `groupedCartItems` and its purpose-coloured headers **deliberately survive** in the cartbar `<details>` even though resolved conflict #10 drops the grouping — RD-FO-1 left them rather than half-migrate into two skins, and the computed now has exactly one consumer, so delete template block and computed **together**. (3) ⚠ **`successMessage` ("Košík bol uložený") is reachable but never readable, and on a FAILED submit it CONTRADICTS the error banner** — `saveCart(false)` sets it, `doSubmitOrder()`'s catch then sets `error`, and the page shows "saved" and the failure **side by side for 3 s** (instrumented; pre-existing, not introduced). Its only other call sites (auto-save, cancel) are deliberately silent, so there is no user-initiated save for it to report. **Retire it, or clear it at the top of `doSubmitOrder()`'s try — do NOT give it a trigger.**
  · **RD-FO-4 DONE 2026-08-08 · [PR #58](https://github.com/karolskolar/gorifi/pull/58).** Suite **394 → 424 passed / 3 skipped** (30 new), **0** spec edits of any kind. All four modals on `NeoModal`, the 4-scenario matrix and all five pre-selection branches measured, save-as-default's failure proven non-blocking, and the cancel flow asserted against a **real** colleague sub-order placed through the public guest checkout — surviving with `paid`/`delivered` flags intact while the host's own order goes to `null`. ⚠ **The QR is proven scannable, not merely present**: decoded off the rendered pixels with `jsQR` (45 modules, three finder patterns), then the payload decoded with `bysquare` and found **byte-identical** to an independent encode — `{amount: 12.54, iban: SK31…, paymentNote: "<friend> / <cycle>"}`, and 12.54 = 9.04 + 3.50 fee, so `paymentTotal` is what a bank app actually charges. ⚠ **A 320 px footer overflow was found and fixed**: `Zrušiť 78.69 + Potvrdiť a odoslať 156.05 + 8 gap = 242.73` against a **240 px** content box — every viewport under ~323 px painted outside the modal border and gave `.modal-scrim` a horizontal scrollbar. Fixed by spending **padding**, not copy (a scoped ≤400 px rule), which `flex:1` re-grows invisibly. The reviewer measured the real quantity as **min-content slack**: 21.26 px ≈ 3 characters on the tightest footer. ⚠ **`headroom 0` conveys nothing** — `.m-foot .btn{flex:1}` makes buttons consume the row exactly whenever they fit, so it is 0 by construction and only goes negative at min-content. ⚠ **Vue whitespace trap, now a house rule**: `condense` deletes a whitespace-only node between elements when it contains a newline, so `</b>\n<span>` silently concatenates two user-visible strings — `Lego domaDúbravka` — with no build error, no lint error and no failing test (`toContainText` won't catch it). Keep such pairs on one line, or use an explicit `&nbsp;`. ⚠ **Two review rationales were disproved, one of them the reviewer's own.** The claim that `position:relative` prevents a scrim yank was false — but so was the replacement, because focusing the *first* radio scrolls to the top in every variant and discriminates nothing. Re-measured on the **last** radio: adding the classic `top:0;left:0` yanks **only when the row is unpositioned**, so `relative` is what disarms the recipe everyone reaches for. Also discarded: `expect(box.top).toBe('auto')` can never pass, since `getComputedStyle` resolves `auto` insets on an abs-positioned box to their *used* value. Recorded as an in-code A/B/C/D table. Two pre-existing behaviours confirmed unchanged: a stale `packeta_address` on a cycle whose parcel was later disabled opens with no method radios and no way back to pickup, and an order pointing at a deactivated pickup location opens with nothing selected. **For module 02:** the ≤400 px footer relief is view-scoped, so every other `NeoModal` footer keeps the canon 16 px — module 03's profile footer needs 164.38/240 today so nothing else overflows, but 05/06 will rediscover it; consider adopting it on `.m-foot .btn` globally.
  · **RD-FO-3 DONE 2026-08-08 · [PR #57](https://github.com/karolskolar/gorifi/pull/57).** Suite **381 → 394 passed / 3 skipped**; 1 pipeline-authored re-point (case (a) cited), 0 pre-existing. `successMessage` **retired**, not cleared. ⚠ **The cartbar is now a DIRECT CHILD of `.app`, and that is safe only by cascade order** — `.cartbar`'s theme rule sits *later* in `friends-theme.css` than `.app>*` at equal `(0,1,0)` specificity. **Proven by counterfactual rather than assumed**: a plain `<div class="sticky bottom-0 z-50">` appended to `.app` computes `relative`/`z-index:1`; the same node with `.cartbar` added computes `sticky`/`50` (rule indices 637 vs 741). Zero `h-48` spacers and zero `fixed` elements remain under `.app`. **RD-GX-1 gets the same free pass only when it converts to the theme class — not before.** The auto-save matrix was verified **at the network level**, not by reading the watcher: no order → 2 taps + 1800 ms ⇒ **0 PUTs** with the server still `order: null`; draft → 3 rapid taps ⇒ **exactly 1**, ≥400 ms after the last edit; submitted → 0 until "Aktualizovať". The reviewer additionally pinned that qty→0 deletes the **server row** end to end. ⚠ **Three things for RD-FO-4/5, all found on review.** (1) **The 320 px actions row has literally ZERO slack** — `82.44 + 82.44 + 107.13 + 2×8 gap = 288.01` against a **288 px** content box. `.btn` is `white-space:nowrap` and flex items floor at min-content, so **one extra character in any label, or a fourth action, overflows the bar immediately** — and unlike RD-FO-2's grid it will *not* silently shrink. (2) `order-cartbar.spec.js:489` asserts `.actions` count **0** on a locked cycle, which **silently settles RD-FO-5's locked-Zaplatiť OPEN as HIDDEN** — showing it requires re-pointing that assertion. (3) `order-cartbar.spec.js` pins the accessible names `Potvrdiť a odoslať`, `Hotovo!` and `Neuložené zmeny` via `getByRole('dialog')`; RD-FO-4 owns all three modals and `NeoModal` does expose `role="dialog"`, so only the **strings** are at risk. ⚠ **Pre-existing defect measured for the first time, for RD-FO-5's closeout:** on a cycle with an existing **draft**, an edit made before the `initialLoadComplete` 100 ms latch flips produces **no PUT and schedules no catch-up** (sweep: 0 ms ⇒ 0 PUTs; 500/2000/5000 ms ⇒ 1 each, cart total updated in all four) — and because `hasUnsavedChanges` only covers "no order" and "submitted && differs", that draft raises **no leave guard** either. The watcher is untouched by this row, so not a regression, but "draft ⇒ exactly one PUT" reads broader than it is.
- [x] RD-FO-4  Modals: Spôsob prevzatia (4-scenario matrix + RadioRow + save-as-default via `updateFriendProfile`) + Hotovo! + Zrušiť confirm + leave guard — `04 §UC-FO-010..013` · model=heavy ⚠ success modal carries NO payment-reference row (reference lives only in the Platba modal → RD-GX-2); RadioRow pattern liftable by RD-GX-1's checkout — lift, don't re-invent; cancel copy must stay true (colleagues' sub-orders survive — assert it). ⚠ **Radio groups inside a trapped `NeoModal` become N tab stops instead of the native one** — RD-FL-2's focus trap cycles in DOM order, which is the documented trade. Harmless today (the trap only derives on for `closable:false`, and no radio-bearing dialog is a `NeoModal` yet), but **this row is the first to put a RadioRow in a modal**. If it ever passes `trap-focus`, either implement radio-group awareness in `NeoModal`'s `focusablesInside()` or accept the extra stops deliberately. Focus does not alter the checked radio (verified), so this is an ergonomics question, not a correctness one.
- [x] RD-FO-5  Locked state (f-order-locked) composition + module 04 closeout: fidelity vs 03/07/08/17-shot, pinned-selector audit, admin invariance — `04 §UC-FO-014,015` ⚠ Kolegovia LOCKED content look is RD-KG-1's — verify shell + amber badge flip only; carries the locked-Zaplatiť OPEN — **DECIDED by the orchestrator 2026-08-08: KEEP HIDDEN**, the spec's own proposed default. It matches shipped behaviour (the button lives inside the `!isLocked` block), the prototype (which hides all locked actions), and `order-cartbar.spec.js:489`'s `.actions` count 0 — which RD-FO-3 had already pinned, so *changing* it would now be the change requiring a re-point. ⚠ **Recorded as a genuine product residual, not a closed question**: a friend who submitted but did not pay before the lock loses the payment shortcut on this screen, while **guests keep a Zaplatiť in their locked state** (README item 9) — an asymmetry on a money-collection touchpoint. Reversing it later is a one-line `v-if` plus that one assertion.

> ## ✅ MODULE 04 IS CLOSED (2026-08-08 · RD-FO-1..5 · PRs [#55](https://github.com/karolskolar/gorifi/pull/55)–[#59](https://github.com/karolskolar/gorifi/pull/59)). What 05/06 inherit.
>
> Suite **353 → 436 passed / 3 skipped**. **Zero pre-existing specs modified across the entire effort** (`git diff --diff-filter=M 7c3f85e..HEAD -- e2e/tests/` still empty after 19 rows). Admin invariance **14/14 byte-identical** against a build of `7c3f85e` — with a **positive control** (the friend login screen *does* differ between the bundles), without which "identical" is unfalsifiable. The locked composition needed **no view change** — RD-FO-1..4 had already produced every UC-FO-014 bullet — but the closeout still found two real geometry drifts, three inaccurate spec claims and four untested assertions.
>
> **The five things 05/06 must not rediscover:**
>
> 1. ⚠ **A class list cannot reach an unclassed element.** Both fidelity drifts had the same shape: an **inline child inside an unclassed block** makes that block own a line box, which then inherits preflight's `1.5` strut instead of `normal` — `.cartbar` 149 vs 146 (an `inline-flex` `summary` inside `<details>`), the bakery header 24 vs 20 (an `inline` `<h3>`). This is A9/A10's structural blind spot; the fix is `line-height:normal` **at the call site**, never a bigger selector list. `GuestSubOrders`/`GuestProductGrid` reuse the same primitives.
> 2. ⚠ **Two 02-owned fidelity residuals, deliberately NOT patched per-call-site**: an icon-only `.chip` renders **29 vs the canon's 32** (preflight's `svg{display:block}` + `vertical-align:middle` — restoring **both** gives exactly 32), and `.stepper button` renders `−`/`+` in Figtree where the canon uses Arial (zero geometry effect). Systemic — one decision in module 02.
> 3. ⚠ **`.stepper.disabled` styles the BUTTONS, not the container** — UC-FO-014 said otherwise, so reading `pointer-events`/opacity off the container **passes vacuously**. Corrected in the spec.
> 4. ⚠ **Purpose/product order is ALPHABETICAL**, not encounter order — `products.js:19` is `ORDER BY purpose, name`, so the strip can never reproduce the prototype's demo sequence. A fidelity comparison must **select the canon's tab explicitly**, or it compares two different cards.
> 5. ⚠ **The Zaplatiť asymmetry is live**: a friend who submitted but didn't pay before the lock loses the payment shortcut, while **guests keep theirs** (README item 9). Module 06 owns the guest half.
>
> Also inherited from RD-FO-4: **promote the RadioRow, don't copy it** (see the RD-GX-1 row), the `:has(:focus-visible)` focus ring (`:focus-within` fires on touch), the min-content footer arithmetic, and Vue's `condense` whitespace trap.

## 3. Colleagues panel (05)

- [x] RD-KG-1  Kolegovia panel restyle: share row, summary heading, `.suborder` cards (fold/badges/big-green Odovzdané via NeoCheckbox `disabled`-capable/Odstrániť `.confirmbox`/cancelled recomputed Σ), empty + locked states, additive `rows` field on `summary` emit — `05 §UC-KG-001..005,007` · model=heavy ⚠ RD-FO-1's badge keeps consuming `count`/`pendingDelivery` — do not re-gate it on `rows`; loadSeq/rowSeq/ready-gate script contracts verbatim; zero e2e edits — guest-host-view.spec.js runs unmodified; paid-409 renders verbatim in a danger banner with confirmbox kept open.
- [x] RD-KG-2  GuestShareDialog recomposed onto NeoModal/NeoCopyRow (create/deactivate/regenerate + warn banner + inline confirm, native share conditional) + the 2 authorized guest-link.spec.js edits (inputValue→textContent for the copy-row `.val`) — `05 §UC-KG-006,007` ⚠ component API (`open/cycleId/cycleName`, `update:open`) frozen — RD-FL-5's and RD-KG-1's entry points need no change; ⚠ `NeoModal` has **no `open` prop** (UC-DS-010's field group lists none — the parent owns the mount), so `05 §UC-KG-006`'s phrase "NeoModal's `open`-driven mount" is satisfied by `<NeoModal v-if="open">` inside the wrapper, which still emits `update:open(false)` from `@close`. `NeoModal` now binds `$attrs` onto `.modal` (attrs FIRST, so `role="dialog"` cannot be clobbered) — a `data-testid` on `<NeoModal>` lands on the dialog element, and `class` composes rather than replaces; keep `role="dialog"` + Esc so the race-guard spec stays green; deletes the bespoke execCommand fallback (NeoCopyRow owns copy); share-sheet title OPEN — **DECIDED by the orchestrator 2026-08-09: KEEP `'Objednávka Gorifi'`.** The handoff rebrands the *surface* but specifies no share-sheet payload, and the sibling decision (02/03's `index.html` title/lang/favicon) was already settled as **leave "Gorifi"** — `document.title` is still `'Gorifi - Objednávky'` and `public-flow.spec.js` pins `toHaveTitle(/Gorifi/)`. Renaming the share sheet alone would make the app introduce itself as Podpultovka in a message while calling itself Gorifi in the tab it links to. If the product rebrands the title globally, this moves with it — one string, one place. ⚠ **Three things from RD-KG-1.** (1) ⚠ **`guest-host-view.spec.js:890,929` use an UNSCOPED `page.getByRole('button', {name: /Zdieľať/})`.** `guest-link.spec.js:280`'s count-0 pin is dialog-scoped so RD-KG-1's new page-level "Zdieľať odkaz" / "Zdieľať objednávku s kolegami" buttons don't disturb it — **but if this row leaves the dialog mounted rather than `v-if`-gated, its own "Zdieľať odkaz" starts matching those page-level locators.** Both are pre-existing and immutable. (2) Reuse RD-KG-1's `.banner.danger.slim` markup (`span.dot` + `div[min-width:0]`) rather than inventing a second error shape. (3) ⚠ **`NeoCheckbox` is a `span[role=checkbox]`, so a wrapping `<label>` can NEITHER activate NOR name it** — a native `<input>` used to supply both for free. Every consumer must compose the three-zone pattern (`@click.self` on the label, `@click` on the text, `aria-label` on the control); 02 §UC-DS-009 delegates this to the consuming screen **every time**, and there are now five call sites doing it.

> ## ✅ MODULE 05 IS CLOSED (2026-08-09 · RD-KG-1..2 · PRs [#60](https://github.com/karolskolar/gorifi/pull/60)–[#61](https://github.com/karolskolar/gorifi/pull/61)). What 06 inherits.
>
> Suite **436 → 468 passed / 3 skipped**. ⚠ **The effort's "zero pre-existing spec edits" streak ENDED here, deliberately and within budget**: RD-KG-2 spent the 2 sanctioned `guest-link.spec.js` substitutions (`inputValue()` → `textContent()`, `toHaveValue` → `toHaveText`), forced by UC-DS-011 making the value box a `div.copyrow > .val` — §UC-FL-013 case **(b)**, structurally unsatisfiable against a mandated primitive. Same regexes, same messages, nothing weakened. **`guest-link.spec.js` is still the ONLY pre-existing spec modified in 21 rows**, and **5 sanctioned edits remain, all belonging to module 06** (§UC-GX-011).
>
> ⚠ **Count ambiguity, recorded so 06 doesn't inherit the confusion:** the backlog row said "2 edits", but the file has **three** `inputValue()` reads and **two** `toHaveValue` assertions = 5 changed lines across 3 tests. The spec itself says "all three occurrences" *plus* the `toHaveValue` substitution. The readings that yield 2 are the two substitution **kinds**, or the deliverables table's two **assertion** substitutions. The implementer followed the spec verbatim and spent no third kind. **Module 06 should count its 5 as spec-listed items, not as lines.**
>
> **What 06 must not rediscover:**
>
> 1. ⚠ **`NeoModal` has no `open` prop — the wrapper owns the mount, and `v-if` is load-bearing, not style.** `guest-host-view.spec.js:890,929` use an **unscoped** `page.getByRole('button', {name: /Zdieľať/})`; leaving the dialog mounted makes its own trigger match them, and the always-present scrim swallows clicks. Mutation-proven: `v-if="open"` → `v-if="true"` fails **two** specs. ⚠ **06's guest checkout/status modals have the same shape** — their triggers ("Zaplatiť", "Zavrieť") are already located unscoped by shipped guest specs.
> 2. ⚠ **A `v-show` panel is `display:none`, so its contents are OUT of the a11y tree** — an unscoped role locator resolves to **0**, not to a hidden element. `toBeHidden()` passes on 0; `toHaveCount(1)` does not. UC-KG-007's "hidden on own tab" wording was stale and is corrected.
> 3. ⚠ **`NeoCheckbox` is a `span[role=checkbox]`: a wrapping `<label>` can NEITHER activate NOR name it.** A native input used to give both for free. Compose the three-zone pattern (`@click.self` label, `@click` text, `aria-label` control) — 02 §UC-DS-009 delegates this to the consumer **every time**, and there are now five call sites doing it. RD-KG-1 shipped this as a silent regression on the hand-over checklist and no test caught it.
> 4. **`NeoCopyRow`'s `value-testid` goes on the `.val` node, never the row** (so text assertions don't swallow the button label). Two consumers today; 06's guest status URL and payment reference are the third and fourth.
> 5. **`.confirmbox` was "reserve (06)" in A10 and is now SHIPPED** by two module-05 surfaces — 06 inherits it live.
> 6. The share-sheet payload stays **`'Objednávka Gorifi'`** (orchestrator decision) — a **product** call tied to `document.title` and `public-flow.spec.js`'s `toHaveTitle(/Gorifi/)`. If the rebrand ever renames the title, both move together.

## 4. Guest flow (06)

- [x] RD-GX-1  g-order restyle: guest brand header + hero `.card.hl` + GuestProductGrid neo restyle (pixel-identical to RD-FO-2's cards) + sticky cartbar + checkout modal — `06 §UC-GX-001..003` · model=heavy ⚠ grid is shared with status edit mode (GSO-T4 one-home rule) — status screen inherits the neo grid early, acceptable interim inconsistency; zero e2e edits in this slice; g-confirm + dead-link stay old-skin here (RD-GX-2/4); submit payload byte-identical, no auth headers. ⚠ **PROMOTE the RadioRow, do NOT copy it** (RD-FO-4 seam, reviewer-endorsed). `.radiorow` currently carries **no styling of its own** — padding, flex, border-colour, background and the 18 px dot are all inline `:style` objects, and the only rules keyed on the class are the two scoped ones in `FriendOrder.vue`. "Lifting" it therefore means copying **two style literals plus two scoped rules** into a second view, where they will diverge silently. Promote `.radiorow` / `.sr-radio` into `friends-theme.css` with a `.sel` modifier instead. Four things are load-bearing: the **native `<input type="radio">` stays in the DOM** (clip-rect hidden, never `display:none`/`visibility:hidden` — that is the keyboard/AT path, and `NeoModal.focusablesInside()` also filters on `visibility`), the call-site `line-height:normal`, the inline border/background pair, and the focus ring — which must be **`:has(:focus-visible)`, not `:focus-within`**, because the latter fires on **touch** and leaves a permanent magenta shadow on the primary surface (keep the `@supports not selector(:has(*))` fallback: `:focus-visible` shipped well before `:has()`, and without it a keyboard user in that window gets *no* indicator). ⚠ Also inherit, don't re-derive: **`position:relative` on the row is NOT inert insurance** — measured A/B/C/D, adding the classic `top:0;left:0` sr-only recipe yanks the scrim to origin **only when the row is unpositioned**; with the row positioned, nothing moves. And the **320 px footer arithmetic**: `.btn` is `white-space:nowrap` + `flex:1`, so a too-wide footer gives **no degradation signal at all** — no shrink, no wrap, no ellipsis — it paints outside the 4 px modal border and hands `.modal-scrim` a horizontal scrollbar. Measure **min-content** need, not flex-resolved width; the checkout footer is the next likely overflow. ⚠ **Two items handed over by RD-DS-3.** (1) **`incDisabled` has no disabled visual and actively lies.** `friends-theme.css` has `.stepper.disabled button{…}` but **no `.stepper button:disabled` rule**, so a stock-capped "+" measures `opacity:1`, `cursor:pointer`, `pointer-events:auto` — hover turns it `--hi` and mousedown applies the **full press physics** (`translate(2px,2px)`, shadow collapse), so the dead button acknowledges the tap and springs back, reading as "added" at exactly the ceiling. Add `:where(.app, .modal-layer) .stepper button:disabled{opacity:.35;cursor:not-allowed;pointer-events:none}` to `friends-theme.css` (a documented addition — the canon has no such rule) or style at the call site. The `pointer-events:none` is the load-bearing part; opacity alone leaves the false feedback. Deliberately NOT inlined in `NeoStepper.vue`: a `<style scoped>` block carries `[data-v-*]` specificity this row could not override. (2) **Re-point or delete `guest-cart.js`'s `formatPrice`** — RD-DS-3 added `lib/money.js` `fmtEur` and could not touch existing files, so the two now co-exist. They are NOT equivalent on garbage input: `formatPrice('abc')` → `"NaN EUR"` and `formatPrice('1e400')` → `"Infinity EUR"` (the `price || 0` guard only catches falsy), while `fmtEur` returns `"0.00 EUR"`. `GuestProductGrid.vue` imports `formatPrice`, so this row is where the split closes.
- [x] RD-GX-2  Platba modal restyle (PaymentModal.vue — props API frozen) + g-confirm screen + payment-reference relocation into the modal — `06 §UC-GX-004,005` ⚠ RD-FO-3/4's friend flow INHERITS this restyle with no change on its side; carries sanctioned e2e edits #2(order-half)/#3/#4; bysquare/qrcode payload byte-identical — a scanned QR must resolve unchanged.
- [x] ⚠ **RD-GX-3 — DO THE `.app` ROOT FIRST.** RD-GX-1 lifted the shared `GuestProductGrid` onto the neo card (GSO-T4: one home, extend never fork), but `GuestOrderStatus.vue` has **no `.app` root** and every theme rule is `:where(.app,.modal-layer) …` — so on the status **edit** screen `.card`/`.cat-tabs`/`.vbox`/`.stepper`/`.banner` resolve to **nothing**. It renders **UNSTYLED, not merely inconsistent**: the tabs run together as "EspressoFilter" and the stepper reads "−2+". Fully **functional** (values, all six testids, save flow and server totals verified), and `guest-status.spec.js` passes **unmodified** — but the row's "acceptable interim inconsistency" anticipated *neo-among-shadcn*, not *raw*. ⚠ **MERGE-ORDER HAZARD: if PR #62 ships without #64, the guest status edit screen is visually broken in production.** §UC-GX-006 requires the `.app` root anyway, so land it as this row's first change.
- [x] RD-GX-3  g-status restyle: four read states (pills / paid-frozen / locked warn / cancelled terminal) + edit mode (reuses neo grid) + shared cancel-confirm (3 entry points; only literal `items: []` cancels) — `06 §UC-GX-006..008` · model=heavy ⚠ carries sanctioned edits #2(status-half)/#5; server flags `editable`/`items_editable` drive everything incl. undefined fallback; invite CTA shows in all four states when `invite_request.available` (server flag wins over prototype).
- [x] ⚠ **RD-GX-4 — verify a claim two rows recorded, before closing 06.** RD-GX-3 measured its cancel-confirm footer (`Ponechať` / `Zrušiť objednávku`) and found the ≤400px padding relief is **NOT invisible at 378px**: resolved widths go from `130.23/159.77` (danger button pinned at its own min-content) to `145/145`, i.e. the footer becomes **symmetric** where the prototype renders it lopsided. Better-looking, but a *change*. ⚠ **RD-FO-4 (`.fo-foot-btn`) and RD-GX-1 (`.gx-foot-btn`) both record "invisible at every width" of THEIR footers**, which are different button pairs — `GuestOrder.vue:588` even claims "verified: resolved widths … unchanged at 378px". **Measure both before trusting either comment**; if they are also false, correct them at the call site. RD-GX-3 also needed **6px**, not 10 — 245.02 at 10px still overflows 240 — so the "10px" precedent is not universal either.
- [x] ⚠ **RD-GX-4 — a stale comment RD-GX-3 deliberately left**: `guest-payment-modal.spec.js:324-327` says `GuestOrderStatus.vue` "still renders its own on-card reference row, so this page legitimately carries one until that row lands". It no longer does. The assertion it explains is *absent*, so nothing fails — either delete the comment or convert it into the `payment-reference` count-0 assertion it was deferring.
- [x] RD-GX-4  Invite CTA restyle (GuestInviteRequest, 4 states, "Chcete si objednať sami?") + g-dead 3 variants + status-404 card + module 06 verification sweep — `06 §UC-GX-009..011` ⚠ carries sanctioned edit #1 (guest-lead-capture.spec.js regex); closes the effort: all 7 sanctioned e2e edits spent, `git diff --stat backend/` empty, api.js + guest-cart.js unchanged, full fidelity pass vs 09–12/14–16-shot + live prototype (13-shot is a duplicate of 15 — editable state compares against the prototype).

---

## 5. Invitation → friend with login (07) — ⚠ FIRST module with backend/schema scope

> Added 2026-08-13, after the redesign closed. **The "no backend change" stack note above
> scopes modules 02–06 only** — module 07 deliberately includes schema migrations and two
> route changes; `07-invitation-approval.md` is the task definition source. The redesign's
> "7 sanctioned e2e edits all spent" ledger also scopes 02–06: module 07 §UC-IA-008 is its
> own sanctioned-edit list (api-security +1 row, guest-lead-capture retarget,
> invite-register-shell counts, ios-input-zoom counts, one new spec file). Copy strings are
> product-owner-signed (the clipboard message is deliberately ty-form — recorded register
> override); NO legacy-mode warning in the dialog (legacy retirement = separate follow-up).

- [x] IA-T1  Schema foundations: `invitations.username` + `created_friend_id` (try/catch ALTER), `generateTempPassword()` = randomCode(12) export, `helpers/friend-create.js` extraction of `getPlaceholderCycleId()` (rewire onboarding.js + friends.js byte-identically) — `07 §UC-IA-001,002` ⚠ both columns land with NO writer (register writes username in IA-T2; approve writes created_friend_id in IA-T3 — no FK by design, consumers tolerate dangling); generateTempPassword has no consumer until IA-T3 and must wrap randomCode, never generateGuestToken (SEC-S2).
- [x] IA-T2  Register hardening (type guards + 120/32/160 bounds — local constants, NOT guest.js's validateIdentity — fixes `{name:123}`→500) + optional username validate/store + InviteRegister.vue 4th field (`.inp`, `maxlength=30`, NO placeholder, vy-form field-help) — `07 §UC-IA-003,004 + §UC-IA-008 items 3,4` ⚠ MUST carry in the same run: invite-register-shell.spec.js `.field-lbl` 3→4 + no-placeholder loop + `.field-help` scoped to the email field; ios-input-zoom.spec.js `.inp` count 3→4 in BOTH tests. Stored username has no reader until IA-T3/T4; register-time availability check is a courtesy — approval revalidates.
- [x] IA-T3  `POST /api/invitations/:id/approve` — requireAdmin per-route (MIXED mount, never wrap the router), bcrypt + uid/invite_code loops BEFORE the tx, ONE transaction (INSERT friend with username/hash/`must_change_password=1`/`onboarding_source` provenance + UPDATE invitation processed/`created_friend_id`), SQLITE_CONSTRAINT→409 dual-layer, 201 `{friend:{id,name,uid,username}, username, tempPassword}` hand-picked (never sanitizeFriend) — `07 §UC-IA-005 + §UC-IA-008 items 1,5-API` · model=heavy ⚠ MUST carry ADMIN_ENDPOINTS += the new route (api-security.spec.js) and the API half of the new invitation-approval.spec.js (incl. ZERO friend_subscriptions/transactions rows as pinned non-writes; temp-password login → `mustChangePassword:true`). Response shape is IA-T4's contract; 409 non-pending carries `created_friend_id` for the dialog's message.
- [x] IA-T4  AdminInvitations approval dialog — "Vytvoriť" opens a dialog (NO navigation), username prefill (`inv.username` else slugify, editable), note prefill `Pozval/a: {inviter}`, inline editable 409, success state = ONLY plaintext holder (mono block + login URL + copy button with the VERBATIM ty-form message), list refreshes BEHIND the dialog, closes only on explicit action + `api.approveInvitation` — `07 §UC-IA-006 + §UC-IA-008 items 2,5-UI` · model=heavy ⚠ MUST carry the guest-lead-capture.spec.js:538-565 retarget (old pinned navigation dies here) UPGRADED to pin `onboarding_source==='guest_order'`; carries the UI half of invitation-approval.spec.js. Retires the `?create=1` CALLER only — the dead receiver block in AdminFriends.vue is IA-T5's (green in between: nothing navigates to it).
- [x] IA-T5  AdminFriends cleanup — relabel `Prihlasovacie meno *`→`Meno *` (modal + table header + placeholder + hint; governing rule: grep 'prihlasovac' in AdminFriends.vue returns nothing) + DELETE the `?create=1` onMounted block (cite by content — plan's line numbers drifted) — `07 §UC-IA-007` ⚠ completes IA-T4's retirement; `/admin/friends?create=1&name=X` must open the plain list, no modal. ⚠ Do NOT touch FriendPortalSession.vue's identical label (pinned ~10× by portal-profile-modal.spec.js, module 03 owns it — that spec passes UNMODIFIED). Also supersede CLAUDE.md's stale 2026-07-07 "Invitation → new friend flow" note.

- [x] IA-T6  Mailgun delivery: `helpers/mailer.js` (global `fetch`, NO new dependency, no-op without env so e2e never sends), wired AFTER the approve transaction commits, `email:{sent,skipped,error}` on the 201, the dialog's outcome line (copy button present in every case), and `--env-file-if-exists` in `ecosystem.config.cjs` — `07 §UC-IA-009` · model=heavy ⚠ a mail failure must NEVER fail the approval (the friend exists and the plaintext is on screen); the send must NOT be inside the transaction (IA-T3's two-writes invariant + network I/O in a sync tx); the API key must reach NO log line or response; `--env-file` (without `-if-exists`) would make the app unstartable where the secrets file is absent. Infra already verified live: domain active, SPF+DKIM valid, EU region, test mail delivered.

## 6. Follow-ups banked from module 07 (definitions: `07 §Follow-ups`)

> Each was found by a review or e2e pass during IA-T1..T6, judged out of scope, and left
> unfixed deliberately. `07-invitation-approval.md §Follow-ups` is the task definition for
> rows 1–3. They are independent of each other and of anything above — run them in any order.

- [x] FUP-T1 (done-by-FC-T2)  `AdminFriends.vue` reset-password placeholder says "Minimálne 4 znaky" while `resetPassword()` rejects anything under 8 — `07 §Follow-ups item 2` ⚠ same copy-vs-behaviour class module 07 existed to remove, on a screen the admin uses; the 8-char rule is also enforced server-side (`friends.js` reset-password) and pinned by `friend-password-policy.spec.js`, so the CODE is right and only the copy lies. Fix the string, pin it. ⚠ **SUBSUMED by FC-T2 below** (11 §UC-FC-003 mandates the "Minimálne 8 znakov" fix) — flip this row `[x] done-by-FC-T2` when FC-T2 lands; run it standalone only if FC-T2 is still far off.
- [x] FUP-T2 (done-by-FC-T2)  The "Prihlásenie" badge reads `friend.username || 'Nastavené'` gated on `hasCredentials` (= `!!password_hash`), so a friend with a password but NO username shows as credentialed while being unable to log in under modern auth — `07 §Follow-ups item 3` ⚠ reachable today: the per-friend "Resetovať heslo" action sets a password without touching `username`. Decide the display for that state (a distinct "heslo bez menu" marker is the obvious answer) — it is a correctness fix on an admin money-adjacent screen, not a cosmetic one. ⚠ **SUBSUMED by FC-T2 below** (11 §UC-FC-002's truthful three-state rendering IS this fix, incl. the reset-password-no-username repro) — same rule as FUP-T1.
- [x] FUP-T3  A JSON POST with an unparsable or scalar body returns **500** from the global handler in `backend/src/index.js` — `07 §Follow-ups item 1` ⚠ APP-WIDE, not invitation-specific: reproduces identically on `/api/admin/login` and `/api/friends/auth` (verified), because body-parser rejects before any route runs. Fix = translate `err.type === 'entity.parse.failed'` / `err.status` to 400 in the global error handler. Touches every endpoint, so it wants its own careful e2e pass (assert a 400 on at least one public and one admin route, and that nothing else regressed).
- [x] FUP-T5  `FriendPortalSession.vue` `saveProfile()` sends `packeta_address` **unconditionally**, so saving the profile modal before `hydrateCurrentFriend()` has landed **WIPES a stored Packeta address** (the field renders empty, the save writes `null`) — found by the FC-T4 round-1 reviewer, pre-existing and deliberately left out of FC-T4's scope ⚠ NOT hypothetical: the same class of bug is what FC-T4's delta logic was written to avoid for phone/email, and the window widened slightly now that a fresh login also hydrates. Fix = extend FC-T4's open-time-original delta to `packeta_address` (`FriendPortalSession.vue:527`), or gate the modal's "Uložiť" until hydration resolves. ⚠ `portal-profile-modal.spec.js:346` pins "a blank Packeta address is sent as null, not ''" — that assertion is about an intentionally-cleared field and must stay green; the new test is a *stored* address surviving a save that never touched it.
- [x] FUP-T6  An oversized image upload still returns **500** for a pure client mistake — `multer`'s `MulterError` (`LIMIT_FILE_SIZE`, the 5 MB cap at `routes/products.js:11` and `routes/bakery-products.js:7`) carries `code`/`field` but **no `status`**, so FUP-T3's "preserve a 4xx" rule cannot see it — found by the FUP-T3 reviewer ⚠ Same bug class as FUP-T3, deliberately NOT smuggled into that row (its backlog row and spec reference are about the JSON body parser). Fix = translate `err instanceof multer.MulterError` to 413 (`LIMIT_FILE_SIZE`) / 400 (the rest) — either in the global handler in `index.js` alongside the 4xx rule, or per-route. ⚠ Whichever home is chosen, keep FUP-T3's two decisions: never echo `err.message`, and no stack log on a client-triggerable branch. `image-upload.spec.js` is the regression net.
- [ ] FUP-T7  Malformed or aborted **multipart** still returns 500 **with a full stack log** — multer raises PLAIN `Error`s for these ("Multipart: Boundary not found", "Unexpected end of form", "Request aborted"/"Request closed" from `make-middleware.js`), which carry neither `status` nor `code`, so neither FUP-T3's 4xx rule nor FUP-T6's `MulterError` mapping sees them — found by the FUP-T6 reviewer, verified live ⚠ Includes the ORDINARY case of an admin's upload dropping mid-flight, so this fires in normal use, not just under attack. Bounded: all five upload routes are admin-guarded, so it is authenticated-only. ⚠ Fix is NOT "match the busboy message strings" — that brittle coupling is exactly what FUP-T3's status-based rule was chosen to avoid. Prefer wrapping `upload.single(...)` in a helper that catches and re-raises with a status, so the translation stays keyed on structure. Whatever the shape, keep FUP-T3's two decisions (never echo `err.message`, no stack log on a client-triggerable branch).
- [~] FUP-T8  ⚠ **LIVE PROD ISSUE, infra — CODE SIDE DONE, AWAITING OPERATOR MEASUREMENT (stays `[~]`; see the log entry).** Neither `deploy/nginx-gorifi.conf` nor `deploy/nginx-gorifi-staging.conf` set `client_max_body_size`, so the in-container nginx defaulted to **1 MB** and answered its own bare HTML 413 before Express ran. ⚠ **CORRECTED — this row's original prescription was WRONG and must not be restored.** It said "the app's own cap is 5 MB" and "fix = set `client_max_body_size 5m` (matching the app)". The shipped admin UI does **not** upload photos as multipart: `processImageFile()` in `CycleDetail.vue` / `AdminBakeryProducts.vue` sends the image as a base64 `data:` URI **inside a JSON body**, so the binding cap is `express.json({limit:'10mb'})` (`backend/src/index.js:63`), NOT multer's 5 MB — the only multipart request the frontend makes is the CSV import (`CycleDetail.vue:507`). Two consequences: base64 is ~4/3 of the file, so the 1 MB default actually failed a source photo at roughly **750 KB**; and `5m` would have **reintroduced the same bug one notch up**, refusing a ~4 MB photo Express accepts. Shipped: **`client_max_body_size 11m`** on `location /api` in both confs — strictly above every app cap, because `10m` is byte-identical to `express.json`'s limit (both 10485760, both reject on *exceed*) and nginx would win that race. ⚠ **OUTSTANDING, and why this row is not `[x]`:** the fix is only proven end-to-end once the **NPM edge** is MEASURED. `docs/deploy/nginx-proxy-manager.md:39` documents `10m` there, but §2b of that same doc concludes the Advanced block containing it "was never applied or never saved" (which is why the security headers moved into the LXC conf). If the edge is really on the 1 MB default, **this change does not fix the bug at all** — the photo dies one hop earlier. If it really is `10m`, then `express.json`'s `entity.too.large` 413 is **publicly unreachable** (byte-identical caps) while multer's 413 stays reachable. ⚠ **Structurally untestable by the gate** — every e2e spec hits Express on :3997 with no nginx in the path (same class as the RD-DS-6 "the gate sends no security headers" finding). Do NOT add an e2e that appears to cover it.
- [!] FUP-T4  Retire the legacy shared-password mode (`auth_mode = 'legacy'`/`'transition'`): the shared-password auth branch, the `login-list` dropdown, the legacy login card in `FriendPortal.vue`, the AdminInvitations amber banner — `07 §Accepted risks` + memory FUP-3 ⚠ **BLOCKED, and the blocker is production data, not code.** Removing shared-password login LOCKS OUT any active friend who has no personal `username` + `password_hash`. That coverage must be measured on the prod DB first (count active friends missing either), and any gap closed by issuing credentials (the per-friend "Nastaviť username" + "Resetovať heslo" actions, or the module 07 approval path for new members) BEFORE this row can start. ⚠ It also wants its own spec module: several shipped specs pin legacy behaviour (`public-flow` asserts the legacy card, `friend-login-list`, `modern-login` stubs the mode probe, `forced-change-ui`), so the sanctioned-edit list is non-trivial. Product owner has confirmed they want it gone (2026-08-13).

---

## 7. Transactional e-mail (08) — branded mail layer, foundation for module 09

> **Modules 08–11 extension, added 2026-08-15.** Spec files `08…11-*.md` + the decisions block in
> `00-overview.md` §Scope extension; framing contracts in `01-architecture.md` §Auth extensions.
> Like module 07, ALL FOUR modules include backend/schema changes. New Slovak strings ship
> product-owner-signed (07/08 precedent) — rows carrying unsigned copy are annotated and ship the
> spec-recorded safe default. Operator work (server-side `.env` edits, Google Cloud OAuth client)
> is annotated inline: it gates STAGING VERIFICATION only, never the build — test seams exist for
> both (Mailgun stub; `GOOGLE_AUTH_TEST_MODE`). Global order across sections 7–10 is the build
> order: 08 → 11 (unblocked rows) → 09 → 10, with FC-T3 placed under section 10 behind its GA-T1
> gate, and the shared login card sequenced ML-T4 → ML-T5 → GA-T4 to avoid a three-way merge on
> `FriendPortal.vue`.

- [x] EM-T1  `sendMail()` optional `html` part + per-message tracking-disable flags (`o:tracking-clicks/opens=no` on EVERY send, incl. text-only) + stub-e2e no-regression on the three mailer rules — `08 §UC-EM-001 + §UC-EM-005 item 4` ⚠ ships with ZERO html callers (EM-T2 wires the first — until then every send is byte-identical to today); html-with-empty-text degrades to text-only and never throws (EM-T2's render-inside-try/catch leans on this); result vocabulary, three-check ordering and `redact()` survive verbatim — the pre-existing stub tests (no-op, 500-degrade, key-never-leaks) pass unchanged. No new dependency.
- [x] EM-T2  `helpers/email-templates.js` — `renderEmail({text, blocks})` (4-block vocabulary, mandatory HTML escaping incl. attribute positions, text pass-through `===`) + credentials mail restyled onto the layer — `08 §UC-EM-002,003 + §UC-EM-005 items 1-3` · model=heavy ⚠ the text part stays `credentialsMessage()` VERBATIM — the renderer's pass-through IS the 07 §UC-IA-006/009 clipboard byte-identity mechanism; render sits INSIDE the approve route's existing mail try/catch (a template bug degrades to `email:{sent:false}`, never a failed approval) and the send stays after the tx commit (IA-T3 invariant); EXTRACT the Mailgun stub harness from `invitation-approval.spec.js` into a shared module (don't fork) — ML-T2 consumes it; `renderEmail` is module 09's foundation: new needs become new block types, never special cases. OPEN (recorded): button label/footer/preheader copy is unsigned — ships URL-as-label + signed-sentence fragments only.
- [x] EM-T3  `PUBLIC_BASE_URL` boot-report line + harness proof of the pin (login_url + BOTH stub-captured mail parts ignore the request Origin) + operator `.env` checklist — `08 §UC-EM-004,005` ⚠ do NOT patch `resolveLoginUrl()` (resolved: pin, don't patch); OPERATOR (outside the rsync tree): prod AND staging pin `PUBLIC_BASE_URL=https://podpultovka.biz` (resolved 2026-08-15 — Mailgun free tier permits one sending domain, so both envs share it; staging mail deliberately links to prod), mode 600, backend restart, then boot line in `/var/log/gorifi/out.log` + one real approval received in Gmail and one non-Gmail client. Module 09's magic links inherit the pin automatically.

## 8. Friends consolidation (11) — unblocked rows; FC-T3 sits in section 10 behind GA-T1

- [x] FC-T1  `friends.js` admin-route hardening (type guards + 120/32/160/200 bounds + `field`-marker 400s — LOCAL constants, never guest.js's `validateIdentity`) + response invariants (`google_sub` joins the strip list as a no-op today; `googleLinked = !!google_sub` derived BEFORE strip) + UC-FC-007 no-back-propagation pin — `11 §UC-FC-001,004,005,007` ⚠ module 10 INHERITS the strip rule (11 ships it first, GA-T4/T5 must apply the same discipline); the bound constants become the maxlength mirror FC-T2/FC-T4 consume; PATCH blank-name → 400 row-unchanged; the module-03 `PATCH /friends/:id/profile` name message stays untouched; ZERO `ALTER TABLE friends` (UC-FC-001 acceptance); API half of the new `friends-consolidation.spec.js`; `invitation-approval.spec.js` passes unchanged.
- [x] FC-T2  AdminFriends consolidation — "Meno a priezvisko" relabel, Kontakt column (amber "Bez e-mailu" badge = module 09's operational remedy) + Google column, truthful three-state "Prihlásenie" badge + "dočasné heslo" marker, 4-field modal with truthful hints, "Minimálne 8 znakov" fix — `11 §UC-FC-002,003 + §UC-FC-008 items 1,5` ⚠ SUBSUMES FUP-T1 + FUP-T2 above (flip them done-by-FC-T2 on landing); sanctioned retarget: `admin-friends-labels.spec.js` exact `'Meno'`→`'Meno a priezvisko'` (case (a), cite UC-FC-008 #1) while the `/Prihlasovacie/i` ABSENCE and the deliberate-keep `'Prihlásenie'` header assertions stay verbatim; `grep -i prihlasovac AdminFriends.vue` stays EMPTY (hints worded for it — re-verify after any rewording); Google column renders muted `-` until GA-T1 lands (graceful, data-driven, no feature flag); the "Odpojiť Google" menu item is FC-T3's, NOT this row's; old shadcn skin — no neo/theme classes; on landing supersede the stale CLAUDE.md `Meno *` bullets. UI half of `friends-consolidation.spec.js`; ONE-admin-token trap applies.
- [x] FC-T4  Friend self-edit Mobil + Email — "Upraviť profil" modal gains the two fields (Podpultovka skin, vy-form field-help, NO placeholder on Email, Mobil keeps its format example) + `PATCH /friends/:id/profile` accepts phone/email with FC-T1's exact bounds/type guards — `11 §UC-FC-009` ⚠ after FC-T2 (acceptance = the "Bez e-mailu" badge round-trip in the admin table); `FriendPortalSession.vue` is the six-leak surface — state keyed on the auth handshake, `portal-session-boundary.spec.js` stays green; clearing email removes the module-09 recovery path with NO confirm (the admin badge is the signal, by decision); do NOT touch the route's pinned "Prihlasovacie meno je povinné" message or the modal's existing labels (module 03 property); ownership spec (A cannot PATCH B) extended to the new fields; e2e crosses friend portal → admin table, so the ONE-admin-token trap applies. ⚠ ML-T6 and GA-T7 later touch this same modal additively — this row lands first.

## 9. Magic-link recovery (09) — needs EM-T2; FC-T2's badge is the missing-e-mail remedy

- [ ] ML-T1  `login_tokens` schema + `friend_sessions.via` + token primitives (randomBytes(32)→hex, SHA-256 at rest) + `createFriendSession(friendId, {remember|expiresAt|via})` TTL split 24 h/60 d across ALL mint sites — `09 §UC-ML-001,002` ⚠ `via` is written only by ML-T3 (`'magic_link'`), NULL everywhere else — leave it value-agnostic (module 10 may add `'google'`); the server accepts `remember:true` (strict boolean) on `POST /friends/auth` but the frontend flip is ML-T4's; change-password/setup-credentials re-mint carries `expiresAt` but NOT `via` — that NULL-via re-mint is what retires ML-T6's waiver (document at the mint site); mint-site inventory: `/friends/auth` both branches, setup-credentials, change-password (capture `expiresAt` BEFORE `invalidateFriendSessions`).
- [ ] ML-T2  `POST /api/magic-link/request` (enumeration-safe identical 200, username|unique-e-mail identifier, 60 s per-friend cooldown, predecessor invalidation in ONE transaction) + the magic-link mail via `renderEmail`/`sendMail` + FIFTH rate-limit bucket `magicLinkLimiter` — `09 §UC-ML-003,004 + §UC-ML-009 rule 1` · model=heavy ⚠ needs EM-T2 (renderEmail + the SHARED stub harness — consume the extraction, don't fork); enumeration byte-identity across 7+ cases INCLUDING a stub-500 (the mailer result maps to the same neutral 200 — never an e-mail-exists oracle); send is fire-and-forget AFTER the response (timing-oracle killer); extend `rate-limit.js`'s do-not-collapse comment, the CLAUDE.md local-run env list grows to FIVE (`RATE_LIMIT_MAGIC_MAX`), isolation self-skip test ships here; the raw token's ONLY test-visible surface is the stub-captured mail body — ML-T3's e2e capture depends on this task's mail; opportunistic expired-row GC lands here (no scheduler — ML-T7 relies on it staying); mail copy proposed-not-signed (UC-ML-006 OPEN).
- [ ] ML-T3  Redemption: `POST /api/magic-link/redeem` (atomic single-use, ONE neutral 401 for all failure classes, login-shaped 200 + `viaMagicLink`) + `/magic/:token` `MagicLogin.vue` + api.js clients — `09 §UC-ML-005` · model=heavy ⚠ `used_at IS NULL` predicate INSIDE the UPDATE is the mechanism — never read-then-write; POST-on-mount behind a single-shot guard (never a GET side effect — the SafeLinks/prefetch class); session = fixed 24 h, `via='magic_link'` (resolved 2026-08-15: NO opt-in at redemption); the 200 carries `viaMagicLink` + `mustChangePassword` — ML-T6 keys off the payload, and `mustChangePassword` routes into the EXISTING forced gate (03 §UC-FL-012), not new gate code; already-authenticated visitor: overwrite `gorifi_friend_auth` (deliberate); view follows the InviteRegister composition precedent (`.app`, 480 px, centred terminal states); expired-token e2e writes `expires_at` directly (DB_PATH self-skip pattern); neutral-failure copy proposed-not-signed.
- [ ] ML-T4  Remember-me re-semantics: checkbox default OFF, `remember` sent by BOTH api.js auth fns (incl. legacy path), localStorage ALWAYS written (in-memory fallback retired — TTL is the mechanism) — `09 §UC-ML-007 + resolved conflicts #1,#2` ⚠ the real work is sanctioned-edit bookkeeping: `modern-login.spec.js:214` aria-checked true→false + the "in memory only" test retargeted to "unchecked ⇒ expiresAt ≈ 24 h" (both case (a), cite UC-ML-007), sweep `portal-fidelity.spec.js` for checked-state pins; ⚠ land BEFORE ML-T5 and GA-T4 — three rows touch the same login card; markup/NeoCheckbox unchanged; needs ML-T1 live server-side or an unchecked login silently keeps 30 d.
- [ ] ML-T5  Login-screen recovery affordance "Zabudli ste heslo?" + one-field request form + neutral success state (modern card only, authMode-gated) — `09 §UC-ML-006` ⚠ the affordance stays attached to the PASSWORD FIELD GROUP — explicit layout seam so GA-T4's Google button doesn't contest the slot (note in code); legacy card renders ZERO recovery affordance (legacy seed asserts it); ONE success sentence for every 200 — the client half of the enumeration guarantee, future edits must not branch on server data; no placeholder, `.inp` mandatory (A12 — `ios-input-zoom.spec.js` counts `.inp`), maxlength 160, zero-pixel ARIA layer on the text-button. OPEN (surface, don't decide): the module-wide Slovak copy sign-off is consolidated on this UC — affordance/form/neutral messages/mail copy (ML-T2)/neutral 401 (ML-T3)/prompt strings (ML-T6).
- [ ] ML-T6  Post-login "set a new password" prompt banner + `currentPassword` waiver on change-password for `via='magic_link'` sessions (conditional current-password field) — `09 §UC-ML-008 + resolved conflicts #4,#5` · model=heavy ⚠ the waiver keys on the SESSION ROW's `via`, never a body field; dies at re-mint (NULL via) and at the 24 h expiry (accepted-risk window recorded in spec); suppressed entirely when `mustChangePassword` routes into the forced gate; `portal-profile-modal.spec.js` passes UNCHANGED — the field is conditionally hidden, never removed, password sessions byte-identical; `magicPromptDismissed` persists into `gorifi_friend_auth`, a fresh redemption resets it; client-side "Zadajte aktuálne heslo" validation becomes conditional, server authoritative regardless.
- [ ] ML-T7  Token-invalidation matrix: every `password_hash` write (change-password, setup-credentials, admin reset-password) + deactivation deletes outstanding `login_tokens`; FK-cascade + GC verified — `09 §UC-ML-009 rules 2-5` ⚠ deletes sit in the SAME transaction/statement group as the `password_hash` write (3 call sites in friends.js); 07's approve endpoint is exempt (a brand-new friend has no tokens — record why at the call site); redemption's own `active=1`/`password_hash` checks are the load-bearing gate, these deletes are conservative hygiene; e2e = the four-row matrix appended to `magic-link.spec.js` (needs ML-T2+T3 for request→event→redeem-401 round trips).

## 10. Google auth (10) — GA-T1 unblocks FC-T3; login card lands AFTER ML-T4/T5

- [ ] GA-T1  Schema: `friends.google_sub/google_email/google_prompt_dismissed` + partial UNIQUE index on `google_sub` + `invitations.google_sub/google_email` — `10 §UC-GA-001` ⚠ partial index because a bare ALTER cannot add UNIQUE (the `idx_friends_username` precedent); UNBLOCKS FC-T3 (11 §UC-FC-006) and makes FC-T1's `googleLinked` derivation live; NO unique index on invitations by design — authoritative uniqueness is `idx_friends_google_sub` at approval (GA-T9), registration checks are courtesy (GA-T8). Highest-leverage row for cross-module unblocking — land early.
- [ ] FC-T3  Admin unlink Google: `DELETE /api/friends/:id/google` (requireAdmin, idempotent 200 — GSO-T5 convergence precedent, verified by re-reading the row) + "Odpojiť Google" row-menu item gated `v-if="googleLinked"` with the no-password `confirm()` warning — `11 §UC-FC-006 + §UC-FC-008 items 2,6` ⚠ was `[!]` blocked on GA-T1; placed here so top-to-bottom stays a valid build order (also needs FC-T1's derivation + FC-T2's menu, both above); does NOT reset `google_prompt_dismissed` (module 10's resolved decision — DELETE touches `google_sub`/`google_email` only; the friend's way back is GA-T7's manual link); NO session invalidation (deliberate asymmetry with admin-username changes, documented in the UC); MUST add the route to `ADMIN_ENDPOINTS` in `api-security.spec.js` (standing rule — with this task, not before the route exists); deliberately a DIFFERENT path from GA-T5's friend-owned `…/google-link` — never multiplex them.
- [ ] GA-T2  `helpers/google-auth.js` verifier (`google-auth-library`, timeout-bounded second outbound call) + `GOOGLE_CLIENT_ID` plumbing (auth-mode response gains `googleClientId: string|null`) + `GOOGLE_AUTH_TEST_MODE` e2e seam + feature-off 503s — `10 §UC-GA-002` · model=heavy ⚠ `verifyGoogleIdToken` is consumed by GA-T4/T5/T8/T10 with its 4096-char type guard and the 401-vs-503 (invalid vs outage) contract defined ONCE here; ⚠ STRUCTURAL RULE (IA-T3 class, no test can hold it): verification is network I/O and MUST run outside any `db.transaction` — code comment at every future call site; `TEST:<sub>:<email>` tokens honoured ONLY when the env is set, boot line names the mode (prod audit); never hand-roll JWT verification. OPERATOR (gates first staging verify, not the build): create the OAuth client with the 6 authorized JS origins per §UC-GA-002 + set `GOOGLE_CLIENT_ID` in both `.env`s; one-vs-two clients for staging = recorded operator OPEN.
- [ ] GA-T3  CSP additions for GIS (6 nginx header lines, BOTH confs — `frame-src` is a NEW directive, GIS iframes are currently blocked by `default-src 'self'`) + `self-hosted-fonts.spec.js` verbatim header copy + scoped route-sweep allowlist + `lib/gis.js` lazy loader — `10 §UC-GA-012` ⚠ must land before any REAL button renders; the spec-file header copy moves in the SAME change as the nginx edit (case (a), cite UC-GA-012 — 01-architecture's explicit obligation); `gis.js` is the ONE home for GIS script loading (never `index.html`, never guest routes) — every button surface (GA-T4/T6/T7/T8/T10) imports it; route sweep: `accounts.google.com` allowed on `/` and `/invite/:code` only, `/g/:token` stays ZERO-external; re-verify the exact allowances against current GIS docs at implementation time (spec's own instruction); testable via the RD-DS-6 throwaway-static-server pattern; record beside the RD-DS-6 CLAUDE.md entry on landing.
- [ ] GA-T4  Friend Google login: `POST /api/friends/auth/google` + password-login response gains `googleLinked`/`googlePromptDismissed` + login-card GIS button ("alebo" divider, modern mode + configured only) — `10 §UC-GA-003,005` · model=heavy ⚠ after ML-T4 AND ML-T5 (same login card — this row adds ONLY the divider + button; ML-T5's affordance owns the password-group slot); response byte-compatible with password login's personal branch + 2 additive fields — GA-T6's prompt keys on them with NO extra request; forwards module 09's `remember` flag to the mint (additive both sides); `not_linked` hint-401-with-code vs GENERIC 401 for `active=0` (the two-step no-active-filter SELECT is deliberate); legacy-mode 409; honours `must_change_password` via the existing forced gate; strip-rule pin (`/google_sub/` zero times, hand-picked response — FC-T1's rule inherited); authLimiter; real iframe untestable — button presence/absence + zero `accounts.google.com` requests is the pin.
- [ ] GA-T5  Friend link/unlink/prompt-dismiss endpoints: `PUT`+`DELETE /api/friends/:id/google-link`, `POST …/google-prompt-dismissed` (owner-guarded) — `10 §UC-GA-004` · model=heavy ⚠ identity-ownership writes: the 409 collision names NO friend, dual-layer (app pre-check + `SQLITE_CONSTRAINT`→409 translation on the partial index — GSO-T10 pattern); re-link replaces without an unlink ceremony; idempotent DELETE (GSO-T5); unlink NEVER touches `google_prompt_dismissed` (resolved #3, symmetric with FC-T3); `warning:'no_password'` on unlink is consumed by GA-T7's confirm copy; PUT rides authLimiter (attacker-suppliable tokens), dismiss/unlink need none; the UI callers land later (GA-T6/T7) — API-only, cleanly testable via the seam.
- [ ] GA-T6  Post-login link prompt on NeoModal: Áno teraz / Teraz nie / Už sa nepýtať, ONE prompt per login max, gate-skip rule, session-boundary safe — `10 §UC-GA-006` · model=heavy ⚠ "Teraz nie" is CLIENT-ONLY, never a server write — the flag endpoint serves only "už sa nepýtať"; prompt state lives on the handshake entry + a local declined-ref — NO module-level state, NO localStorage, NO friend-id-keyed state (the six-leak-bugs hazard restated as a spec requirement); trigger matrix: non-Google modern login only, SKIP when the forced-password-change/credential-setup gate fired, never on session restore; the mandatory two-friend session-boundary e2e is UC-GA-013 item 5; small print points at GA-T7's profile section.
- [ ] GA-T7  Profile-modal Google section: linked/unlinked states, manual link + unlink with the no-password warning — `10 §UC-GA-007` ⚠ purely ADDITIVE to module 03's "Upraviť profil" (which FC-T4 and ML-T6 have also touched by now) — `portal-profile-modal.spec.js` passes UNCHANGED; updates the handshake-scoped `googleLinked` in place so GA-T6's prompt logic stays consistent within the session; never touches `google_prompt_dismissed`; seeds from the login handshake if the profile fetch doesn't carry the derivation.
- [ ] GA-T8  Invite-registration Google attach: optional `google_id_token` in `POST /invitations/register` (verify → freeze `google_sub`/`google_email` on the invitation row) + InviteRegister optional GIS block — `10 §UC-GA-008` ⚠ absent token ⇒ BYTE-IDENTICAL flow (pin it); 400 `field:'google'` — registration is not a login; two courtesy 409 dedupes (existing friend / other pending invitation, app-check only); the invitation row's google fields are FROZEN at submit (07's convention) — GA-T9 copies them, never re-verifies; the register handler becomes async (07's hardened path — care); the AdminInvitations "Google" badge is assigned to GA-T9; rides abuseLimiter (⚠ local-run caveat: repeated runs exhaust the invite-code lookup — raise `RATE_LIMIT_ABUSE_MAX`).
- [ ] GA-T9  Approval of a Google-attached invitation: friend INSERT carries the two google columns, interim-collision 409 + "approve without the Google link" (`drop_google_link: true` re-submit, resolved 2026-08-15) + dialog Google line + invitations-list badge — `10 §UC-GA-009` · model=heavy ⚠ IA-T3's STRUCTURAL INVARIANTS preserved: still ONE friend INSERT, exactly TWO writes inside the tx, bcrypt/collision loops/pre-checks OUTSIDE it, NO token verification at approval (the stored sub is data, not a credential) — restate as code comments, no test can hold them; the 409 leaves NO friend row written; the 201 body gains NOTHING (strip rule — the dialog learns link state from the invitation row it already has); `invitation-approval.spec.js` passes unchanged + gains the Google cases. OPEN (recorded): the "…alebo sa prihlás cez Google" sentence is unsigned — the credentials message ships UNCHANGED until the product owner signs it.
- [ ] GA-T10  Admin surface: google-allowlist CRUD (`settings.admin_google_subs`, GET strips sub values — `/"sub"/` zero times) + `POST /api/admin/google-login` + AdminSettings section + AdminLogin GIS button (password stays, permanent backup) — `10 §UC-GA-010,011` ⚠ the three allowlist routes join `ADMIN_ENDPOINTS` in `api-security.spec.js`; the two PUBLIC Google login endpoints must NOT (UC-GA-013 item 1); the mint is byte-identical to password login — ONE admin token app-wide, Google login ROTATES it too, so any e2e mixing UI login with API-minted tokens must adopt the browser's token (the IA trap); allowlist add proves possession via an id_token — no hand-typed identities; removing the LAST entry is allowed (password backup is the guarantee, brief item 3); no `auth_mode` dependency (that governs the friend surface only); admin screens stay shadcn.

> **Section 7–10 closing note.** The manual staging walkthrough with a REAL Google account is the
> only coverage for wrong-aud/expired real tokens and the real GIS iframe — gated on the operator
> OAuth-client checklist in GA-T2. Unsigned-copy OPEN items ride: EM-T2 (button/footer/preheader),
> ML-T5 (module-09 consolidated sign-off), GA-T9 (Google sentence in the credentials message) —
> each ships its spec-recorded safe default and none blocks a row.

---

## Log


<!-- /next-task appends one line per completed task below (durable cross-session record). -->

2026-08-15 · FUP-T8 · <SHA> · [PR #<N>](https://github.com/karolskolar/gorifi/pull/<N>) · ⚠ **CODE SIDE DONE — row deliberately left `[~]`, acceptance needs an operator measurement the sandbox cannot perform.** `client_max_body_size 11m` on `location /api` in both deploy confs. ⚠ **The row's own premise was wrong and the implementer caught it:** the shipped admin UI does NOT upload photos as multipart — `readAsDataURL` in `CycleDetail.vue`/`AdminBakeryProducts.vue` sends a base64 `data:` URI **inside a JSON body**, so the binding cap is `express.json({limit:'10mb'})`, not multer's 5 MB (the only multipart the FE sends is the CSV import). Hence the prod bug was worse than recorded (base64 ≈ 4/3 of the file ⇒ photos failed at ~**750 KB**, not 1 MB) and the "obvious" `5m` fix would have **reintroduced the bug one notch up**, refusing a ~4 MB photo Express accepts. `11m` not `10m` because 10m is byte-identical to `express.json`'s limit (both 10485760, both reject on *exceed*) — nginx would win that race and serve bare HTML where the app has a Slovak JSON 413. Scoped to `/api`: no other location takes a body, so they keep the protective 1 MB default; `client_max_body_size` is single-valued, so the `add_header` inheritance trap documented in the same file does NOT apply. ⚠ **Review found the finding that actually matters (major):** the **NPM edge**'s `10m` is **documented-only and UNVERIFIED**, and §2b of that very doc concludes the Advanced block containing it "was never applied or never saved" — which is exactly why the security headers were moved into the LXC conf. So there are two worlds and only measurement separates them: if the edge is really `10m`, `express.json`'s `entity.too.large` 413 is **publicly unreachable** (byte-identical caps) though multer's 413 stays reachable and nothing legitimate is refused; if the block never applied and the edge is on nginx's **1 MB default**, then **this row fixes nothing** and the photo still dies one hop earlier. Both confs and the NPM doc were corrected in-commit to stop asserting the chain is coherent, and to say plainly that the edge must be measured, not read. Also corrected: the "sitting above every cap *guarantees* the app always answers" claim (false for any public request — the same comment half-retracted itself 11 lines later), the staging conf's uncorrected copy of it, and the multipart-envelope sentence (true, but ~5.25 MB is already ~5 MiB clear of a 10m cap, so it is a note, not a justification for the extra MiB). ⚠ **Structurally untestable:** every e2e spec hits Express on :3997 with no nginx in the path, so **no test was added, deliberately** — a test claiming to cover this would be vacuous (same class as RD-DS-6's "the gate sends no security headers"). `nginx -t` could not be run either (no nginx/docker/podman in the sandbox, and the implementer did not claim otherwise); the real gate is `deploy/deploy.sh:247`, which runs `nginx -t` remotely before `nginx -s reload` under `set -e`. Verified instead: brace-depth 0 and every directive semicolon-terminated in both files, and a sanity batch (`image-upload` + `malformed-body` + `api-security`) **66 passed** with `git diff -- backend/ frontend/` empty. Review: **revise → corrections applied in-commit** (2 major, 4 minor; all six acted on). **Operator procedure to close the row lives in PR #<N>.**

2026-08-15 · FUP-T6 · 5534e92 · [PR #84](https://github.com/karolskolar/gorifi/pull/84) · Oversized upload returns 413, not 500 — the multer half of FUP-T3. `MulterError` carries `code`/`field` but **no `status`**, so FUP-T3's "preserve any 4xx" rule was blind to it and a >5 MB upload answered a pure client mistake as a server fault (plus a remotely-triggerable full stack in the log, the exact thing FUP-T3's 4xx branch exists to avoid). ⚠ **The design point: the handler GIVES the error a status (`Number(carried != null ? carried : multerStatus(err))`) rather than answering it in a second branch** — so it flows through FUP-T3's existing 4xx branch and **inherits both of that row's decisions for free** (no stack log; `err.message` never echoed — `LIMIT_UNEXPECTED_FILE`'s message quotes the client-supplied field name back at the caller). Copy reuses the existing `CLIENT_ERROR_MESSAGES` map; no parallel mechanism. `LIMIT_FILE_SIZE` → **413**, every other code (`LIMIT_UNEXPECTED_FILE`, `LIMIT_FILE_COUNT`, `LIMIT_PART_COUNT`, `LIMIT_FIELD_*`) → **400**. Placed in the GLOBAL handler, verified rather than assumed: multer is per-route middleware but delivers failures via `next(err)`, and `grep '(err, req, res, next)' backend/src` returns exactly one hit, so all **five** `upload.single(...)` routes (`products.js:43,72,147` + `bakery-products.js:50,191`) already arrive there — enumerated, not sampled, and all five confirmed 413 live. Reviewer walked the status computation exhaustively (carried `0` / `'abc'` / 5xx / `statusCode`, `multerStatus` → `null` ⇒ `Number(null)` = 0, a SqliteError-like `code`-bearing error): nothing 500 becomes 4xx except a `MulterError`, nothing 4xx becomes 500, and FUP-T3's four cases are unmoved. `err.code` joined the warn line only because multer's codes are a fixed 9-value library enum, never client text; `err.field` is client-supplied and deliberately NOT logged, and `req.originalUrl` cannot carry a raw newline (verified), so no log-injection surface was added. ⚠ **Auth boundary pinned across ALL FIVE routes** (review minor 2, fixed in this commit — the shipped test sampled only one): the guard must run before multer or an anonymous caller learns whether a file was too big. `products.js` carries `requireAdmin` inline; **the two bakery routes carry NO inline guard and are protected only by the `requireAdmin` on their MOUNT** (`index.js:92`) — the fragile case, and precisely why one sampled route could not protect the property. e2e: NEW +13 tests in `image-upload.spec.js` (8 shipped + 5 from the widened loop), written RED first — 6 failed / 5 passed before the fix, where the 5 include two counter-assertions (a within-limit upload still 200s; the anonymous 401) that must never be red. Gate: `node --check` green; orchestrator's independent **707 passed / 3 skipped / 0 failed across all 58 files** on a freshly seeded DB (baseline 699/3 — delta exactly the 8 shipped tests), plus 66 more after the two review fixes. Review: **approve, 3 minors**, all three acted on rather than waived — the over-strong comment corrected in-commit, the auth loop widened in-commit, and the two residuals recorded as **FUP-T7** (plain multipart `Error`s still 500-with-stack, incl. the ordinary mid-flight abort) and **FUP-T8** (⚠ live prod issue: the container nginx has no `client_max_body_size`, so its 1 MB default silently overrides the app's 5 MB cap and a 1–5 MB photo upload fails in production today — and the e2e gate is structurally blind to it, since every spec hits Express directly). ⚠ Harness: the FUP-T5-era DB-accumulation effect has a practical workaround — run `portal-session-boundary.spec.js` FIRST on a freshly seeded DB (3.3 s); running it last reds its two double-login walks on any full run.

2026-08-15 · FUP-T5 · f51fc85 · [PR #83](https://github.com/karolskolar/gorifi/pull/83) · An unhydrated profile modal wiped a stored Packeta address. `saveProfile()` sent `packeta_address` unconditionally, but the modal seeds it at open from `props.friend`, which arrives via the **fire-and-forget** `hydrateCurrentFriend()` — so opening the modal before that GET landed and saving wrote `null` over a real address. FC-T4 added the open-time-original delta for `phone`/`email` for exactly this reason and left this field out of scope; this row closes it, so `saveProfile` now has ONE rule rather than two. ⚠ **Approach (b) — gating the save on hydration — was rejected as STRUCTURALLY UNABLE to fix it, not merely riskier:** `hydrateCurrentFriend` swallows its error (`FriendPortal.vue:287-289`), so a "hydration **settled**" gate still admits a save whose field is empty and whose write is `null` — the identical wipe; and a "hydration **succeeded**" gate would permanently disable ALL profile saving (including the name) for any friend whose profile GET fails, on a fetch that is non-fatal by design. Record that reasoning before anyone re-proposes the gate. ⚠ **PAYLOAD-SHAPE CHANGE: `name` is now the only key `saveProfile` always sends.** `docs/specification/03-friend-login-portal.md:525` still describes the old `{ name, packeta_address }` body — authorized deviation (this row's own text names the delta as the fix), recorded here in prose rather than by amending 03, following FC-T4's precedent for the same class. `friends-consolidation.spec.js:1036`'s exact-keys pin `['name','packeta_address']` survives **only because that test genuinely fills the address**; a future test that saves without touching it must expect `['name']`. Reviewer walked every trim case and found no combination that silently swallows a real change — the one that could (a stored **whitespace-only** address) is unreachable, because `friends.js:700-702` is the column's ONLY app-wide writer and has normalised `?.trim() || null` since the column's first write (`bd605d2`). Two cases get strictly better: a stale hydrate response landing after a save used to write the stale address back on the next save, and the hydrate-lands-while-open window is now non-destructive in both directions. `profilePacketaOriginal` is seeded per modal OPEN (after the field is assigned) in a component `:key`ed on `sessionSeq`, renders nowhere, and carries nothing across friends — the six-leak rule holds. `FriendOrder.vue:873`'s separate `updateFriendProfile` call is untouched and safe: it is gated on a non-empty address, sends `{ packeta_address }` alone (can only set, never clear), and `order-modals.spec.js:696` still pins that exact single-key payload. e2e: +1 test (`⚠ an unhydrated modal must NOT wipe a stored Packeta address`) that stalls the profile GET, asserts the empty-field precondition, then checks BOTH the wire payload and a server-side re-read — red first, receiving `{name, packeta_address: null}` where `{name}` was expected. **Sanctioned spec edit** (one, minimal): `portal-profile-modal.spec.js:346`'s "blank Packeta address is sent as null, not `''`" keeps its assertion in substance — only its setup changed, because the shared read-only fixture has no stored address, so under the delta a whitespace fill reads as *unchanged*; it now provisions its own friend WITH an address (plus an on-screen non-vacuity check) so the clear is a genuine change. The shared fixture deliberately stays address-less — the prefill tests above assert that field opens empty. Gate: orchestrator's independent 116-test batch, then 90 more after a review-minor comment fix; full suite **699 passed / 3 skipped / 0 failed** (baseline 698/3, delta is exactly the 1 new test). Review: **approve, 2 minors** — a stale in-function comment still claiming "the pinned `{ name, packeta_address }` payload is preserved" (fixed in this commit; it was the one that could have led a future reader to reintroduce the wipe) and the 03 staleness (recorded above). No backend change (`git diff -- backend/src` empty).

2026-08-15 · FUP-T3 · 004d035 · [PR #82](https://github.com/karolskolar/gorifi/pull/82) · Global 500 on an unparsable/scalar JSON body → correct 4xx (`07 §Follow-ups item 1`). `express.json` is mounted above every router, so a body it refuses never reaches a route and the global handler in `index.js` reported every such CLIENT mistake as a 500 — app-wide, on every JSON endpoint, reachable unauthenticated. ⚠ **The fix PRESERVES whatever 4xx the error already carries** (`err.status ?? err.statusCode`, 400–499 echoed; everything else keeps 500) rather than flattening to 400 via a `type` allowlist: body-parser has already classified the failure and picked the better status, so `entity.parse.failed` → 400 but **`entity.too.large` → 413** and **`encoding.unsupported`/`charset.unsupported` → 415** (all three were 500 before). The generalisation's load-bearing premise — **no app route or helper throws with a status** (every `throw` is a bare `Error` or a rethrown better-sqlite3 `SqliteError`; no route calls `next(err)` at all) — was verified independently by the orchestrator AND the reviewer, and is stated in the code comment as the invariant it depends on. Reviewer additionally enumerated every third-party source of a status-bearing error in the real chain: `express.static` with default `fallthrough:true` calls `next()` (not `next(err)`) for anything under 500 so it can never deliver a 4xx here at all; `express-rate-limit` answers 429 itself; `cors` rejects with a bare `Error` → still 500 (verified live, which keeps `helpers/credentials-message.js:82`'s documented "a 500 before any route runs" true); `multer`'s `MulterError` carries no status → still 500 (**recorded as FUP-T6**, deliberately not smuggled in here). Free improvement discovered in review: Express's `decode_param` 400 on a malformed `%`-escape (`GET /api/products/%E0%A4%A`) was a 500 too. ⚠ Two deliberate decisions, both to be kept if this handler is ever touched: the 4xx branch **does not log a stack** (any unauthenticated client can trigger it — a stack per hit is a free remote log-flood; it logs one compact `console.warn` with method/URL/`err.type`/status, and for a body-parser rejection the stack pointed into `body-parser/lib/types/json.js` anyway, so nothing diagnostic was lost), and **`err.message` is never echoed** (its text quotes the offending input and names a byte offset). Genuine 500s keep `console.error(err.stack)` byte-identical. ⚠ Consequence, pinned as a decision not an accident: an anonymous malformed body on an **admin** route now returns 400 rather than 401, because the parse failure precedes `requireAdmin`. Verified to be no oracle — the 400 is identical for an existent admin route, a nonexistent API route, a nonexistent nested route and a non-API path, while **well-formed** anonymous requests still get 401; `api-security.spec.js`'s `ADMIN_ENDPOINTS` census sends only well-formed bodies, so that boundary is untouched. Recorded residuals: the SPA fallback's `res.sendFile` can still hand this handler a 412 (bogus `If-Match`, verified live) or a 404 in the `existsSync`→`sendFile` race, answering an HTML route with JSON — but it did that before too as a 500, so only the status changed and both reachable cases are more truthful now; and the 4xx warn line interpolates `req.originalUrl`, which on `/api/guest/:token/…` writes a guest link token into the app log (nginx's `combined` access log already records the full request line on prod and staging, and only the token holder can trigger it — but it is the one new place the app itself writes a URL-borne credential). e2e: NEW `malformed-body.spec.js`, **19 tests**, written RED first — 14 failed / 5 passed before the fix, 19/19 after; the 5 pre-green ones are counter-assertions (valid bodies still reach their routes, CORS still 500) so the fix cannot pass by breaking them. Gate: `node --check` green; orchestrator's independent 106-test batch, then **698 passed / 3 skipped / 0 failed across all 57 files** on a freshly seeded DB (baseline 679/3 — delta is exactly the 19 new tests, zero existing specs touched). Review: **approve, ZERO findings**, 3 notes (all recorded above). No UI ⇒ e2e-tester skipped; the 19 API e2e tests are the coverage.

2026-08-15 · FC-T4 · 969571d · [PR #81](https://github.com/karolskolar/gorifi/pull/81) · Friend self-edit Mobil + Email (11 §UC-FC-009) — module 11's friend-facing half. `FriendPortalSession.vue`'s "Upraviť profil" modal gains **Mobil** (`maxlength=32`, format-example placeholder `+421 900 000 000`) and **Email** (`type=email`, `maxlength=160`, NO placeholder per the 2026-08-10 login decision, vy-form field-help verbatim); `PATCH /friends/:id/profile` accepts phone/email through FC-T1's `validateAdminFriendFields` via a new `only` field-subset param, so the Slovak messages and `field` markers cannot drift from the admin routes'. `name`/`packeta_address` keep module 03's rules and the pinned `'Prihlasovacie meno je povinné'` is byte-untouched (the blank-name check MOVED above the contact validation — verified `{name:'', email:{}}` still yields the pinned message). ⚠ **Two review-round-1 majors, both real, both invisible to a green suite:** (1) Mobil/Email rendered EMPTY for a whole freshly-logged-in session — `hydrateCurrentFriend()` ran only on the two session-RESTORE paths and neither `/friends/auth` response carries contact data, so the friend could not see whether an address was on file, directly under the hint saying a missing e-mail costs them their recovery link; the round-trip test missed it because it re-opened the modal after a `page.goto`. Fixed by hydrating after BOTH fresh-login paths, fire-and-forget, plus a **session-boundary guard** on `hydrateCurrentFriend` itself (pins `sessionSeq` at call time + re-checks `currentFriend.id` before merging) — `switchUser()` does not bump `sessionSeq`, so the id check is load-bearing, not belt-and-braces. (2) `requireFriendOwner` deliberately allows ANY target id while `auth_mode !== 'modern'` (the documented legacy migration window), so this row would have widened that window to `email` — and 09 resolves recovery by `lower(trim(email))` "exactly one active match", making a planted address a recovery seam on a victim with no e-mail of their own that SURVIVES the flip to modern. Fixed with a **contact-only 401 gate** (`phone !== undefined || email !== undefined` && `owner.friendId == null`), narrower than `requireFriendOwner` and keyed on **presence not truthiness** so a `{email: null}` clear is gated too; costs real users nothing (`/friends/auth` mints a per-friend token in BOTH modes, and `setFriendsPassword()` has no caller anywhere in `frontend/src`). Both fixes mutation-verified. Frontend sends a contact field ONLY when changed, so an admin's concurrent edit is not clobbered and the pinned `{name, packeta_address}` payload shape survives — asserted on the wire. Recorded, non-destructive: a hydrate landing AFTER the modal is open leaves the fields blank for that open (the delta then omits them rather than clearing). e2e: +11 tests (4 API bounds/type-guard/atomicity, 3 UI field-contract/round-trip/error-banner, fresh-login prefill, payload-omission, the legacy-mode 401, and the e2e-tester's hydrate-race leak test that holds A's profile GET open across a logout into B's session). Review: 2 rounds → **approve**, all 4 findings resolved, no new defects. Gate: `node --check` green; orchestrator's own runs 66 + 242 + 393 + 165, then **679 passed / 3 skipped / 0 failed across all 57 files** on a fresh DB. ⚠ **Harness finding, NOT a regression:** `portal-session-boundary.spec.js`'s two double-login walks timed out at 30s on the SHARED e2e DB — reproduced identically with FC-T4's source stashed, and they run in **3.2s on a freshly seeded DB** (31.6s → 3.2s). Accumulated friends make the login-list/subscription surface ~10× slower; this is deterministic and data-driven, NOT the "memory pressure, different set each run" hazard CLAUDE.md documents. Re-seed before trusting a slow-walkthrough failure.

2026-08-15 · FC-T2 · 7b40140 · [PR #80](https://github.com/karolskolar/gorifi/pull/80) · AdminFriends consolidation UI — SUBSUMES FUP-T1 + FUP-T2 (both flipped done-by-FC-T2). Table: `Meno a priezvisko` header, `Kontakt` column (amber `Bez e-mailu` badge — module 09's operational remedy, FC-T4's acceptance keys on it), `Google` column (`googleLinked` only, muted `-` pre-GA-T1, no unlink item — FC-T3's), truthful three-state `Prihlásenie` (username / amber `Neúplné` with `title` naming what's missing — the FUP-T2 fix, partial state created via the REAL reset-password path / `-`) + `dočasné heslo` marker (full state only, spec-literal). Modal: 4 writable fields with FC-T1's bounds as `maxlength` (120/32/160/200), spec-verbatim hints (worded to pass the grep guard), read-only Prihlásenie/Google blocks, `Minimálne 8 znakov` (the FUP-T1 fix). ⚠ DEVIATION, review-upheld + spec AMENDED (11 §UC-FC-003): save errors render in a NEW in-dialog `modalError` Alert — the radix overlay hides the page-level Alert, so the spec's letter would render errors invisibly; `error` keeps its four non-modal writers, nothing writes both. Sanctioned edits: `admin-friends-labels.spec.js` exactly 3 hunks, all case (a) with UC-FC-008 #1 citations (two label retargets + the old name-hint assertion re-pointed to the mandated new copy — reviewer judged it within the sanction); `/Prihlasovacie/i` absences + `Prihlásenie` deliberate-keep byte-unchanged; grep gate `grep -i prihlasovac AdminFriends.vue` EMPTY. Old shadcn skin, zero neo/theme classes (reviewer-swept). Cells carry `data-testid` (`contact-cell`/`login-cell`/`google-cell`). e2e-tester: PASS, +2 tests / 2 extended (create-flow end-to-end; UI-driven email clear → badge reappears; full-state modal read-only; reset-password dialog rejecting 7 chars then updating the row live) — `friends-consolidation.spec.js` now 26, `admin-friends-labels` 4. Gate: orchestrator 74/75 (1 pre-existing flake green solo) + reviewer's independent 28/28 + e2e-tester regression sweep. Review: **approve, 1 minor** (the spec-letter staleness — amended in the module doc, this entry is the record). CLAUDE.md module-07 `Meno *` bullet superseded in the same commit. `vite build` note: this host needs `NODE_OPTIONS=--max-old-space-size=1536` or it SIGSEGVs.

2026-08-15 · FC-T1 · f4f223c · [PR #79](https://github.com/karolskolar/gorifi/pull/79) · friends.js admin-route hardening + response invariants — module 11 opens. Bounds as LOCAL constants (`MAX_NAME_LENGTH 120 / MAX_PHONE_LENGTH 32 / MAX_EMAIL_LENGTH 160 / MAX_NOTE_LENGTH 200`, applied after trim — FC-T2/FC-T4 mirror these as `maxlength`); strict `adminString()` type guard (fixes the LIVE `{email:123}`→500) + `validateAdminFriendFields()` on POST/PATCH only; 400s carry `{error, field}`; POST/PATCH blank-name → `'Meno a priezvisko je povinné'` (relabelled, no `Prihlasovacie` on the admin path); email keeps `includes('@')`; `PATCH /:id/profile` (module 03) byte-untouched and pin-tested. `sanitizeFriend()` derives `googleLinked = !!google_sub` BEFORE the strip and `google_sub` joins the strip list (no-op until GA-T1; module 10 INHERITS the rule — commented); ships on every sanitized response incl. friend `/profile` (reviewed superset, one-home rule). Zero `ALTER TABLE`; POST now stores name/phone/display_name TRIMMED (reviewed: no byte-wise consumer breaks). ⚠ Review round 1 found a REAL blocker the suite was blind to: `adminString(null)` read as "absent", so `PATCH {phone:null,…}` — the exact shipped AdminFriends.vue clear payload — returned 200 while silently KEEPING the old values (a removed e-mail would have kept the module-09 recovery seam alive on stale data); reproduced live, fixed as `null → ''` → flows through `value || null` and writes NULL; accepted side effect `PATCH {name:null}` → 400 blank-name (old code 500'd on the NOT NULL constraint); pinned by a new null-clear test with TDD red confirmed against the pre-fix build. Minor: PATCH validates before the existence lookup (400-before-404) — deliberate fail-fast, commented against the GSO-T5 convention. e2e: NEW `friends-consolidation.spec.js` (17 API tests — FC-T2 adds its UI describes to the same file): type guards, at/over-bound, null-clear, blank-name row-unchanged, UC-IA-005-style strip regex on list/detail/201, UC-FC-007 invitation-row `toEqual` snapshot after approve→edit, module-03 message pin. Gate: `node --check` green; orchestrator 49/49 (friends-consolidation 17 + api-security 32) + 94/95 wider batch (1 pre-existing dialog flake, green solo); review 2 rounds → **approve**. No UI ⇒ e2e-tester skipped.

2026-08-15 · EM-T3 · adae4b9 · [PR #78](https://github.com/karolskolar/gorifi/pull/78) · `PUBLIC_BASE_URL` boot line + pin-beats-Origin proof + operator checklist — **module 08 complete**. Boot line at import time in `credentials-message.js` (lands under the `[mailer]` line in out.log): `[mail] PUBLIC_BASE_URL=<resolved origin>` / unset-fallback line / a THIRD case for a set-but-invalid value (schemeless `podpultovka.biz` reported as `invalid (…)` falling back, never as "set") — deviation upheld by review as the exact silent-regression mode the line exists to surface; the set branch prints `URL.origin`, so userinfo/path can never leak. `resolveLoginUrl()` byte-untouched (pin-don't-patch). e2e: A/B pair on `withMailHarness` — pin + different allowlisted Origin ⇒ login_url + BOTH mail parts equal the pin and the Origin appears in neither; unset ⇒ Origin wins everywhere (the non-vacuity control). `ecosystem.config.cjs` comment-only (programmatically confirmed): the "optional" claim rewritten to the required prod+staging pin (`https://podpultovka.biz` BOTH — Mailgun free-tier one-domain decision 2026-08-15) + 5-step operator checklist; the literal server `.env` edits remain OPERATOR work — nothing deployed. Gate: `node --check` green ×3, 50/50 orchestrator-verified + reviewer's independent 50/50; mutation-verified (reverting the boot block reddens exactly the 2 new tests). Review: **approve, 2 minors banked**: (1) the invalid-branch boot line echoes the RAW env value into the log — cap length and strip `://[^@]+@` userinfo when next touching the file; (2) the third boot-line case is untested — a ~15-line harness case (`PUBLIC_BASE_URL: 'podpultovka.biz'` ⇒ invalid line + fallback login_url) would pin it; both fit ML-T2's neighbourhood (module 09 touches this seam). No UI ⇒ e2e-tester skipped.

2026-08-15 · EM-T2 · 42e23e4 · [PR #77](https://github.com/karolskolar/gorifi/pull/77) · `renderEmail({text, blocks})` in `helpers/email-templates.js` + the credentials mail restyled onto it. Block vocabulary (ML-T2's contract): `paragraph` / `kv` (mono values) / `button` (label defaults to url; a plain-text URL line ALWAYS renders under the button) / `small`. Renderer throws on non-string/empty/WHITESPACE-ONLY `text` (the EM-T1 reviewer note, honoured), missing/empty/non-array `blocks`, unknown type (own-property gate — `constructor`/`__proto__`/`valueOf` verified to throw, the variantGrams hazard closed), malformed fields; `text` returned by reference so the pass-through is `===` (mutation-verified: `.trim()` reddens the pin). Route: render INSIDE the existing mail try/catch, after the tx commit — a renderer throw degrades to `email:{sent:false}` on a 201 and can never skip the invitation UPDATE (reviewer verified lines 442–483 directly); `sendMail` still receives the original `credentialsMessage()` string, so clipboard/mail byte-identity holds by construction. Harness EXTRACTED to `e2e/mailgun-harness.js` (behaviour-identical — env blanking, stub lifecycle, `setMailSafeTarget` verbatim; `ctx`/`adminToken` swap stayed in the spec's thin wrapper) — ML-T2 consumes `startMailgunStub`/`withMailHarness`/`multipartFields`/`sendViaMailer`/`renderViaTemplates`. ONE sanctioned assertion flip in `invitation-approval.spec.js` (approve-route test: no-html → html present, pre-commented for exactly this); every other pre-existing test byte-identical (diff = 6 accounted hunks). OPEN honoured: zero invented Slovak copy — URL-as-label + §UC-EM-003 rule-3 signed fragments only; `DIM #6b6459` is an invented mail-safe de-emphasis grey (brand approximation, recorded). Gate: `node --check` green; orchestrator-verified 65/65 (email-templates 3 + invitation-approval 45 + guest-lead-capture 17) in one clean run. Review: fresh-context — **approve, 1 minor**: sparse `blocks` array (`[,]`/`new Array(3)`) silently renders an empty branded card instead of throwing (`.map()` skips holes) — unreachable via JSON, first-party JS only; ⚠ ML-T2 should fix it when touching the renderer's consumer (iterate indices, fail on holes/`undefined`). Reviewer's first run showed 17 failures — the documented CORS trap (127.0.0.1 vs localhost), not the diff. No UI ⇒ e2e-tester skipped.

2026-08-15 · EM-T1 · 2825561 · [PR #76](https://github.com/karolskolar/gorifi/pull/76) · `sendMail()` grows the optional `html` part + `o:tracking-clicks/opens=no` on EVERY send (incl. text-only). Seams honoured: ZERO html callers ship (EM-T2 wires `renderEmail()` — until then every send is byte-identical to before apart from the two flags, pinned by the approve-route test); html with empty/absent text degrades to TEXT-ONLY and never throws (the contract EM-T2's render-inside-try/catch leans on); html only attaches when `typeof html === 'string' && html !== ''` — non-string html is silently dropped, no new throw path. Three mailer rules verified intact: flags + html sit AFTER the recipient→config→validity checks so no-op-without-env holds (zero-requests stub test unchanged), result vocabulary/three-check ordering/`redact()` byte-identical in the diff, nothing new logged. e2e: 4 new TDD tests in `invitation-approval.spec.js` (multipart both-fields, no-html ⇒ field ABSENT not empty, empty-text degrade, approve-route text-only) + two helpers (`multipartFields()` boundary/CRLF parser, `sendViaMailer()` child-process direct-import with all three MAILGUN_* vars unconditionally overridden to the local stub — an ambient real key can never send); placed beside the existing stub harness so EM-T2's planned extraction lifts them as one block. Gate: `node --check` green; orchestrator-verified 43/43 on the file (one run flaked on the documented 4GB-host Chromium instability — timeout, then an in-memory-corrupted playwright bundle (`cïlors2`), disk file clean, test passes alone and the final full run is 43/43). Review: fresh-context, independently re-ran tests on its own throwaway backend — approve, ZERO findings, 3 notes (whitespace-only text counts as present — EM-T2's renderer must treat that as its own error; `sendViaMailer` inherits ambient MAILGUN_FROM, harmless/stubbed; child 'error' event unawaited, theoretical). No UI ⇒ e2e-tester skipped (the stub tests are the coverage — IA-T3 precedent).

2026-08-13 · IA-T1 · 1c84a67 · [PR #71](https://github.com/karolskolar/gorifi/pull/71) · Schema foundations for module 07. Two try/catch ALTER TABLE invitations migrations (`username TEXT`, `created_friend_id INTEGER`, no FK by design — informational back-link, consumers tolerate dangling per the GSO-T9 precedent), `generateTempPassword()` = `randomCode(12)` (SEC-S2: delegates to the module-private RNG, deliberately NOT a `generateGuestToken` re-export), and `helpers/friend-create.js` with `getPlaceholderCycleId()` extracted from onboarding.js and now shared with friends.js POST (the inline copy differed only in returning an id vs an object — verified against git history by the reviewer; `dbHelpers.prepare` closes over the one connection so no transaction semantics moved). Seams honoured: both columns have NO writer (IA-T2 writes username, IA-T3 writes created_friend_id) and `generateTempPassword` has NO consumer until IA-T3 — verified by repo-wide grep. Both columns surface through `GET /api/invitations` (`SELECT i.*`, requireAdmin) with no route change — the `source`/GSO-T10 precedent — and no public route selects `*` from invitations, so nothing leaked. Gate: `node --check` × 4 green; migration idempotence proved over three consecutive starts on one DB_PATH; 131 e2e passed across 11 files (regression only — no UI in this task, e2e-tester skipped). Reviewer: approve, 1 minor (this `[x]` flip).


2026-08-13 · IA-T2 · bdca40d · [PR #72](https://github.com/karolskolar/gorifi/pull/72) · Register hardening + the applicant's username field. `POST /invitations/register`: local 120/32/160 bounds + a local `registerString()` type guard (stricter than guest.js's `asString`, which coerces numbers — that coercion IS the `{name:123}`→500 bug), so no path reaches `.trim()`/`.toUpperCase()` on a non-string; guest.js's module-private `validateIdentity` deliberately NOT imported/exported (GSO-T3 money path). Optional `username` lowercased+trimmed, validated ONLY when non-empty (⚠ `validateUsername` returns "povinné" on empty — calling it on a blank value would have 400'd the existing happy path). ⚠ DEVIATION, upheld by review: the username checks sit AFTER the invite-code lookup, because `isUsernameTaken` before it is a username-enumeration oracle open to anyone with no invitation — verified there is no oracle (bad code + taken vs free username return byte-identical 400s). ⚠ The username 400 message is ROUTE-LOCAL, not the shared helper's: `validateUsername`'s own string is diacritic-less (`Uzivatelske meno musi mat 3-30 znakov`) and would be a THIRD name for a field whose label says "Prihlasovacie meno" and whose sibling 409 says "prihlasovacie meno"; the helper is still called for the DECISION and is untouched (`onboarding.js` is its other consumer, where its wording is fine). InviteRegister.vue gained the 4th field (`ir-username`, `.inp` per the A12 iOS rule, `maxlength=30`, no placeholder, vy-form `.field-help` byte-identical to the spec incl. the U+2013 en dash) plus maxlengths on the three existing inputs; blank username is omitted from the body entirely (asserted on `postDataJSON()` keys, not just the effect). Sanctioned spec edits spent: `.field-lbl` 3→4, the no-placeholder loop, the strict-mode `.field-help` SCOPED via a new `data-testid="invite-email-help"` (not weakened to `.first()` — it still proves the EMAIL hint says "Nepovinné", plus count 2 and the username hint pinned verbatim), and `.inp` 3→4 in BOTH ios-input-zoom tests. e2e-tester found two real gaps and closed them: a REAL 409-with-retry journey (the pre-existing failure test only mocked a generic 500) and DOM order `['ir-name','ir-phone','ir-username','ir-email']` (a count assertion cannot catch reordering); it confirmed the 320px overflow test already exercises the 4-field form. Non-vacuity mutation-proved: reverting invitations.js reddens 5 of the new tests and `{name:123}` returns 500 again. Gate: `node --check` green; 23 targeted / 49 broader / 583 full-suite passed, 0 failed. Review: approve, 3 minors — 2 fixed here, the 3rd deferred (see below). Seam: `invitations.username` still has NO reader in the admin UI (`AdminInvitations.vue` never references it) — that is IA-T4's job, confirmed by the e2e-tester.
FOLLOW-UP found by review (NOT this row's scope, app-wide): a JSON POST with an unparsable or scalar body (`-d 'null'`, `-d '"hi"'`) still returns 500 from the global handler in `backend/src/index.js` — body-parser rejects before any route, so it reproduces identically on `/api/admin/login` and `/api/friends/auth`. Fix = translate `err.type === 'entity.parse.failed'`/`err.status` to 400 in the global error handler, as its own task.2026-08-07 · RD-DS-1 · `task/rd-ds-1` · [PR #41](https://github.com/karolskolar/gorifi/pull/41) · **Design-system foundation landed inert.** `frontend/src/friends-theme.css` is the canonical `theme.css` (224 lines) ported with adaptations **A1–A8 and nothing else** — proven mechanically by stripping the A5 `:where(.app, .modal-layer) ` prefix with sed and diffing against the canon: the residual is exactly A1/A2/A3 (three dropped global rules), A4, A7, A8. 221 lines = 224 − 3 dropped; 153 component rules scoped, and the ONLY unprefixed lines are the five A6 scope roots (`.app,.modal-layer` token block, `.app`, `.app::before`, `.app>*`, `.modal-layer`) — verified in the *minified bundle* too (0 bare `.card{`/`.btn{`/`.h-screen{` across all 42 theme class names, 155 surviving `:where` occurrences). UC-DS-002 renamed the three colliding shadcn vars to `--ui-accent`/`--ui-accent-foreground`/`--ui-border`/`--ui-radius` in both `:root` and `.dark` (`.dark` legitimately declares no `--radius`) with all consumers updated; `var(--accent|--border|--radius)` now has exactly ONE consumer outside `friends-theme.css` — the spec-required `pp.accent` entry from UC-DS-004. **Inertness proven byte-identically, not just visually**: full-page PNGs of `/` and `/admin` at 378 px and 1180 px from a build of the pristine tree vs. the changed tree are MD5-equal on all four. Fonts load (one `css2` stylesheet + exactly three woff2: Darker Grotesque 800, Figtree 400–800, Courier Prime 400/700 — no Anton, no Inter, no italics, no 900) and render nowhere. Reviewer: **approve, zero findings**, one round. Gate re-run by the orchestrator: `vite build` green, e2e **238 passed / 3 skipped** = baseline, **zero e2e spec files edited**. ⚠ Spec correction worth recording: UC-DS-001 justifies the scoping with a *live* `.h-screen` collision "used across admin views", but the repo uses `min-h-screen` everywhere and `h-screen` appears in no template, so Tailwind never emits that utility — the collision is **latent, not live**. The scoping decision stands (it makes the collision structurally impossible for future admin code), but don't re-read that clause as evidence of a present bug. Seams left open exactly as planned: no `class="app"` on any view (each screen row mounts it), no `neo/*` or `snap-tab.js` (RD-DS-2/3/4), `index.html` title/lang/favicon untouched (open product decision), `.pimg` no-photo fallback open (RD-FO-2). Note `pp-*` and `font-display/body/courier` emit zero rules in the current bundle because nothing consumes them yet — correct JIT behaviour, and no safelist was added or may be.


2026-08-13 · IA-T3 · 6cb189e · [PR #73](https://github.com/karolskolar/gorifi/pull/73) · The atomic approve endpoint — the row that actually creates a working login. `POST /api/invitations/:id/approve`, `requireAdmin` PER-ROUTE (the mount stays bare; reviewer confirmed live that `/code/:code` and `/register` are still anonymous, i.e. IA-T2's public flow is intact, and that a friend Bearer session / bogus admin token / anonymous all 401 without touching the invitation). Username = `body.username ?? invitation.username`, lowercased+trimmed, empty ⇒ 400 — so an admin who CLEARS the field gets a 400 rather than a silent fallback to the applicant's request (spec-literal; reviewer judged it the right dialog UX and gave it its own assertion). ⚠ ORDERING: bcrypt + both collision-retry loops + `getPlaceholderCycleId()` run OUTSIDE the transaction — proved empirically with temporary instrumentation, bcrypt+loops 61.8–64.7 ms then the tx 0.13–0.21 ms, ~400× shorter (instrumentation removed, grep-verified). ⚠ ATOMICITY proved by sabotaging WRITE #2 so write #1 had already succeeded: friends count unchanged, invitation still pending, clean Slovak 500 — `dbHelpers.transaction` is a genuine better-sqlite3 transaction, so a throw rolls back. ⚠ The SQLITE_CONSTRAINT→409 translation is REAL, not decorative: deleting the app-level `isUsernameTaken` check left 15/15 green via the UNIQUE index, and the reviewer confirmed the regex matches better-sqlite3's actual `UNIQUE constraint failed: friends.username` while a `friends.uid` collision correctly falls through to 500. The THREE deliberate non-writes (no `friend_subscriptions` — an invited friend starts UNFILTERED, deliberately diverging from onboarding's bakery auto-subscribe; no session mint; no `transactions` row — creation is not a financial event, the GSO-T6 lesson) are asserted as zero rows AND mutation-verified, with a non-vacuity test proving onboarding DOES create `['bakery']` in the same run. ⚠ CREDENTIAL CONTRACT verified end to end by the reviewer: the temp password is UPPERCASE-ONLY (`randomCode(12)`) and `friends.js:35` lowercases only the USERNAME — never the password — so it authenticates unmangled; login returns `mustChangePassword: true`; `password_hash` is a `$2…` digest never containing the plaintext. The plaintext exists in exactly ONE place repo-wide outside schema.js (the 201 body); error paths log only `e.message`. 201 body is hand-picked and pinned by an exact-keys assertion PLUS a raw-text regex for `invite_code|access_token|password_hash`, so a later `SELECT *` fails loudly. 11 mutations verified in total. Gate: `node --check` green; 15 new tests, 47 targeted (verified independently by the orchestrator), full suite 603 passed / 3 skipped. Review: **approve, ZERO findings**. No UI ⇒ e2e-tester skipped (the 15 API tests are the coverage).
⚠ STRUCTURAL RISK with no automated pin (reviewer note, carried into CLAUDE.md): "bcrypt outside the transaction" and "exactly two writes inside" are invariants a refactor can break with the ENTIRE SUITE STILL GREEN — moving `hashPassword` into the `db.transaction` callback is undetectable by any test here. The code comment at `invitations.js:326` is currently the only guard. Same class as the `instances: 1` concurrency caveat.
Residual (correct call, matches the `onboarding.js:235` precedent): on a brand-new empty-cycles DB, `getPlaceholderCycleId()` may INSERT the `_placeholder` cycle and the transaction can still 409/500, leaving that row behind — harmless and idempotent, the next call reuses it.


2026-08-13 · IA-T4 · a6124c2 · [PR #74](https://github.com/karolskolar/gorifi/pull/74) · The approval dialog — "Vytvoriť" now opens a dialog instead of navigating (the `router.push('/admin/friends?…')` is DELETED; the dead receiver in AdminFriends is IA-T5's). Prefills: username from `inv.username` else a slugify of `inv.name` (NFD → strip combining marks → lowercase → whitespace→`.` → drop outside `[a-z0-9._-]` → clamp 30 → **<3 ⇒ empty**), note from `Pozval/a: {inviter_name}`. Success state swaps the dialog body: mono block with login URL + username + temp password and a copy button whose string is the product-owner-signed ty-form message — verified codepoint-identical to the spec across all three sources (132 chars, single U+002D, no NBSP). ⚠ The dialog is the ONLY holder of the plaintext: no auto-close, no timeout (the one `setTimeout` only flips the copy label), the list refresh runs BEHIND the open dialog, and `confirmApprove` early-returns when `approveResult` is set so a double-click/double-Enter mints exactly ONE friend (proved).
⚠⚠ **E2E FOUND TWO REAL BUGS IN THIS ROW'S OWN FEATURE — both fixed and mutation-verified.** (1) **Keyboard-only "Vytvoriť" silently AUTO-APPROVED.** Enter on the focused trigger fires `keydown` on the BUTTON; Radix synchronously focuses `approve-username` during that same keydown; the **keyup half of the same physical press** then hit the input's `@keyup.enter="confirmApprove"` and approved instantly — minting a real account and a one-time temp password **the admin never saw**, i.e. exactly the failure this module exists to fix. Fixed at the mechanism, not with a timing heuristic: both inputs moved to **`@keydown.enter`**, which is structurally incapable of seeing a phantom keyup whose target was the button, plus an **`event.repeat` guard** for HELD Enter (auto-repeat delivers genuine keydowns to the freshly focused input ~500 ms after open — the keydown switch alone would have accepted them). Deliberate Tab→type→Enter still submits, and that is asserted in the same test as the non-vacuity gate. (2) **Browser Back silently destroyed an uncopied temp password** (SPA navigates, component unmounts, plaintext gone, no warning — §UC-IA-006's "unrecoverable by design" means the explicit close, not an accidental navigation). Fixed with `onBeforeRouteLeave` + native `confirm()` (matching this view's existing `confirm()` conventions); covers the header back chevron too. Reload deliberately unguarded. ⚠ SPEC-HARNESS TRAP recorded by the tester: the Back test CANNOT use `page.goto` — backing out of a document-loaded entry is a real document navigation a vue-router guard never sees, so it would pass for the wrong reason; it navigates in-SPA via the dashboard nav instead.
⚠ SPEC-HARNESS FIX in both e2e files: there is exactly ONE admin token app-wide (`admin.js:85` = `INSERT OR REPLACE … 'admin_token'`), so `loginAsAdminUI(page)` INVALIDATES the token minted in `beforeAll`; both helpers now adopt the browser's token via `localStorage.getItem('adminToken')`. Any future spec mixing UI admin login with API `admin()` calls needs this. Verified pre-existing, masks nothing.
Also closed: the three row actions beside the new button — "Spracované" / "Zamietnuť" / "Vymazať" — had **ZERO e2e coverage anywhere in the suite** before this row (grep-verified); they have it now. Deviations accepted by review: the note is ALWAYS sent (trimmed, possibly `''`) rather than omitted-when-empty, so the endpoint's `note ?? existing` branch is unreachable from the dialog — harmless (nothing in `frontend/src` reads `admin_note`, and §UC-IA-006 prefills from `inviter_name` regardless) and WYSIWYG is right for a field on screen; `api.js`'s `request()` does not surface `err.status`, so 400 and 409 render identically inline, distinguished by the server's Slovak sentence — acceptable, both e2e-covered. Empty-note branch NOT tested: **structurally unreachable** — `invitations.invited_by_friend_id` is NOT NULL with an FK and no `ON DELETE`, and `foreign_keys = ON`, so SQLite RESTRICTs deleting an inviter who still has invitations; recorded in a comment with the condition that would make it real. Gate: 30 tests in invitation-approval (14 API + 16 UI), 83 targeted / 610 full suite, 0 failed. Review: approve, 1 minor (a test title that stated the opposite of its body — fixed). E2E: pass + the two bugs above.2026-08-07 · RD-DS-2 · `task/rd-ds-2` · [PR #42](https://github.com/karolskolar/gorifi/pull/42) · **NeoIcon + BrandChrome, still inert.** Three files under `frontend/src/components/neo/`: `NeoIcon.vue`, `icons.js` and `BrandChrome.vue`. All 14 glyphs were verified **mechanically, not by eye** — the probe re-parses `ui.jsx` lines 5–18 at run time and compares the rendered children attribute-by-attribute — and came back byte-identical ×14, same shape order, no added or dropped attributes, with every default size and `stroke-width` matching the UC-DS-007 table (`check` at 3.6, `gear` at 2). Independently re-verified by the reviewer from a separate harness. Neither `ui.jsx` nor `theme.css` carries `stroke-linecap`/`stroke-linejoin`, so the glyphs render with SVG defaults — confirmed against the prototype. Ticker is **static**: `animationName: none` on `.ticker`, its span and `.hazard`, segment present exactly 3× separated by two U+00A0 verified by codepoint (resolved conflict #1 — the README's "marquee" loses to the canonical stylesheet). Fidelity against the live prototype at 378 px is essentially exact: every measured box identical, and a pixel diff of the chrome band shows 0.25 % of pixels differing by >32/255, all text antialiasing, with the **hazard band diffing zero pixels**. ⚠ `ICONS` lives in its own `icons.js` module, NOT inside `<script setup>` — the reviewer caught that a `<script setup>` literal compiles into `setup()` and is re-allocated **per mounted instance** (confirmed in a minified bundle), and an order screen mounts dozens; after the move the map sits ahead of every `setup()` and no glyph path survives inside one. Unknown-name lookup is `Object.prototype.hasOwnProperty.call`-guarded, so `constructor`/`valueOf`/`toString`/`__proto__` render nothing instead of resolving to a truthy function — the same own-property discipline CLAUDE.md's GSO-T3 scar demands, cosmetic here but a house rule. `BrandChrome` sets `inheritAttrs: false` because the `title` **prop shadows the native `title` attribute** on a multi-root template; verified that `id`/`class`/`title`/listeners land on no rendered element and Vue emits no extraneous-attrs warning. Interpretive call, endorsed by the reviewer as settled by the spec itself: **`#titles` FILLS the `.titles` wrapper rather than replacing it** — the theme styles by descent (`.appbar .titles .t`), so UC-DS-006's own wordmark snippet (a bare `<span class="t">`) would render at body size against a replacing slot. Reviewer verdict **approve**, one round, two minors (one applied, one deferred as a spec decision). Gate re-run by the orchestrator after the fix: `vite build` green, e2e **238 passed / 3 skipped**, zero spec edits — no new e2e spec by design (UC-DS-014 item 6 defers primitive smoke to RD-FL-2). ⚠ **Seam raised into RD-FL-3 as a blocker** (see that row): BrandChrome's fixed 3-slot structure cannot express the portal appbar's clickable `.titles` or its pencil-between-titles-and-`.grow`. Not patched here on purpose — UC-DS-006 calls the structure fixed, so it needs a spec amendment. Also worth knowing before module 03: `title` is `default: ''` rather than `required` (Vue cannot express "required unless `#titles` is used", and `required` would warn on every wordmark screen), and `#titles` overrides both `.t` and `.s`, so a wordmark screen inlines its own `<span class="s">` — which is exactly what `portal.jsx` and `guest.jsx` do. Trap for any future isolated harness: `friends-theme.css` depends on Tailwind preflight's `box-sizing:border-box` (A1 dropped the theme's own reset), so without preflight `.hazard` measures 13 px instead of 10 px.


2026-08-13 · IA-T5 · ecdc9d0 · [PR #75](https://github.com/karolskolar/gorifi/pull/75) · **Module 07 closeout** — the relabel that removes the lie the bug report started with. `AdminFriends.vue`: table header + modal label `Prihlasovacie meno` → **`Meno`**, the stale `Zobrazuje sa v prihlasovacom dropdowne` placeholder REMOVED (the dropdown exists only in the retiring legacy mode), the hint → `Toto meno vidí správca a kolegovia.` (byte-identical to module 03's profile help — same field, same truth), and the `?create=1` prefill receiver DELETED by content (`useRoute` dropped as newly-dead; `router` still used at three sites). Governing rule met: `grep -i prihlasovac frontend/src/views/AdminFriends.vue` → EMPTY. ⚠ TWO edits beyond the three literal spec lines, both upheld by review: the `// login name` JS **comment** was the same mislabelling in code form, and the dead `useRoute` import (there is no lint script in `frontend/package.json`, so nothing would have caught it). ⚠ `<TableHead>Prihlásenie</TableHead>` KEPT and this is the nuance the UC needed: it renders `hasCredentials ? (username || 'Nastavené') : '-'`, i.e. it reports REAL credential state in a column distinct from "Meno" and makes no claim about the name field — removing it would delete the only truthful login signal on the screen. New `admin-friends-labels.spec.js` (4 tests, non-vacuity proved twice — reviewer AND implementer each reverted independently). ⚠ Its sweep covers TEXT **and** ATTRIBUTES: the retired claim lived in a `placeholder=`, invisible to `innerText`, so a PARTIAL relabel (header + Label fixed, placeholder left) would pass an innerText-only sweep — verified by partial revert, `listCopy` passes / `modalCopy` fails. A first version of that comment justified the attribute half with a measurably FALSE claim ("innerText-only would have been green against the pre-fix build" — it already fails, the claim was in the header and Label too); corrected, because this repo carries invariants in comments. `portal-profile-modal.spec.js` passes UNMODIFIED (the guard on `FriendPortalSession.vue`, which was NOT touched — module 03 owns its identical label). Gate: 4 new tests; 74 targeted (verified independently by the orchestrator on its own rebuild); full suite **622 passed / 3 skipped, 0 failed**. Review: revise → 1 major + 1 minor, BOTH FIXED: the major was CLAUDE.md's stale 2026-07-07 note (agents cannot edit CLAUDE.md, so the orchestrator did it) — both bullets are now struck with `SUPERSEDED` pointers, since one of them literally read "friendName=Prihlasovacie meno (login)", the exact lie this row exists to delete. E2E: the separate e2e-tester pass was **deliberately skipped for this row** — its user-facing surface is four strings and a deleted route branch, its four tests cover every acceptance criterion, and non-vacuity was independently mutation-proved twice; the two substantive UI rows (IA-T2, IA-T4) did get the full e2e-tester treatment and it found real bugs both times.
⚠ CLAUDE.md gained a **module 07 section** carrying the two UI hazards this module discovered (the `@keyup.enter`-fires-on-dialog-open race, and the route-leave guard a one-time secret needs) and the two e2e harness traps (ONE admin token app-wide, so a UI login invalidates an API `beforeAll` token; and a back-navigation test cannot use `page.goto` or it passes for the wrong reason). `07-invitation-approval.md` gained a §Follow-ups section with five items found and deliberately not fixed: the app-wide unparsable-body 500, the reset-password "Minimálne 4 znaky" placeholder vs the 8-char handler, the `username || 'Nastavené'` credentialed-badge false positive, SMTP delivery, and the legacy shared-password retirement.

2026-08-14 · IA-T6 · 58e82f7 · merged to main · **Mailgun delivery.** `helpers/mailer.js` (global `fetch`, **no new dependency** — Node 20.20.2), no-op returning `skipped:'not_configured'` when any of the three env vars is absent, `AbortSignal.timeout(10s)`, fixed failure vocabulary (`invalid_recipient|timeout|network|HTTP <status>`) — never Mailgun's own text — and a `redact()` layer on both log sites. ⚠ Send happens strictly AFTER the transaction commits: IA-T3's invariants (bcrypt/loops outside, exactly two writes inside) are intact, and network I/O in a synchronous better-sqlite3 transaction would hold the write lock across a round trip. ⚠ A mail failure NEVER fails the approval — reviewer proved the timeout path live: 201 after 10086 ms, friend created, temp-password login works, fake key in neither body nor log.
⚠⚠ **THE MAJOR REVIEW FINDING — the feature would have shipped SILENTLY DEAD.** `deploy.sh` ran `pm2 restart $PM2_APP`, which relaunches from PM2's STORED per-process config and does **not** re-read `ecosystem.config.cjs`; the `pm2 start … --only` fallback only fires when restart fails, i.e. never for a running app. So `node_args` never reached the interpreter, `/var/www/gorifi/.env` was never loaded, the mailer stayed a no-op — and `not_configured` is the ONE outcome the dialog renders as **nothing**, so there was zero signal on any surface. **Measured on the live box before the fix:** `pm2 describe gorifi-staging` → `interpreter args │ N/A`, `pm2 jlist` → `node_args: []`. Fixed by restarting FROM THE FILE (`pm2 restart $REMOTE_PATH/ecosystem.config.cjs --only $PM2_APP`, fallback kept), reproduced empirically on PM2 7.0.3 both ways. ⚠ `--env-file-if-exists`, never `--env-file` (which aborts startup on a missing file, bricking any host without the secrets). A **boot log line** now makes a botched env rollout visible: `[mailer] Mailgun enabled for <domain> via <baseUrl>` / `[mailer] disabled (no …) — outbound mail is a no-op`; the key is never printed, not even a prefix.
⚠ The signed credentials message has ONE home: the backend owns it (`helpers/credentials-message.js`), mails that exact string, and returns it as `credentials_message`; the dialog copies what the server sent, so mail and clipboard are byte-identical BY CONSTRUCTION rather than by two maintained literals. Consequence: the server needs the login URL → `PUBLIC_BASE_URL` → `Origin` **intersected with the shared allowlist** (`config/origins.js`, now the one home, imported by `index.js`) → `https://podpultovka.biz`. ⚠ The Origin is allowlisted, not merely shape-checked, because that URL goes into an email a THIRD PARTY receives from a DKIM-signed brand domain: the vector was already closed by `cors` rejecting foreign origins on the actual request (measured 500, not just preflight), but nothing asserted that cross-file coupling. Reviewer notes the negative half of the new test is vacuous today for that reason — it is written as a property that holds under both layers.
⚠ `invalid_recipient` moved BELOW the config check: order is presence → config → validity. Before, a malformed address on any deployment without Mailgun env (local dev, the e2e recipe) raised the red "sa nepodarilo odoslať" alert although nothing was attempted, contradicting "`not_configured` ⇒ nothing user-facing". The recipient-PRESENCE check stays first — that ordering IS load-bearing for `no_recipient`.
⚠ **MAIL SAFETY IN THE SUITE.** Approval now mails, and the suite is target-agnostic while the documented staging target HOLDS the secrets — so ~20 approving tests would have fired real sends to `@example.*`, hard-bouncing on the brand's only sending domain. `e2e/fixtures.js` is the one home: `TARGET_IS_LOCAL` (parsed **hostname**, never a substring — `https://localhost.evil.example` and `https://real.host/localhost` both read NON-local; an unparseable URL fails towards "do not send"), `fixtureEmail()` (no address off-local ⇒ `no_recipient`, transport untouched), `setMailSafeTarget()` for the stub harness, and `test.skip(!TARGET_IS_LOCAL, …)` where an assertion genuinely needs a real address. `guest-lead-capture.spec.js`'s single approving test takes the SKIP deliberately — two assertions there are load-bearing on the address (the dialog summary and `created.email`), and `fixtureEmail()` would force both to become conditional, weakening exactly what that retarget exists to preserve. ⚠ A `TEMP FORCED` literal `false` on `TARGET_IS_LOCAL` was caught in the tree mid-round (transient debug state, since reverted and replaced with the real derivation) — a hardcoded `false` there removes the send-path coverage on EVERY target with a green suite, so the constant now carries a "never hardcode this" warning naming both failure modes. Verified by the orchestrator: 0 skipped locally, 13 mail-bearing tests actually RAN.
Gate: `node --check` clean on 9 files, `bash -n` on deploy.sh; 92 targeted / 631 full suite, 0 failed. Review: revise → 1 major + 5 minors, ALL fixed (+ the residual). E2E: covered by the row's own specs (server-side transport — `page.route` cannot see it, so the spec spawns its own backend with a fake key against a local `node:http` stub, blanking ambient env first).
2026-08-07 · RD-DS-3 · `task/rd-ds-3` · [PR #43](https://github.com/karolskolar/gorifi/pull/43) · **Form primitives + snapTab + the money helper, still inert.** Four new files: `neo/NeoStepper.vue`, `neo/NeoCheckbox.vue`, `lib/snap-tab.js`, `lib/money.js`. NeoCheckbox reuses RD-DS-2's `NeoIcon name="check"` rather than inlining a second tick path (verified: one copy of `M20 6L9 17l-5-5` in `icons.js`, rendered at stroke-width 3.6). **No `max` in the stepper** — confirmed 250 consecutive "+" taps keep incrementing; stock ceilings stay in the views. ⚠ **Three deliberate deviations from `ui.jsx`/the spec letter, all reviewer-endorsed with measurements.** (1) **The stepper suppresses the emit when the value cannot change** (at `min`, or on a disabled button) where ui.jsx emits unconditionally: `modelValue` is identical either way, but CLAUDE.md's reactivity idiom is `cart.value = {...cart.value}`, so an emit on a no-op tap changes the cart object's identity, wakes the deep `watch`, and fires a spurious debounced PUT **plus an orange "unsaved changes" banner on an untouched cart**. ui.jsx has no auto-save, so its unconditional emit costs nothing there and costs something here. Measured 8 clicks → 6 emits. (2) **JS guards sit on top of `disabled`/`pointer-events:none` and are load-bearing, not belt-and-braces** — the reviewer measured that `dispatchEvent(new MouseEvent('click'))` **does** reach a listener on a `<button disabled>` (while `.click()` does not), and that a disabled `.cbox` computes `pointer-events:auto` because the stylesheet has no `.cbox` disabled rule, making the JS guard its **only** protection. A locked cycle must not be mutable by any path. (3) `fmtEur` is **fail-closed on non-finite input** (`0.00 EUR`, never `NaN EUR`), byte-identical to `v.toFixed(2) + ' EUR'` across 200 000 random finite doubles plus edge values — zero divergences. ⚠ **One deviation was REVERTED on review**: the checkbox briefly used `tabindex="-1"` when `disabled`, reasoning by analogy to `<button disabled>`. But this is a `<span role="checkbox" aria-disabled="true">` — the case where WAI-ARIA APG says to **keep** the element focusable, since `aria-disabled` means "present but unavailable" as opposed to native `disabled`'s "gone". Shipping both got the worst of each: the element announced "unavailable" only to someone who could already reach it. It bites module 05 directly, where `disabled` marks locked/pending hand-over rows and **the disabled state IS the information** — a keyboard/AT user would never have encountered those rows. Now `tabindex="0"` (UC-DS-009's literal text) with the JS guards keeping it inert; verified a disabled+checked box takes a tab stop, reports `aria-checked="true"`, and still emits 0 on click/`.click()`/`dispatchEvent`/Space/Enter. `snapTab` is character-for-character UC-DS-012's body; proven by A/B rather than asserted — `snapTab` left `window.scrollY` at 0 while `scrollIntoView()` on the same page yanked it to 1441. `fmtEur` went in a **new `lib/money.js`** rather than reusing `guest-cart.js`'s identical-output `formatPrice`, because that module's charter is guest-cart primitives and importing it for a friend balance card would silently promote it to a shared money module; `money.js` carries an in-repo pointer naming the duplicate and RD-GX-1 as its resolver. Reviewer verdict **approve**, one round, three minors (two applied, one handed to RD-GX-1 — see that row). Gate re-run by the orchestrator after the fixes: `vite build` green, e2e **238 passed / 3 skipped**, zero spec edits; no new e2e spec by design (UC-DS-014 item 6 defers stepper/snapTab smoke to RD-FO-1/2). ⚠ **Operator note — a mid-run 236/3 was an orchestrator error, not a regression**: a server from the previous gate run still held :3997, so the new one died `EADDRINUSE` and the suite ran against a process whose `backend/public` had been `rm -rf`'d and rebuilt underneath it. That is exactly the `e2e/README.md` gotcha ("start a fresh server"); killing it and re-running gave a clean 238/3. Always confirm the port is free before seeding. Two things modules 04/06 must not trip over: `min > modelValue` makes a "−" tap **increase** the value (`Math.max(2,0)` → 2 — verbatim ui.jsx, so don't pass a `min` above the current quantity when clamping a cart down), and `modelValue` is `required` with no coercion, so a hand-rolled `cart[key]` binding that can be `undefined` renders an empty `.val` and makes "+" emit `NaN` (`guest-cart.js quantityIn()` already returns `|| 0`). Also unresolved for module 04: the prototype itself uses `.mono` for a body string (`<div class="mono">slivka, para orech, karamel</div>`), contradicting UC-DS-012's "mono is never used for running body text".

2026-08-07 · RD-DS-4 · `task/rd-ds-4` · [PR #44](https://github.com/karolskolar/gorifi/pull/44) · **Overlay primitives — module 02 is now COMPLETE and still inert.** `neo/NeoModal.vue` + `neo/NeoCopyRow.vue`. Plain `<Teleport to="body">`, **not radix** (`ui/dialog` stays admin-only per UC-DS-004 rule 4) — verified the layer's parent is `<body>`, `closest('.app')` is null, and the tokens still resolve there: `.btn.accent` inside the modal computes `rgb(255,45,135)`. That is the whole reason RD-DS-1's token block names `.app, .modal-layer`; **never optimise the teleport away**. ⚠ **Scroll-lock restore is COUNTED, not blanked** — a module-scope `openLocks` + `savedBodyOverflow` pair: only the *first* lock records the baseline (an inner one would record the `hidden` we just wrote and restore it forever) and only the *last* unlock restores. Pinned from both sides: `body.style.overflow='scroll'` → `hidden` → back to `'scroll'` (a naive `= ''` gives `''`), and an overlapping second modal's unmount leaves the page **still locked**. Survives a same-tick A-off/B-on swap and 20 rapid cycles. A per-instance `didLock` flag was added on review so lock/unlock cannot desynchronise even if `onBeforeUnmount` ever runs without `onMounted` (Vue skips a queued `mounted` on an already-unmounted instance while `bum` hooks run unguarded — real in principle, undrivable in practice, worst case one spurious unlock). ⚠ **All three close paths route through ONE JS guard**, per RD-DS-3's lesson that CSS and the `disabled` attribute don't stop `dispatchEvent` — verified `closable:false` is dead against a real scrim click, `scrim.click()`, a synthetic bubbling `MouseEvent` from a deep child, `keyboard.press('Escape')` and a synthetic `KeyboardEvent`; and that flipping `closable` false→true live re-enables Esc, so the flag is read at event time rather than captured. Focus enters the modal, returns to the opener (captured in `setup()`, restored *before* the node leaves the DOM — removing a focused element would reset `activeElement` to `<body>`), guards on `opener.isConnected`, and leaves no ring (`outline-style:none`). Unmount-while-open releases listener + lock + focus **without `@close` ever firing** — the parent-gated `v-if` model (UC-DS-010 lists no `open` prop) makes a mid-flight route change safe by construction. `NeoCopyRow` flips for 2000 ms (measured 2013 ms), **restarts** the timer on re-click rather than leaking it like the prototype, and flips **even when `writeText` rejects, throws synchronously, or `navigator.clipboard` is undefined** — a clipboard failure must never strand the UI. Copies the exact untruncated value while `.val` ellipsizes. `type="button"` added (the prototype omits it) so a copy row inside module 03's invite `<form>` can't submit it. ⚠ On review `NeoModal` gained **`v-bind="$attrs"` on `.modal`, bound FIRST**: `mergeProps` merges `class`/`style` and chains handlers but is **last-wins for everything else**, so attrs-first means a consumer's `class` composes with `.modal` while a stray `role`/`aria-modal`/`tabindex` **cannot clobber** the dialog semantics RD-KG-2's race-guard spec depends on. Without this a consumer's `data-testid` vanished silently (Vue drops it either way — `inheritAttrs` only changes whether you get warned). Reviewer verdict **approve**, one round, three minors (two applied, two findings raised as seams). Gate re-run by the orchestrator after the fixes: `vite build` green, e2e **238 passed / 3 skipped**, zero spec edits; no new e2e spec by design (UC-DS-014 item 6 defers modal/copy-row smoke to RD-FL-2/RD-FL-7). ⚠ **Two seams raised, neither patched here:** the **focus trap** (recorded as a blocker on RD-FL-2 — porting the radix forced-password gate to a trap-less `NeoModal` would make Tab a gate bypass) and the **scrim drag-close** (recorded as an open spec amendment above — needs a UC-DS-010 change, affects every form-bearing modal). Harness traps worth reusing: `overflow:hidden` on `<body>` does **not** block programmatic `window.scrollBy` (only `overflow:clip` does) — drive a real `page.mouse.wheel`; and the prototype must be served over HTTP, since `file://` makes Babel's XHR of the `.jsx` files fail CORS.

2026-08-07 · RD-FL-1 · `task/rd-fl-1` · [PR #45](https://github.com/karolskolar/gorifi/pull/45) · **The design system switches ON — first row that changes a rendered pixel.** `FriendPortal.vue` only (+63/−33): `class="app"` on the root, `BrandChrome` in all three states, loading/error restyle, page column moved to `mx-auto w-full max-w-[760px] px-4 sm:px-7`. **Templates only** — the extracted `<script setup>` block diffs `14a15`, i.e. one added import and nothing else, so every named function, computed, the `gorifi_friend_auth` shape, the `expiresAt` check, `window.scrollTo(0,0)` and the fire-and-forget hydrate are byte-identical. Legacy login (UC-FL-003) is untouched — the diff doesn't reach lines 864–986 at all — which matters because **the seed provisions legacy mode, so that's the branch the suite actually exercises**. Admin invariance proved by MD5: `/admin` login, dashboard and friends are byte-identical full-page PNGs against a build of the parent commit, with no class-set differences on any element. ⚠⚠ **THE FINDING OF THIS ROW, now in CLAUDE.md as its own section: `.app>*{position:relative;z-index:1}` neutralises EVERY Tailwind positioning utility on a direct child** — `fixed`, `absolute`, `sticky`, even `relative z-10` (z-index silently clamped to 1). Same `(0,1,0)` specificity, and `friends-theme.css` is imported after `style.css`, so it wins; the theme's own `.cartbar`/`.cat-tabs`/`.modal-layer` survive only because they sit *later in the same stylesheet*. Nested one level, the utilities are fine. **It fails silently — no build error, no failing spec.** The voucher overlay is a hand-rolled `div.fixed.inset-0.z-50` that *was* a direct child and would have collapsed into page flow, breaking UC-FL-001's "visually untouched" pin; fixed with `<Teleport to="body">`. The reviewer reproduced the hazard live and confirmed the teleport is not merely *a* fix but the *right* one: the counterfactual (wrap in a plain div, stay inside `.app`) restores `fixed` but makes the modal inherit `var(--font-body)` — measured `Figtree` vs today's `system-ui` — so "render inside `.app`" taken literally breaks the very requirement it accompanies. Every remaining screen row inherits this: a hand-rolled fixed overlay under `.app` must **teleport out** (when its subtree uses no theme tokens) or be **rebuilt on `NeoModal`/`.modal-layer`** (when it wants them). Radix-portaled dialogs and `NeoModal` are immune. Interim authenticated appbar: `#titles` holds plain `.t`/`.s` (name/uid) and `#trailing` carries the three existing shadcn ghost controls **verbatim** — same SVGs, `title` attrs and handlers — so nothing is lost and none of RD-FL-3's four owned elements (`chip acc`, `NeoIcon` glyphs, the `.titles` click handler, the pencil between `.titles` and `.grow`) is pre-empted. Reviewer verdict **approve**, one round, three minors: the landmine documentation (done here, in CLAUDE.md) plus two residuals handed to RD-FL-3 (see that row) — the 68 px banner overhang and the fact that `error` currently surfaces nowhere for an authenticated friend. Gate: `vite build` green, e2e **238 passed / 3 skipped**, **zero spec edits** — UC-FL-001's own acceptance criterion. ⚠ Operator note: the first review agent was killed mid-run by a session limit and produced no findings; it was re-spawned from scratch. A partially-completed review is not a review — don't salvage one.

2026-08-07 · RD-FL-2 · `task/rd-fl-2` · [PR #46](https://github.com/karolskolar/gorifi/pull/46) · **Modern login + the forced-password gate on NeoModal + the effort's FIRST e2e additions.** Suite goes **238 → 250 passed / 3 skipped** (12 new in `e2e/tests/modern-login.spec.js`), zero existing specs edited. ⚠⚠ **`h-screen` turned out to be a LIVE collision, not the latent one RD-DS-1's review concluded** — Tailwind's JIT emits a utility the moment its name appears in **scanned source**, so writing UC-FL-002's mandated `<h1 class="h-screen">` generated `.h-screen{height:100vh}` alongside `:where(.app,.modal-layer) .h-screen`: same specificity, **different property**, so BOTH applied and the login headline became a full viewport tall (measured 855 px gap at 378 px). Fixed centrally with `blocklist: ['h-screen']` in `tailwind.config.js` — the only instrument that scales, since renaming is impossible (the spec pins the class verbatim), inline `height:auto` would have to be repeated on every heading in 03–06 with one omission invisible until someone opens that screen, and a specificity bump can't win against a different property. `min-h-screen` is a **separate candidate** and still emitted (all 19 admin/guest views use it); the parent build never emitted the bare utility, so nothing pre-existing was removed. ⚠ **Consequence: bare `h-screen` outside `.app` now silently resolves to nothing** — use `min-h-screen` or `h-[100vh]`. ⚠ **THE BLOCKER IS CLOSED: `NeoModal` now has a focus trap** (`trapFocus: {type: Boolean, default: null}`, `trapping = trapFocus === null ? !closable : trapFocus`, so non-dismissable gates trap by default and ordinary dialogs are unchanged unless they opt in — `null` rather than `false` because Vue's Boolean casting must keep "absent" distinguishable from "explicitly off"). Tab is intercepted in the **existing** document `keydown` listener — no new listener, no new lifecycle — and the branch `return`s before the Esc path, so Esc semantics are untouched. Proven by temporary revert: without it, `Tab #3 escaped the gate → [...,"BODY"]`. ⚠ **The first implementation was REJECTED on review and rewritten**: it let the browser move focus and only intervened at the edges, which escaped four ways — a `<summary>` tail (focusable, missing from `FOCUSABLE`), an `[inert]` tail (counted by us, skipped by the browser), a `visibility:hidden` tail, and a **positive `tabindex`** reordering the real sequence. It also carried a **false comment** claiming `getClientRects()` covers `visibility:hidden` — it does not; such elements still generate boxes. `trapTab()` now **always** `preventDefault()`s and moves focus itself by index (`items[(idx ± 1 + n) % n]`), so containment no longer depends on the browser agreeing with our list; worst case is a wrong element *inside* the dialog. `focusFirstThatTakes()` is **load-bearing, and more so than its comment says**: without it a candidate that refuses focus parks focus on its predecessor forever (the next Tab recomputes the same index), and beyond `<details>` it is what skips a `<button>` inside `<fieldset disabled>` — which passes every filter yet Chromium refuses. It is hard-bounded (`step <= n`) and falls back to the container. Second-pass review re-ran all four vectors plus empty/single lists, focus-outside-on-arrival, 140 rapid presses, focus handlers that redirect, and an all-refusing list: **all contained**. This matters because `NeoModal` is the shell every 03–06 dialog composes on and `<details>` already ships in `FriendOrder.vue`/`GuestOrder.vue`/`GuestProductGrid.vue`. ⚠ **The new spec is HERMETIC — it must stay the idiom for every future modern-mode spec.** The first version flipped global `auth_mode` in `beforeAll`/`afterAll`; that was rejected because `fullyParallel: false` does **not** serialise *files* and no `workers` is pinned (so `cpus/2` — on a ≥4-core host a concurrent `public-flow`/`friend-login-list` sees the modern card and loses its `friend_sessions`, deleted once per flip), and a worker death before `afterAll` strands `auth_mode='modern'` so every later legacy spec fails misleadingly. Now a `beforeEach` stubs the one endpoint the view reads (`page.route('**/friends/auth-mode', …)`, which outlives navigation) and `afterAll` **asserts** the server still reports `legacy` rather than restoring it. Verified not vacuous: `auth_mode` appears nowhere in `backend/src/routes/friends.js`, so the personal-login branch is genuinely ungated — auth, the gate, `change-password` and the cycle list all hit the real backend. This also discharges 02 §UC-DS-014 item 6's deferred smoke for **BrandChrome, NeoIcon, NeoCheckbox and NeoModal** (`NeoCopyRow` stays RD-FL-6's). Legacy branch byte-identical — the reviewer diffed old 864–986 against new 978–1100 and got **empty output**; the modern branch is a new *preceding* `v-else-if`, and the gate is a standalone sibling, not spliced into a chain (the GSO-T6 trap). Admin invariance: five admin pages byte-for-byte identical PNGs against a parent-commit build **served on the same port** — a first pass on a different port showed `/admin/settings` differing by 99 bytes because that page renders `window.location.origin`. Reviewer verdict **approve** after one revise round. ⚠ **THEME-LEVEL FOLLOW-UP, schedule before RD-FL-3 lands:** every `.inp` is **52.5 px** vs the prototype's 48 and every `.btn` **47** vs 44 — Tailwind preflight's `button,input,…{line-height:inherit}` gives 1.5 where the prototype gets `normal` (~1.2). Arithmetic confirmed to the pixel by the reviewer. It is a systematic inflation on **every field and button in modules 03–06**, inherited from RD-DS-1's port, and until a one-rule amendment lands in `friends-theme.css` (`:where(.app,.modal-layer) .inp, … .btn { line-height: normal }`) every "match to the pixel" comparison starts from a known miss and each row re-litigates it. Left `forced-change-ui.spec.js` at `test.fixme` (un-fixme-ing would edit an existing spec, and its coverage is now redundant); whoever retires it must fix **two** things together — the stale `/resetoval vaše heslo/` regex (per UC-FL-012 the *regex* moves, not the copy) and `dialog.getByLabel('Nové heslo')`, now ambiguous under strict mode against "Potvrdiť nové heslo" (use `/^nové heslo$/i`, as the new spec already does).

2026-08-07 · RD-DS-5 · `task/rd-ds-5` · [PR #47](https://github.com/karolskolar/gorifi/pull/47) · **Two module-02 amendments module 03 was blocked on** — inserted as a row rather than done per-screen, because both touch **shared** design-system files and would otherwise be re-litigated in every remaining row. Suite **250 → 253 passed / 3 skipped** (`e2e/tests/neo-control-metrics.spec.js`, 3 tests), zero existing specs edited. **(A) `BrandChrome` gained `#after-titles` + a titles-click affordance**, closing the RD-FL-3 blocker: UC-DS-006 had declared the structure fixed at three slots, but `portal.jsx:97` puts `onClick` on the component-owned `.titles` div and `:101` puts the pencil *between* `.titles` and `.grow` — where `#trailing` (after the spacer) flings it to the right edge and `#titles` (a column flex) stacks it under the name. ⚠ **`titlesAction` is a label-carrying `String`, not a Boolean**: `.titles` holds the friend's name and code, so a bare `role="button"` announces "LEGO lego-4821, button" — the person's name, never the action's. Folding "is it interactive" and "what does it announce" into one prop makes an unlabelled interactive titles block **impossible to ship** rather than merely discouraged. `titlesClass` was rejected because a class can paint `cursor:pointer` but cannot attach a handler, so look and behaviour would become separable. Opt-in is **structural**: attributes *and* listeners are one `v-bind` object that is `{}` when inactive — a static `@click` would always register a handler even as a no-op, and that is what makes the no-regression proof possible. Verified independently by the reviewer across 8 prop/slot combinations plus a live-browser comparison: every element, attribute, class, text node and computed style identical, the **only** delta being the unused slot's fragment anchors (two empty text nodes, exactly what `#leading`/`#trailing` already emit), which form no anonymous flex item — `.titles` ends at 170, `.grow` starts at 184, one 14 px gap and nothing more. ⚠ The UC-DS-006 acceptance criterion was **corrected on review** to state that caveat: it first said "markup identical", which a strict whole-DOM comparison fails on the anchors while nothing is wrong — inviting a false regression report or a "fix" that removes the slot. **(B) Adaptation A9** — `:where(.app, .modal-layer) .inp, … .btn{line-height:normal}`. Tailwind preflight's `button,input,…{line-height:inherit}` gave 1.5 where the canon computes `normal`, inflating **every** field by 4.5 px and **every** button by 3 px. Now `.inp` 48 / `.btn` 44 / `.btn.sm` 38, matching the prototype exactly (reviewer re-derived the constants from the prototype served over HTTP, not from the build, so the assertions aren't tautological; deleting A9 from the CSSOM at runtime restores 52.5/47/41.5). ⚠ **Recorded as A9 in §UC-DS-001's exhaustive adaptation list** — the file is a byte-for-byte port and that section says "any other deviation is a fidelity bug", so an unrecorded addition reads as corruption to the next person diffing it. Scoped to the two **classes**, not to `button,input,…` inside `.app`: a broad element rule would restyle the shadcn children still rendering inside a half-migrated `.app` (UC-DS-002's transitional guarantee) — confirmed it would have hit `FriendPortal.vue:802-835`. `:where()`'s zero specificity leaves every compound variant's cascade untouched. Bare `.btn.ghost` renders **43** — not an invented number: the reviewer measured the canon computing exactly that (14px at `normal` + 20 padding + 6 border), and it clears UC-DS-005's 38 minimum. All hit-target minima still met; nothing had to be traded between the two specs.

2026-08-08 · RD-FL-3 · `task/rd-fl-3` · [PR #48](https://github.com/karolskolar/gorifi/pull/48) · **Authenticated appbar + balance card, and the first cross-session leak.** Suite **253 → 272 passed / 3 skipped** (`e2e/tests/portal-appbar.spec.js`, 19 tests), zero existing specs edited. The appbar is the first consumer of RD-DS-5's amendment: pencil in `#after-titles` (DOM order asserted — `.titles`→pencil→`.grow`, and geometrically `pencilLeft − titlesRight < 40px` while `chipLeft > pencilLeft + 40`, i.e. it hugs the name instead of being flung to the trailing edge, which was the whole point), `.titles` interactive via `titlesAction`. `FriendBalanceCard` stops importing `BalanceBadge` — which is **shared with admin** and stayed untouched, verified structurally (absent from the diff, and `FriendBalanceCard` is imported only by `FriendPortal`) *and* empirically (MD5-identical `/admin/friends`, `/admin/friends/1`, `/admin` against a parent-commit build **served on the same port**; `/admin/friends` renders `BalanceBadge` on 10 rows, so the comparison bites). ⚠⚠ **THE FINDING OF THIS ROW — `switchUser()` did not clear session-scoped display state, so one person's data rendered in the NEXT person's session.** The legacy dropdown lets anyone pick any friend, so on a shared device that is a *different human*. Reproduced end to end through the legacy dropdown with a different friend: fail A's profile save → banner → logout → log in as **B** → A's error banner still on screen. Three refs were affected and all three are now cleared: `error` (found first), plus `voucherResolved` — which **names money** ("Kredit 4.20 € pridaný") and whose own 5s timeout is set at *resolve* time, so it rendered on the **login screen** for whoever came next — and `subscriptions`, the worst of the three despite being invisible: `loadCycles`'s subscription GET is a **silent catch**, so if the next friend's GET fails the modal prefills the **previous** friend's preferences and "Uložiť" writes them onto the new account — a cross-account *write*, not a stale read. **The rule this row establishes: session-scoped display state dies with the session.** Every later row adding a ref that outlives a request must ask whether `switchUser()` should clear it. Note the pre-existing hazard was merely *invisible* before RD-FL-3 added the banner; surfacing it is what made clearing it this row's job. ⚠ **A second finding was an over-implementation of mine, corrected on review:** the pencil was given `role`/`tabindex`/`aria-label`, making it a **second button with the identical accessible name and action** adjacent to `.titles` — two consecutive "Upraviť profil, button" stops on a four-control bar, and a spec assertion of `toHaveCount(2)` that would have frozen the ambiguity into a contract. Not forced by either spec: §UC-FL-004 asks for `title` only and `portal.jsx:101` is a bare span, while §UC-DS-006's zero-pixel-ARIA clause is written about controls that are the **only** route to their action — the pencil is not. Now `aria-hidden="true"` + a testid, keeping `@click`/`title`; one tab stop, pointer behaviour unchanged. Also closed RD-FL-1's two residuals: the voucher banner moved onto the shared 760 px geometry, and the authenticated `error` banner now exists at all (previously **no** failure of `resolveVoucher`/`saveProfile`/`saveSubscriptions`/`openInviteModal` reached any UI). ⚠ But `resolveVoucher` is the one writer that banner can **never** serve — the voucher modal is `fixed inset-0 z-50 bg-black/70` with no dismiss and stays open on failure, so the message sits behind the scrim; whichever row next touches that modal owns its in-modal error surface. Reviewer verdict **approve** after one revise round (two majors, both mutation-pinned) plus a second-pass minor applied here. Two OPENs remain open, used as recorded defaults and test-pinned: positive-balance green mono, and the transactions modal left as untouched shadcn. Notes for RD-FL-4..7: `getByRole('button', {name:'Upraviť profil'})` still collides with the profile modal's `DialogTitle`, so pick locators accordingly; the pencil (16 px) and logout (18 px) glyphs meet the prototype but **not** WCAG 2.5.8's 24 px, and RD-FL-4's gear has the same shape; `e2e/README.md`'s friends-list section was corrected (the dropdown reads the **public** `/friends/login-list`, restore hydrates via the **owner-gated** `/friends/:id/profile`, `api.getFriends()` has one caller left) — the `active=true` stub several specs still carry is **dead**, don't copy it forward.

2026-08-08 · RD-FL-4 · `task/rd-fl-4` · [PR #49](https://github.com/karolskolar/gorifi/pull/49) · **Cycle list — the densest pin surface in module 03.** Suite **272 → 290 passed / 3 skipped** (`e2e/tests/portal-cycles.spec.js`, 18 tests), zero existing specs edited. All three pins verified live rather than argued: the `<h2>` accessible name concatenates across the `.hl` span; `cardFor()` resolves to exactly **1** for each of six active-card states and **0** for archive rows; the `<h3>`'s `textContent` strictly equals the cycle name with zero inner spans. ⚠ **Found while writing the tests: the balance card ALSO carries a literal `p-4`** (`p-4 sm:p-5`, from RD-FL-3). Harmless only because `cardFor()`'s `has:` filter excludes it (it contains no heading at all) — but it is why **the page column must never carry `p-4`**, which is now written into §UC-FL-006's Page-column paragraph where the next author reads it *before* writing markup. The archive rows deliberately use a plain bold `div` for the name so they cannot answer the locator. ⚠ **The backlog row for this task was WRONG and the conflict is recorded above** — "ships WITHOUT the share row" is unimplementable against an uneditable spec. ⚠ **A major caught on review: the plan block could scroll the whole page sideways.** `plan_note` is *free admin text*, so one pasted Google Sheets URL gave **531 px scrollWidth against a 320 px viewport — 211 px of horizontal overflow**, the exact production failure `mobile-no-h-overflow.spec.js` exists for. Not a regression (the old `whitespace-pre-line` div had the same hole, and neither `.card` nor the column clips), but this row rewrote that element, added `overflow-wrap:anywhere` to **both name blocks**, and then wrote a geometry test that would have certified the invariant forever while the sibling block stayed open. Fixed, and the fixture now *contains* an 80-char URL so the test actually exercises it: **320/320, zero overflow**, reproduced red-then-green. ⚠ **Second review finding — a spec that would have forced the next row to edit it:** the adjacency assertion pinned `lastChild.tagName === 'BUTTON'`, but UC-FL-007's share row is a **`div` wrapping** the button, so RD-FL-5's in-place replacement would have broken a spec RD-FL-4 had just created. Now structure-agnostic. ⚠ The implementer correctly **rejected the reviewer's literal fix** (`[aria-label="Zdieľať s kolegami"]`), which matches *neither* control: today's transitional button takes its name from visible text and has no `aria-label`, while UC-FL-007's future button has the `aria-label` but visible text just "Zdieľať" — the selector would have been vacuously false now and passed only after RD-FL-5. Matching by accessible name from **either** source is the fix. Behavioural nuance, reviewer-confirmed safe: money goes through `fmtEur`, so a submitted order totalling zero now renders `0.00 EUR` where `formatPrice` rendered `-` — strictly better, since `-` conflated "zero" with "absent" and both renders are gated on `hasOrder`, which the backend sets only for a submitted row. `showArchive` joins the session-scoped refs cleared by `switchUser()` (UC-FL-008 pins the fold default-closed, and a surviving component instance is the only path by which the next person lands on it open). Reviewer verdict **approve** after one revise round. Spec edits made here: §UC-FL-006 gains the `p-4` prohibition (written as "express horizontal padding with `px-*`; the all-sides `p-4` shorthand is banned; `py-6` is fine" — the brief's "horizontal padding only" would have been *false*, since the column does carry vertical padding), and §UC-FL-008's self-contradictory archive-badge parenthetical is replaced with what `portal.jsx:139` actually does (a plain `.badge` for **both** types). ⚠ **Operator note that costs a green-looking run:** `DB_PATH` must be exported to **both** the server *and* the Playwright process. Setting it only on the server makes the two direct-DB-write tests self-skip and the suite reads **288 passed / 5 skipped** — a *green* run two tests short, which is the dangerous shape of this mistake. Notes for RD-FL-6/7: `currentVoucher`/`pendingVouchers`/`showVoucherModal` still survive `switchUser()` (only `voucherResolved` is cleared) — unreachable today because the voucher scrim covers the logout control, but whichever row gives that modal a dismiss control makes it reachable. And the cycle card root is a bare `div` with `@click` and no keyboard route — identical to the pre-redesign shadcn `Card`, so no regression, and neither spec asks for one; flagged so it stays a decision rather than an oversight.

2026-08-08 · RD-FL-5 · `task/rd-fl-5` · [PR #50](https://github.com/karolskolar/gorifi/pull/50) · **Share row + colleague count — module 03's ONLY script change.** Suite **290 → 301 passed / 3 skipped** (`e2e/tests/portal-share-row.spec.js`, 11 tests), zero existing specs edited, `GuestShareDialog.vue` untouched. RD-FL-4's transitional shadcn button was **replaced in place**, and RD-FL-4's deliberately structure-agnostic adjacency assertion survived **without an edit** — the reviewer confirmed it is still meaningful rather than accidentally vacuous (it now matches via the `aria-label` branch on a descendant of the last child). ⚠ **Both guards on the new fetch were mutation-tested by the implementer AND independently re-mutated by the reviewer**, who confirmed they are **complementary, not redundant**: `guestCounts.value = {}` in `switchUser()` handles data that already landed, `++guestCountSeq` (bumped at the top of `loadCycles`, before any await) handles data still in flight — neither assumes the other. Deleting the seq guard fails exactly the two deferred tests (past-logout, past-second-`loadCycles`); deleting the clear fails exactly the no-reload logout test, which shows the **previous friend's count on screen**. The reviewer could construct neither a stale write nor a wrongly-dropped fresh response: the guard sits after the only `await` with no `await` between it and the write, and `loadCycles` can only throw before the batch fires. This matters more here than in RD-FL-3/4 — a stale value is **another friend's colleague data**, not merely stale UI. The fetch hangs off `loadCycles`'s tail rather than its four call sites, so the restore paths, both logins and the subscription refetch are all covered and the seq bump has one home. ⚠ **`guestCounts` is a MERGE map that is never reset per batch, so a refetch that FAILS leaves the previous count standing** rather than falling back to the "Objednávate aj pre kolegov?" copy. Reproduced by the reviewer (seed 5 → flip the stub to 500 → save subscriptions → badge still reads 5). Kept as-is — showing last-known-good is the better UX and it is **not** a cross-session leak, since `switchUser()` is the only authenticated→login transition (the other seven `authState='login'` sites are all in `onMounted`-only `loadInitialData` plus one dead branch) — but the in-code comment was corrected, because it claimed a self-healing that only holds on the success path. Zero counts are **written, not skipped** (a decrementing count must be able to clear without a logout), with `Number.isFinite` landing a malformed payload on 0 rather than `NaN`; the badge is suppressed at 0, matching `portal.jsx:76`'s `c.guestCount ? … : null`. ⚠ **`screenshots/02-shot.png` CANNOT verify this row — it crops mid-card immediately after the plan block** and never reaches the badge row or the share row. Verified against `friends/portal.jsx:73-81` and the live prototype over HTTP instead, reading computed values; the reviewer judged that superior to eyeballing a raster. The file's §Design-reference line now carries that caveat, and the test title no longer names the shot. Reviewer verdict **approve**, one round, two minors (both applied here). ⚠ **Seams for RD-KG-2:** `portal-share-row.spec.js:305-318` now pins from the **portal side** that the dialog has `role="dialog"`, **contains the shared cycle's name as text**, does **not** contain the other open cycle's name, closes on Escape to `toHaveCount(0)`, and re-targets as a single instance — moving the cycle name into a `title`/`aria-label`, truncating it, or adding a cycle picker breaks it. **For RD-FL-6..8:** `switchUser()` is currently the only session-ending path, which is what makes one clearing site sufficient; any new one (a global 401 handler — `api.js` has none today — or a session-expiry redirect) must clear `guestCounts` **and** bump `guestCountSeq`, since doing only one reopens the leak. The share row must stay the card's **last** child (two specs fail otherwise), and `data-testid="share-row"` is now load-bearing in six assertions. Recorded for the spec owner, not a defect here: **"N kolegovia" is grammatically wrong in Slovak for N≥5** ("kolegov"), but `portal.jsx:77` writes the single form and 01-architecture §i18n makes prototype copy final — inventing pluralization was correctly declined.

2026-08-08 · RD-FL-6 · `task/rd-fl-6` · [PR #51](https://github.com/karolskolar/gorifi/pull/51) · **Profile modal on `NeoModal`, and TWO plaintext-password leaks.** Suite **301 → 324 passed / 3 skipped** (`e2e/tests/portal-profile-modal.spec.js`, 23 tests), zero existing specs edited. Two revise rounds. ⚠⚠ **THE FINDING: friend B's credential-setup dialog auto-opened PRE-FILLED WITH FRIEND A'S PLAINTEXT PASSWORD.** `setupUsername`/`setupPassword`/`setupPasswordConfirm`/`setupError`/`showCredentialSetup` all survived `switchUser()`, and `authenticate()` **re-raises** `showCredentialSetup` for the next friend — so the dialog rendered itself, with **no action by B**, and "Nastaviť" would have written A's password onto B's account. Reproduced end to end (transition mode, one instance, no reload): `password field = "LEAKED-SECRET-1"`. Strictly worse than the `changeCurrentPassword` case this row set out to fix, which at least needed the next person to open a modal. Also cleared: `usernameAvailable`/`usernameChecking` **and `clearTimeout(usernameCheckTimeout)`** — `checkUsernameAvailability` has no guard at all, so its 400 ms callback lands *after* the clear and writes "Užívateľské meno je voľné" under B's empty field. A second, milder instance found on the next pass: **`showLoginPassword`** left the next person's own password field rendering as `type="text"` — it exposes no prior secret, but it inverts a security default across a session boundary on a shared device. ⚠ **This is the SIXTH consecutive row to find something surviving `switchUser()`, and the sixth miss was found by a reviewer's sweep, not by reviewing the ref list — so reviewing the list has demonstrably stopped working.** A four-item structural plan is now recorded above the RD-FL-8 row; items 2 and 3 go to RD-FL-7, item 4 (extract `FriendPortalSession.vue` with `:key="friendId"`, so logout destroys the instance and **there is no list to forget**) to RD-FL-8. A lint rule was explicitly rejected — no ESLint exists, and the rule still couldn't tell a session ref from a device preference. **Discharged RD-DS-4's open spec amendment**: the profile modal is the first *closable* form-bearing modal, so the scrim **drag-close** became live here. A press inside `.m-body` released over the scrim fires `click` on the nearest common ancestor — the scrim — so `@click.self` matched and the half-filled form was lost; instrumented and confirmed the pre-fix code *would* have closed. Now the mousedown origin must also be the scrim. ⚠ Three refinements the reviewer forced, all reproduced: the origin listener runs in the **capture phase** (a descendant's `stopPropagation` on `mousedown` otherwise leaves the flag latched and **re-opens the whole hazard** — this is the shared shell modules 04–06 fill with checkout, pickup, payment and third-party content); the flag is **primary-button-only** (a right- or middle-click sets it and produces **no `click`** to consume it, so a later programmatic click closed the modal); and `openProfileModal()` clears `error`, since a message left by another writer rendered inside the modal as if the profile save had failed, with no `Chyba:` prefix and no dismiss ×. ⚠ **A hypothesis was RETIRED with evidence rather than guarded against:** the reviewer suspected a classic scrollbar drag on `.modal-scrim` (`overflow-y:auto`) would close the modal, and neither party could reproduce it — because **Playwright launches headless Chromium with `--hide-scrollbars`**. With `ignoreDefaultArgs: ['--hide-scrollbars']` at 420×300 a real 15 px gutter appears, and thumb drag, track click, thumb click and arrow click each deliver `mousedown` + `mouseup` on the scrim and **no `click` at all**. An `offsetX` guard would have been dead code. Reviewer-endorsed judgement calls: **one scrim handler instead of the recorded two** (the two-handler form depends on listener registration order); **consume-on-close**; the **`label id` + `aria-labelledby` inversion** for the two read-only `div`s (`<label for>` only associates with labelable elements, so a plain `for` would have bound to nothing); and the page-banner **suppression** while the modal is open (the three non-voucher `error` writers each need their own modal open and the voucher overlay sits on top of everything, so two surfaces at once is unreachable). ⚠ Also fixed a genuine flake with a diagnosis worth keeping: the credential test passed alone and failed in a full run because `loadGuestCounts` `Promise.all`s one request per open cycle — **135** in a full-suite DB — so two logins in one page queue ~270 XHRs behind the browser's 6-connection limit and starve the second login's own calls ("Overujem…" forever). Muted in the two two-login tests; the underlying unbounded fan-out is handed to RD-FL-8, since cycles are never auto-closed and it degrades silently with age.

2026-08-08 · RD-FL-7 · `task/rd-fl-7` · [PR #52](https://github.com/karolskolar/gorifi/pull/52) · **Subscription + invite modals, and the anti-leak regression net.** Suite **324 → 339 passed / 3 skipped** (`portal-subscription-invite.spec.js` 12, `portal-session-boundary.spec.js` 3). ⚠⚠ **A MODULE-06 BLOCKER was found and defused here, and the reviewer's own prescribed fix would have shipped broken.** `NeoModal`'s × carried `aria-label="Zavrieť"`, colliding with the **spec-verbatim** footer label in `03 §UC-FL-011`, `05:375` **and** `06 §UC-GX-005` — `getByRole('button', {name:'Zavrieť'})` resolved to **2**. Three **pre-existing, non-editable** specs already use that unscoped query against modals module 06 puts on this very shell (`guest-status.spec.js:664`, `guest-order.spec.js:865`, `guest-lead-capture.spec.js:466`), and `06 §UC-GX-005` names `guest-status.spec.js:653–665` as an acceptance criterion **while mandating a "Zavrieť" footer** — a spec requiring a strict-mode violation with no legal escape. The reviewer prescribed `aria-label="Zavrieť dialóg"`; the implementer **measured that it still collides**, because Playwright matches `getByRole(…, {name})` as a whitespace-trimmed **case-insensitive SUBSTRING** unless `exact: true` — which those three specs don't pass. It failed 4 tests on the first gate run. ⚠ **The rule is therefore NON-CONTAINMENT, not inequality**, and since the three specs can't be edited to add `exact: true`, only a **synonym** works: **`aria-label="Zatvoriť dialóg"`**. Names now read `["Zatvoriť dialóg","Kopírovať","Zavrieť"]`, count 1. Recorded in §UC-DS-010 with the substring mechanism and the failed attempt, so nobody "simplifies" it back. That also let one of the three spec edits be **reverted**. ⚠ **On the "zero existing spec edits" property — it was self-contradictory and is now reformulated in `03 §UC-FL-013`.** `UC-FL-013` said "no spec files modified by this task" while `UC-FL-011` **in the same document** mandates moving the invite fetch error out of the page banner, invalidating an RD-FL-3 assertion. New rule: **pre-existing specs are immutable**; a *pipeline-authored* spec may be edited only when a module spec mandates the behaviour change or the assertion is structurally unsatisfiable against a mandated primitive — re-pointing, never weakening, with the clause cited and the count reported. Final count here: **1 file, 3 hunks**, both surviving edits forced (`input`/`toHaveValue` → `.copyrow .val`/`toHaveText`, since UC-DS-011 makes the value box a `div`; and the relocated invite error, which strictly *strengthens* — it now also asserts the page banner stays clean). **The session-boundary net** (`portal-session-boundary.spec.js`) enumerates the **surface, not the refs**, so it survives later rows adding state: walk the authenticated surface filling `SENTINEL-<n>`, log out, log B in, re-walk, assert no sentinel in `page.content()`, every password-ish field masked, every value `''`-or-B's-own, and — added on review — **every visible `[aria-pressed]` reads `"false"`** (catches the `showLoginPassword` class for *any* future reveal toggle without naming a ref, with an anti-vacuity guard since only the login screen carries one today). ⚠ **Its reach is honestly stated in its own header: of the six leaks that motivated it, it would have caught TWO.** It misses state that renders but is neither a field value nor a password type (`voucherResolved`, `showArchive`, `guestCounts`, and all of this row's own clears), and **a green run is NOT evidence for the `show*Modal` clears** — the scrim makes the logout control unclickable while a dialog is open, so those are by-construction defence this file cannot falsify. Bites verified three ways, each a rebuild: the implementer mutated `showLoginPassword` and `changeCurrentPassword`, the reviewer independently mutated `setupUsername`. Two more `switchUser()` residuals closed: **`setupSaving`** — worst of the class, because that dialog is **auto-raised** for the next person and both footer buttons are `:disabled="setupSaving"`, so B gets an inert dialog they did nothing to summon (reachable via "submit → Esc → logout"; Esc closes a `closable` dialog regardless of a disabled footer) — and `changePasswordSaving`. The implementer also cleared **`profileSaving`, which I had deferred**, arguing the deferral's basis was the *error-surface* convergence seam while a boolean in-flight flag raises no such question, and leaving one member of a class uncleared is an exception the next sweep must re-derive. **Accepted.** `inviteError` (a dedicated ref rather than the shared `error`) and `inviteSeq` (the `guestCountSeq` precedent — an invite code is an identity, and clearing the ref cannot stop a late response) both reviewer-approved. Reviewer verdict **approve** with a major follow-up, applied. Note: `changePasswordSaving`/`profileSaving` are fixed but **not** test-covered — only `setupSaving`'s dialog auto-raises, so only it admits a test pinning real user-visible harm; RD-FL-8's extraction retires all three.

2026-08-08 · RD-FL-8a · `task/rd-fl-8a` · [PR #53](https://github.com/karolskolar/gorifi/pull/53) · **The `switchUser()` leak class is over, by construction.** `FriendPortal.vue` 2117 → 882 lines; the authenticated surface is now `FriendPortalSession.vue` (1412), mounted `v-if="authState === 'authenticated'"`. Net **+487 / −1526**. Suite **339 → 342 passed / 3 skipped**; **zero pre-existing specs touched** (four pipeline-authored files: two pure additions, two re-points with the mandate cited per §UC-FL-013). ⚠ **`switchUser()` shrank from ~35 assignments to 11**, and the reviewer walked the old block line by line to confirm nothing that should still be cleared was dropped. The survivors are the credential store, appbar identity and the login form — none reachable by an unmount. ⚠ **The extraction silently closed a SEVENTH leak class** the old `switchUser()` never cleared at all: `showVoucherModal`/`currentVoucher`/`pendingVouchers`, `shareCycle`, and the four forced-gate refs. The voucher one **rendered another friend's € amount over the login screen**. ⚠ **The `:key` story changed twice under measurement, and the second correction matters.** Removing `:key` while keeping `v-if` leaves the suite **green** — the unmount is what protects today. But `:key="friendId"` was **incompletely calibrated**: `entry` (the handshake payload: cycles, subscriptions, gate flags) is seeded into the child **once at setup** and produced *before* the child exists, while `authenticatePersonal` writes `selectedFriendId` and only *then* awaits `beginSession()`. So Vue flushes the key at the first `await` and a friend-switch-in-place would mount with the **previous friend's cycle list**, permanently, firing `loadGuestCounts` over A's cycle ids with B's token — **the key protects the refs and leaks the payload.** Now keyed on `sessionSeq`, bumped inside `beginSession()` one statement before `entry.value`, so it fires exactly when a fresh payload is ready and also covers the same friend re-authenticating. Proved in a standalone Vue page reproducing the sequence: identity-key → `{who:'A'}`, sessionSeq-key → `{who:'B'}`. ⚠ **`FriendPortalSession` is a FRAGMENT (9 root nodes), so `v-show` on it is a Vue no-op** — that fact is load-bearing (it is why `v-show` can never be the "cheap" refactor here) and is now stated in the file. ⚠ **A comment block asserting a measured `<KeepAlive>` matrix was REMOVED rather than repeated**: the implementer could not reproduce it (the only placement that keeps the `v-if` chain compiling puts `KeepAlive` *inside* the branch, where it caches nothing) and retracted their own earlier claim instead of standing on it. Replaced with the timing experiment, which proves the same point. **A10 grew from 3 classes to 10** on review — `.field-lbl`, `.field-help`, `.sub`, `.appbar .chip`, `.badge`, `.banner`, `.copyrow .val`, `.tab`, `.tbl td`, `.statuspill`. Stopping at three would have left **module 03's own shipped surface** inflated at the exact moment RD-FL-8b baselines it, and `.tab` is **module 04's sticky strip** — measured 45 px against the canon's 42 `min-height`, i.e. at 1.5 the content alone exceeded the declared minimum, so `min-height` had **stopped governing at all**. The port now reproduces the canon **exactly on all 17 probed fixtures**; every delta was a reduction; an unpredicted knock-on is that `.copyrow`'s copy button was being *stretched* by the inflated `.val` and returns to its canon 44. Excluded and named: `.tabbadge` (delta 0 — fixed `height` + `inline-flex` centring). ⚠ **Measurement trap worth keeping: `normal` is FONT-METRIC driven, and Google Fonts only downloads a family when something uses it** — a first probe read `.copyrow .val` 3 px high purely because Courier Prime had not swapped in. Force-load fonts on both sides before comparing. Also fixed: the guest-count worker queue kept **dispatching after logout** (~132 token-less 401s on the e2e DB) because `onBeforeUnmount` cancelled the username debounce but never bumped the sequence counters — the new concurrency cap was *masking* it, not fixing it. ⚠ **Two process errors the implementer flagged against themselves, both worth adopting:** piping `vite build` through `| tail -2 &&` takes **`tail`'s** exit status, so three "mutation" probes silently ran against a **stale bundle** and produced non-discriminating results; and dropping the `RATE_LIMIT_*` budgets on a server restart produced 29 unrelated failures, exactly as `e2e/README.md` warns. **The orchestrator had the same `| tail` habit and now checks build/suite exit codes explicitly.** Reviewer verdict **approve** after one revise round (1 major, 5 minors). Seams for RD-FL-8b and 04–06: the `entry` handoff pattern (parent probes, hands the payload down, child seeds once) **does not survive a mid-session identity change** — if module 04 adopts it for `FriendOrder`, key on the handshake; a fragment child silently no-ops any wrapper directive; and the page-level `error` banner is now single-writer and permanently behind the voucher scrim, so whichever row next touches that modal should give it an in-modal surface and retire the banner with its last writer. ⚠ **New house rule, now in the spec: no HTML comment may live in `BrandChrome.vue`'s template** — Vue keeps template comments in the DOM in dev builds (`comments` defaults to `__DEV__`), and the first cut's slot rationale rendered into the markup. It codifies existing practice: all six `neo/` primitives already carry zero template comments. Reviewer verdict **approve**, one round, one minor (the AC wording, fixed). Two notes for later: `neo-control-metrics.spec.js` awaits `document.fonts.ready` but the mono face loads lazily on first use, so the `inp mono` row measures against the fallback (48 vs the canon's 46) — harmless while it only asserts `line-height:normal` and `≥46`, but tightening it to `near(…, 46)` would flake; and `onTitlesKeydown` fires on bubbled keys, so a future consumer nesting a text field inside `#titles` while `titlesAction` is set would have Space swallowed — invalid ARIA anyway, and the prototype's own handler bubbles identically, so it wants an `e.target !== e.currentTarget` guard only if that composition ever becomes real.

2026-08-09 · RD-GX-4 · `task/rd-gx-4` · `319c08b` · [PR #65](https://github.com/karolskolar/gorifi/pull/65) · **The last row — invite CTA, g-dead ×3, status-404, and the effort's closeout.** `GuestInviteRequest.vue` restyled to four states (folded pink `--hi` card → form → done → already-requested); behaviour is GSO-T10's and unchanged. A real fidelity defect found: ⚠ Tailwind preflight's `svg{display:block}` breaks the prototype's inline padlock badge onto two lines (41.14 → 52.41px tall, glyph 6.13px off centre) — fixed with `inline-flex` + a 4px gap, mutation-proved. ⚠ **Two earlier rows' footer claims measured FALSE and corrected at their call sites** (RD-GX-1's `.gx-foot-btn` at every covered width; RD-FO-4's `.fo-foot-btn` for 1 of its 4 footers). Spent sanctioned edit #1, the 7th and last. Suite 513 → **525 passed / 3 skipped**. Review: approve, 3 minors (comment/ledger accuracy), all fixed pre-commit. ⚠ Gate harness note: the run needs `CORS_ORIGIN=<gate origin>` and `e2e/seed.mjs`, or ~31 unrelated-looking UI specs fail and read like a regression — see the closeout below.

2026-08-09 · RD-DS-6 · `task/rd-ds-6` · [PR #66](https://github.com/karolskolar/gorifi/pull/66) · **The restyle shipped in the WRONG TYPEFACE — found on staging, after the whole effort merged.** The deployed nginx sends `style-src 'self'` + `font-src 'self' data:`, which blocks the `fonts.googleapis.com` `<link>` RD-DS-1 introduced and the `fonts.gstatic.com` woff2 behind it: `document.fonts.size === 0`, everything fell back to `Inter`. ⚠ **No test could have caught it — every gate in all 25 rows ran against Express on `localhost:3997`, with no nginx and therefore no CSP header.** Fixed by self-hosting all three families (9 woff2, 118,552 B) rather than relaxing CSP. ⚠ **Both `latin` AND `latin-ext` per family** — Slovak č/š/ž/ľ/ť/ď/ň are U+0100–017F, so a `latin`-only ship falls back mid-word. Reviewer independently confirmed all nine files are **MD5-identical** to Google's bytes and every caron glyph is present. ⚠ **A second asset was broken by the same CSP**: `InviteRegister.vue`'s Goriffee logo — and its upstream URL **404s** too, so it had been dead twice over; replaced with Goriffee's current official SVG (**colour treatment differs — design sign-off pending**). New `self-hosted-fonts.spec.js` (9 tests) serves the built app under the **verbatim production CSP** and sweeps `/`, `/invite/:code`, `/g/:token` for any non-same-origin subresource. Suite 531 → **534 passed / 3 skipped**.

---

## Module 06 closeout — and the end of the effort (2026-08-09, RD-GX-4)

**All 25 rows `[x]`. Modules 02+03, 04, 05 and 06 closed. Suite 525 passed / 3 skipped.**

### The sanctioned-edit ledger — 7 spent, 7 permitted, none beyond

| # | spec item | file | row |
|---|---|---|---|
| 1 | 06 §UC-GX-011 item 1 | `guest-lead-capture.spec.js` `/nabudúce/i` → `/objednať sami/i` | RD-GX-4 |
| 2 | 06 §UC-GX-011 item 2 | `guest-order.spec.js` + `guest-status.spec.js` (two halves) | RD-GX-2 / RD-GX-3 |
| 3–4 | 06 §UC-GX-011 items 3–4 | `guest-order.spec.js` | RD-GX-2 |
| 5 | 06 §UC-GX-011 item 5 | `guest-status.spec.js` | RD-GX-3 |
| 6–7 | 05 §UC-KG-007 item 1 | `guest-link.spec.js`, `guest-admin-view.spec.js` | RD-KG-* |

No `.only`, no added `test.skip`, no deleted or weakened assertion anywhere in the effort.

### What the closeout actually proved

- ⚠ **`git diff --stat 7c3f85e HEAD -- backend/` is EMPTY.** The whole 25-row restyle
  touched no route, no schema, no helper, no middleware. Every GSO-T1..T10 invariant in
  `CLAUDE.md` still holds by construction, not by inspection.
- `frontend/src/api.js` was **never touched by any row** (`git log 7c3f85e..HEAD -- …`
  is empty), so no request shape, header set or endpoint moved.
- `lib/guest-cart.js` changed **exactly once**, in RD-GX-3 (`d08784a`, the `formatPrice`
  deletion with zero importers), and is unchanged since.
- **Admin invariance**, measured against a real build of `7c3f85e` with both servers
  seeded identically at 1280×900: 12 of 14 admin routes **byte-identical**; the 2 that
  differ are data, localised to a random `invite_code` cell (485px) and one digit of a
  port number (71px). ⚠ The run carried a **positive control** — `/` and `/g/:token`
  reported 90.04% and 100% different — without which "identical" is unfalsifiable, and
  a same-server-twice control established a 0-pixel noise floor.

### ⚠ Two false measurement comments, corrected at their call sites

`.m-foot .btn` is `flex:1` (grow 1, basis 0) with the flexbox default `min-width:auto`,
so each button's **min-content is its floor**, and the ≤400px padding relief is
invisible only while the even split already clears **both** floors. That is a narrower
condition than three rows assumed.

- **RD-GX-1's `.gx-foot-btn` claim was FALSE** — "verified: resolved widths unchanged at
  378px" was wrong; the relief moves 12px from the accent button to `Späť` at every
  width the query covers.
- **RD-FO-4's `.fo-foot-btn` claim was FALSE for 1 of its 4 footers** — `Spôsob
  prevzatia` goes 133.95/156.05 → 145/145 at 378px, the same symmetric shape RD-GX-3
  found. The other three genuinely are invisible (both floors already under the split).

Both call sites now carry the measured table rather than the claim. The lesson for any
future footer: ⚠ **`.btn` is `nowrap` + `flex:1`, so a too-wide row gives NO degradation
signal** — no shrink, no wrap, no ellipsis. Measure min-content, never the flex-resolved
width, and never generalise one footer's verdict to another's button pair.

### The gate harness — three ways it lied, all mine

Recorded because every one produced a *plausible* wrong number:

1. **A stale server on the port** silently served a `backend/public` that had been
   deleted and rebuilt underneath it → a false 236/3. Confirm the port is free first.
2. **`| tail` takes tail's exit code**, not the build's — three mutation probes once ran
   against a stale bundle. Always `echo "EXIT: $?"` on its own.
3. ⚠ **`CORS_ORIGIN` must include the gate's own origin.** The backend allowlists
   `gorifi.skolar.sk` / `gorifi-dev.skolar.sk` / `localhost:5173` only, so a same-origin
   SPA on `localhost:3997` gets **500s on every XHR** and renders a blank body — which
   surfaces as 31 unrelated-looking UI failures (admin login, colleagues panel,
   distribution) and reads exactly like a regression. Also: **`e2e/seed.mjs` is not
   optional** on a fresh `DB_PATH`. And ⚠ `pkill -f` matches its own shell (exit 144) —
   chain nothing after it.

### Recorded residuals (none blocking)

- ⚠ **`410 Gone` is browser-cached.** The backend sends no `Cache-Control` on the dead-link
  410, so a reactivated link still shows the dead screen until a hard reload. Pre-existing
  and **backend-owned**, therefore out of scope for a frontend-only effort;
  `guest-invite-dead.spec.js` documents it as the reason each variant gets its own link.
- `api.js:352` still quotes the old fold line "Chcete si nabudúce objednať sami?" in a
  comment. Left stale **deliberately** — `api.js` showing no diff is a closeout condition,
  and a comment cannot mislead a test.
- `GuestOrderStatus.vue`'s dead card also renders for 5xx/network and then advises
  "Skontrolujte, či je odkaz skopírovaný celý", which is wrong for a server error.
  `GuestOrder.vue` got a reason-aware description this row; its twin did not, because
  §UC-GX-010 only specifies the 404 case there. Spec-conformant, worth a follow-up.
- **Spec inaccuracies found**: §UC-GX-009's header ("root keeps `data-testid="invite-cta"`")
  contradicts its own items 3/4, which put `invite-done`/`invite-requested` on the state's
  root — one element cannot hold two testids. Resolved by keeping `invite-cta` on each
  state's root and moving the state testid to the text span (a wrapper was rejected: an
  unclassed block in a gapped flex column is the A10 preflight hazard). Both readings
  satisfy every shipped assertion. Also, §UC-GX-011's "full inventory" omits `cartbar`.
